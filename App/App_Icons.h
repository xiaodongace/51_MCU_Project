/*
 * App_Icons.h - 任务清单用的图标（7 张，每张 128x64 整屏，画在**主屏 SPI** 上）
 *
 * 用途：在第 1 级任务清单里，光标指到哪个任务，主屏就显示哪个任务的图标。
 *       整屏只有一张图、不放文字（任务名在同一块屏的菜单行上已经有了）。
 *
 * 【2026-09-23 修正】原来这里写的是"64x48、画在 I2C 副屏上、384 字节"，
 *   三处都不对，实际是：
 *     尺寸 128x64（整屏）；画在**主屏**（App_Display.c 的 sub_blit，
 *     坐标宏 ICON_X=0 / ICON_PAGE0=0 / ICON_W=128 也在那里）；
 *     每张 1024 字节（= 128 列 x 8 页，SPI OLED 的页格式）。
 *   7 张共 7168 字节，是 code 区最大的单块常量（占 12.4%）—— 想省 Flash 先看这里。
 *
 * 数据按"页格式"存：每 128 字节是一页（8 行像素），7 张 x 8 页 = 56 页。
 * 直接 memcpy 进 SPI 的显存缓冲即可，不需要任何解码。
 */
#ifndef __APP_ICONS_H
#define __APP_ICONS_H

#include "Config.h"

/* 取第 idx 张图标，返回指向 code 区的指针（1024 字节，页格式） */
u8 code *Icon_Get(u8 idx);

#endif
