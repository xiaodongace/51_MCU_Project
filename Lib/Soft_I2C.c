/*
 * Soft_I2C.c - 【2026-09-22 已废弃，文件保留仅作说明，不含任何函数】
 *
 * 这是 STC 官方库里的"软件模拟 I2C"驱动。本工程不用它：
 *   · 副屏（I2C OLED）走的是 Driver/I2C_OLED/I2C_OLED.c，自带一套自包含的
 *     软件 I2C（I2C_Start / Send_Byte / I2C_WaitAck），不依赖本文件；
 *   · 时钟芯片 PCF8563 走的是**硬件 I2C**（Lib/I2C.c 的 I2C_WriteNbyte / I2C_ReadNbyte）。
 *
 * 为什么不直接从工程里移除：
 *   Keil 的 UV4 命令行编译会把工程的 <File> 列表写回 .uvproj，
 *   手工删掉的文件节点会被它恢复。所以改成"清空文件内容"这种稳的做法：
 *   节点还在，但里面没有函数 —— 编译后不占任何 Flash。
 *   （App_Game.c 用同样的处理。）
 *
 * 保留在工程里的唯一代价：编译时会多编一个空文件（约 0 秒）。
 *
 * 以后若真要用软件 I2C：从 STC 官方库把函数体拷回本文件即可。
 */
