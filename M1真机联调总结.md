# M1 真机联调总结

> 时间跨度：2026-09-17 22:00 ~ 2026-09-18 20:00（共 13 轮真机迭代）
> 工程：`D:\Develop\embedded-project\51_MCU_Alarm\Project.uvproj`
> 当前状态：**功能全部跑通，编译 0 Error / 0 Warning**

---

## 一、当前验收状态

| 项 | 状态 |
|---|---|
| 编译 | `0 Error(s), 0 Warning(s)`，`UNRESOLVED = 0`，浮点库引用 = 0 |
| 尺寸 | `code = 40740` / 可用 60 KB，余量约 20 KB |
| 三块屏走时 | ✅ 正常（SPI 大字时间、数码管时分秒、副屏秒） |
| 温湿度 | ✅ NTC 温度、DHT11 湿度均正常刷新 |
| 独立按键 | ✅ 短按 / 长按 / 页面切换全部正常 |
| 矩阵键盘 | ✅ 校时、闹钟编辑输入正常 |
| 旋转电位器 | ✅ 0~10 档，自学习量程后到底 0、到顶 10 |
| EEPROM | ✅ 设置与 8 组闹钟掉电保存 |
| 蜂鸣器 | ⚠️ **当前全局静音**（`BUZZER_ENABLE 0`，按要求最后处理） |
| 日期 | ⚠️ 显示 2000-01-01（**按键校时只设时/分**，年月日留待 UI 重构） |

---

## 二、13 轮里修掉的 9 个真问题（按发现顺序）

| # | 问题 | 根因 | 症状 |
|---|---|---|---|
| 1 | 数码管一位都不亮 | `Nixie_ScanInit()` 定义了但**全工程无人调用** | 数码管全灭 |
| 2 | SPI 主屏漆黑 | 6 根软件模拟 SPI 线**没配端口模式**，停在准双向口，弱上拉在 1~3MHz SCL 下上升沿爬不到门限 | 主屏不亮 |
| 3 | 按键无任何反应 | **Timer3 从未被初始化**，`g_sysTick` 恒为 0，20ms 消抖判据永不成立 | 按键全无响应 |
| 4 | 副屏整屏垂直滚动 | I2C 每字节一次事务 → 丢字节 → 数据字节被当成控制字节 → 落进 `0x40~0x7F` = "设置显示起始行" | 整屏上移、某行被劈成两半 |
| 5 | 时间三屏全静止 | PCF8563 的**日/月/年寄存器为 0**，读出来判为非法；而我按"开机不重设时间"的规则**读失败时也没写** | 时间冻结 |
| 6 | **1ms 节拍变成 217ms** | `Driver/Timers.c` 配置 Timer3 时**漏赋 `TIM_PS`**，库函数 `TM3PS = TIMx->TIM_PS` 把内存垃圾值写进预分频寄存器 | 按键无反应、时间冻结、闹钟不响 |
| 7 | 蜂鸣器唱歌停不下来 | 设置页曲目项只能"换一首并播放"，**没有停止路径** | 按任何键都在唱 |
| 8 | 副屏花屏且永不恢复 | 自愈只重写显存，**修不了 SSD1306 的控制器寄存器** | 花屏后长期不恢复 |
| 9 | 板子自动重启 | **热的 printf 把 RTX51 任务栈踩穿**（`FREE_STACK` 仅 20 字节），走 `?RTX_STACKERROR` 复位 | 读到 `y=259 mo=93 94:08:07` 这类算术上不可能的值，随后自动重启 |

### 其中三个值得单独记住的教训

**① "逐字照抄"不等于正确。**
问题 3 和问题 6 都是我把某份代码"照抄"进工程——**而那份源文件本身就有 bug**
（用户原工程的 `Timers.c` 同样漏了 `TIM_PS`）。照抄只保证"和源文件一致"。

**② 结构体字段漏赋值是最隐蔽的一类。**
局部结构体未赋值的字段 = 内存垃圾，会被库函数原样写进硬件寄存器。
且它落在 C51 的可覆盖段，**值随代码布局变化** → 表现为"以前好用、改完就坏"。
本工程最后做了全量审计，又揪出两处（`COMx_InitDefine` 漏 `Morecommunicate`、
`I2C_InitTypeDef` 漏 `I2C_SL_ADR`/`I2C_SL_MA`）。

**③ 诊断手段本身会成为故障源。**
问题 9 是我自己加的诊断 printf 引发的。判断"内存被踩"的快捷方法：
**看打印值在算术上是否可能**——BCD 掩码后的月最大 25、时最大 45，
出现 93/94 就一定是内存问题，别再查外设。

---

## 三、判断方向时走过的两次弯路（记录下来避免重犯）

**弯路 1：把"USB 冷启动时间不走"当成首要嫌疑，猜了三轮。**
先后猜过"PCF8563 电源未稳"（延长重试窗口）、"I2C 总线被从机拉住"（加 9 时钟解锁），
都不是根因。真正有用的做法是**加一行参照系诊断**——在 TASK_SENSOR 里打印 `g_sysTick`，
因为它的 1 秒靠 RTX51 心跳数出来、**不依赖 g_sysTick**，天然是可靠参照。
一行数据（每 5 秒 +23）就直接锁定了 Timer3。

**弯路 2：凭"注释说的"下结论。**
曾按 `I2C_config` 的注释认为 `I2C_Speed = 13` 就是 400 kHz，也按注释认为
"开机不要重设时间"。**代码才是真值**——后来逐行读 `Lib/I2C.h:27` 的
`I2C_SetSpeed` 宏、读 `Lib/Timer.c` 的 `Timer_Inilize()` 才发现真相。

**⇒ 教训：停止凭记忆和注释判断，去读实现。用户 09-18 明确要求过这一点。**

---

## 四、下一阶段：UI 重构（方向已确认）

用户 2026-09-18 定的方向：**SPI 利用率拉满，I2C 只做点缀。**

| | SPI 主屏（带硬件字库，汉字不占 Flash） | I2C 副屏（需软件取模） |
|---|---|---|
| 主界面 | 大字时间 + 日期 + 温湿度 | 一行状态（闹钟开启数 / 音量） |
| 闹钟列表 | **中文**列表，带开关状态 | 滚动条 + 序号 |
| 闹钟编辑 | **中文**字段名 + 大数字 + 当前字段高亮 | 一行操作提示 |
| 番茄钟 | **中文** + 大字倒计时 | 阶段（专注/休息） |
| 设置 | **中文** 6 项 + 当前值 | — |
| 响铃 | **中文**"闹钟响铃" + 停止/贪睡提示 | — |
| 校时 | **完整年月日时分秒**（补掉当前的缺口） | — |

**同时要做的三件事**：
1. 补完整校时页（当前只能设时/分，日期不对）
2. 每页加操作提示（解决"不知道 I2C 屏怎么操作"）
3. 蜂鸣器解除静音（`Buzzer.h` 的 `BUZZER_ENABLE` 改回 1）

**资源盘点**（做之前先算清楚）：
- Flash：余量约 20 KB。16×16 汉字 = 32 字节/个 → 副屏取模可放约 600 个，实际用 30~60 个够
- 软件取模工具与教程：`day11/Resource/中景园电子ZJY096S08Z0WG01技术资料/03-OLED取模教程`
- RAM：`data = 57.7`、`xdata = 1761`（8 KB 可用），有空间但**任务栈很紧**——
  加代码时务必盯住栈深度，**不要再往热路径塞 printf**

---

## 五、给后续开发的三条硬约束（血泪换来的）

1. **宁可少做，也要先保证三块屏和按键可用**——功能面塌了，别的都无从验证。
2. **改完必须三重验证**：编译 0/0 + `UNRESOLVED = 0` + 读 `.m51` 看库引用与尺寸。
3. **一次只改一处**。13 轮里有两次"改多了"导致新问题掩盖了老问题，白白多花两轮。
