# UI 改造任务清单（2026-09-19 定稿 · 逐点完成）

> **执行规则（用户第 9 点）**：每一点彻底完成、真机验证通过之后，才开始下一点。
> 每点单独编译（`0 Error / 0 Warning` + `UNRESOLVED = 0` + 浮点库 = 0）、单独出一版 hex、单独烧录验证，不合并。

---

## 总览

| # | 项目 | 状态 | 卡在哪 |
|---|---|---|---|
| **1** | 第 2 级只显示当前任务行 | ✅ **已完成**（待上机） | — |
| 2 | 任务图标：菜单光标所到之任务 → I2C 显示图 | ⏸ | 图标素材从哪来 |
| 3 | 「0 日期和时间」+ 校时交互重做 | ⏸ | 输入格式与删除计数规则 |
| 4 | 「1 闹钟」列表样式（选中靠左/其余靠右） | ⏸ | 时间格式是 HH:MM 还是 HH:MM:SS |
| 5 | 「2 番茄钟」列表样式（同第 4 点） | ⏸ | 列表内容是什么 |
| 6 | 「4 云台」半圆仪表盘 + 指针 | 🟢 就绪 | — |
| 7 | 「5 设置」+ 电位器音量/震动强度固化 | ⏸ | 震动强度要不要真驱动马达 |
| 8 | 空闲 30 秒自动回主菜单 | ✅ 已存在（可调） | — |
| 9 | 本清单 | ✅ | — |

---

## 点 1 · 第 2 级只显示当前任务行 ✅

**要求原文**：「SPI 屏幕在第二级菜单每次点击确认进入任务之后，SPI 屏幕只显示第一行当前任务选项，下面全部消失，
只有当返回到第二级菜单时，才显示全部选项。**注意第 3 点不需要遵循第 1 点。**」

**实现**：`App_Display.c` 的 `main_draw_menu()`

```c
u8 rows = Menu_IsOpen() ? MENU_ROWS : 1;   /* 第 1 级 4 行；第 2 级只 1 行 */
for (row = 0; row < rows; row++) { ... }
```

- 第 1 级（任务清单，`Menu_IsOpen() == 1`）：画 4 行
- 第 2 级（已进任务）：只画第 0 行 = 当前任务名，下面由 `SPI_OLED_Clear()` 留空
- 第 3 点（校时）将来自己单独处理，不受本条约束

**验证**：编译 `0 Error / 0 Warning`，`code 43128`。
**上机判据**：进「闹钟」后主屏只剩一行 `> 1 闹钟`，下面空白；按 KEY4 返回后 4 行全部回来。

---

## 点 2 · 任务图标 ⏸

**要求原文**：「在第二级菜单时，SPI 屏幕中"`>`"当前指向的任务，对应的 I2C 屏幕会显示当前任务的图片，
图片的样子最贴切当前任务的名称。在主菜单时，I2C 屏幕显示内容。」

**要做**：7 个任务各一张 I2C 屏图标。I2C 一屏 128×64，扣掉第 1 行标题，可用区约 **128×48**。

**待确认 —— 图标素材从哪来？**

| 选项 | 说明 |
|---|---|
| **A** | **我用 Python 自己生成取模数组**（在代码里画简单图形：铃铛 / 番茄 / 尺子 / 齿轮 / 半圆仪 / 方块）。不需外部素材，纯代码，Flash 占用可控 |
| B | 你提供图片（PNG/BMP 都行），我用取模工具转成 C 数组 |
| C | 用现成图标字库（暂不推荐，本板没有） |

**成本估算**：每张 48×32 = 192 字节；7 张 = **1344 字节 Flash**（余量约 20KB，占 6.5%）。

---

## 点 3 · 「0 日期和时间」+ 校时交互重做 ⏸

**要求原文**：
- 「0 时钟」改名为「**0 日期和时间**」
- 点进去后选择「修改年月日」或「修改时分」——**选择项显示在 SPI 屏上**
- 修改反馈显示在 I2C 屏上：**年内容修改显示在第一行，月日修改在第二行**，I2C 下面显示当前年月日
- 没点确认修改什么时间时，I2C 仍显示图片；点击确认后 I2C 显示修改反馈、SPI 只显示当前选项、其他选项全部消失；返回上一级时 I2C 重新显示图片
- **KEY4 用来删除最后输入的一位数字**；删完全部数字后取消修改并返回上一级；此时 KEY4 只能删数字
- 输入全为 0 时，删除要**计算输入了几位 0**
- 矩阵键盘此时才能用来输入

**待确认**：
1. **输入格式**：年月日 = 8 位数字（`YYYYMMDD`）？时分 = 4 位（`HHMM`）？
2. **删除计数**：输入「0000」时要按 4 次 KEY4 才退得出去——这个理解对吗？
   （即：单独记一个"已输入位数"计数器，每按一位 +1、每删一位 −1，与数值是否为 0 无关）
3. 现在「主界面矩阵键盘直接输 HHMM 校时」这条**快捷方式要保留还是取消**？
   （新交互是"进日期和时间任务后才能输"）

**关于"修改日期后自动匹配周几"的提问** —— 见本文件末尾《附录 A》。

---

## 点 4 · 「1 闹钟」列表样式 ⏸

**要求原文**：「KEY2 键按下，I2C 第一行只显示 `Alarm Edit`，下面显示为三个闹钟，闹钟都为时分秒，
而且**全部字体靠右显示，只有第一个闹钟字体靠左显示**，靠左显示代表我当前可以设置这个闹钟，
当按下上或下按键时，对应的闹钟字体靠左显示，其他闹钟靠右显示。」

**要做**：
- I2C 第 1 行固定 `Alarm Edit`（不随子状态变）
- 第 2/3/4 行 = 3 个闹钟，当前选中的那个**靠左**（x=0），其余**靠右**（右对齐到第 128 列）
- KEY1/KEY3（上/下）切换选中项

**待确认**：
1. **「闹钟都为时分秒」—— 是 `HH:MM:SS` 吗？** 我理解闹钟一般只到分（`HH:MM`）。
   如果确实要秒，PCF8563 的硬件闹钟**不支持秒**（只有分/时/日/周），秒只能靠软件每秒比对。
2. 现在有 **8 组闹钟**（`ALARM_MAX = 8`），I2C 只能显示 3 行 ⇒ 需要滚动。确认按"选中项所在窗口滚动"来？

---

## 点 5 · 「2 番茄钟」列表样式 ⏸

**要求原文**：「"2 番茄钟"选项也可以和第 4 点相同」

**待确认**：**列表里的"各项"是什么？** 番茄钟本来只有一个倒计时。可能的理解：

| 选项 | 列表内容 |
|---|---|
| A | 专注时长 / 休息时长 / 长休息时长（3 项，选中靠左） |
| B | 历史完成的专注段记录 |
| C | 其它（你说） |

---

## 点 6 · 「4 云台」半圆仪表盘 🟢

**要求原文**：「确认后最后一行显示为 `Angle 180 DEG`，上面一行可以改为一个**只有边缘有线的半圆**，
里面有一个**粗针**，每次改变角度，粗针就改变角度。关于云台方面，**每次改变 10 角度**就很好。」

**要做**：
- I2C 第 1 行：任务名（沿用现在的 `GIMBAL`）
- 第 2/3 行（y=2..5，共 32 像素高）：半圆轮廓 + 指针
- 第 4 行：`Angle 180 DEG`
- 每次按键角度 ±10°（现在是 ±5°，改成 10）

**实现要点（不需要外部素材）**：
- 半圆用「圆弧像素点」在 RAM 缓冲里点出来（半径约 26，圆心在屏底中部）
- 指针 = 从圆心指向角度方向的粗线（2 像素宽）
- **只重画指针所在的那块矩形**（约 64 列 × 4 页 = 256 字节 ≈ 80ms @400kHz），不做整屏重画
- 半圆边缘是静态的，切页时画一次

---

## 点 7 · 「5 设置」+ 电位器固化 ⏸

**要求原文**：
- 「电位器调节**音量和震动强度**功能放在设置里面」
- 「每次调节完毕按下 KEY4 返回上一级菜单时把当前设置的**百分比刻进固定值**里面」
- 「当在外面旋转电位器时不会影响到已经设置好的百分比」
- 「电位器旋转的百分比进度设置为**每百分之一步进**」

**要做**：
- 设置项加「震动强度」；「音量」改为由电位器调节
- 在设置页里旋电位器 = 调整当前项的百分比（1% 步进）
- 按 KEY4 返回时把百分比存进 EEPROM（`Storage_SaveAll()`）
- 其它页面旋电位器**不再改动设置值**

**待确认**：
1. **震动强度要不要真的驱动马达？** 现在 `MOTOR_ENABLE = 0`（全局禁用）。
   当初禁它是因为怀疑马达百毫安负载拉低 3.3V 导致 OLED 花屏——但后来日志否掉了这个假设（马达从未被启动）。
   启用的话，要重跑一遍"马达开/关 + OLED 是否正常"的真机对照。
2. 现有 6 个设置项（音量 / 曲目 / 提醒方式 / 主屏开关 / 日出唤醒 / 贪睡时长）**保留吗**？加"震动强度"后是 7 项。

---

## 点 8 · 空闲自动回主菜单 ✅ 是已有的

**是我设的。** `App_Menu.c` 的 `Menu_CheckIdle()`：

```c
if (SysTick_Elapsed(s_lastActMs) >= (u32)(IDLE_BACK_HOME_SEC * 1000U))
{
    Menu_Home();
}
```

`App_Public.h:80` → `#define IDLE_BACK_HOME_SEC  30`（**30 秒**）。
响铃页 / 闹钟编辑页 / 番茄钟页**不参与**空闲返回（正在操作的事不能被弹走）。

要改秒数、或取消这个功能，说一声即可（改一个宏）。

---

## 点 9 · 本清单 ✅

---

# 附录 A · "改完日期自动匹配周几"是怎么实现的

在 `App/App_Clock.c` 里，用的是 **Sakamoto 算法**（坂本算法）：

```c
u8 Clock_WeekdayOf(u16 year, u8 month, u8 day)
{
    static u8 code t[] = { 0, 3, 2, 5, 0, 3, 5, 1, 4, 6, 2, 4 };  /* 月份修正表 */
    u16 y, sum;
    u8  m;

    y = year;
    m = month;
    if (m < 3) { y = y - 1; }        /* 1、2 月算作上一年的 13、14 月 */

    sum = y + y/4 - y/100 + y/400 + t[m-1] + day;
    return sum % 7;                  /* 0=周日, 1=周一, ... 6=周六 */
}
```

**原理**：公历的星期是以 **400 年为一个完整周期**的（400 年恰好 146097 天 = 20871 周整）。
把「年份贡献 + 月份贡献 + 日期贡献」全加起来，模 7 就是星期。

| 项 | 含义 |
|---|---|
| `y` | 年份；1、2 月要减 1 年（因为闰日加在 2 月末，得把 1/2 月划到上一年才对齐） |
| `y/4 - y/100 + y/400` | 这 400 年里的闰日总数 |
| `t[m-1]` | 12 个月各自的偏移（写死在表里） |
| `day` | 当月第几天 |

**用在哪里**：
- `Clock_Set()` 写时间时 → `t->week = Clock_WeekdayOf(t->year, t->month, t->day)` 一起写进 PCF8563 的周寄存器
- `Clock_Refresh()` 读回时**也重算一遍**（不信任芯片里的周值）
- 开机默认时间 `Clock_Init()` 里同样算一遍

**已真机验证过**：SPI 曾显示「2000-01-01 周六」，而 2000-01-01 确实是周六。

**纯整数、无浮点、无大数组** —— 12 字节的表在 `code` 区，不占 RAM、不链 `C51FPL.LIB`。
