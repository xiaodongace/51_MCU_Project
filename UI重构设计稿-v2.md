# UI 重构设计稿 v2 —— "主屏切任务 / 副屏看内容"（照 demo30）

> 定稿方向：2026-09-19 用户指定
> **主屏（SPI）= 任务切换界面；副屏（I2C）= 当前任务的具体内容。**
> 取代 `UI重构设计稿.md`（v1 的分工是"主屏做全部交互、副屏做状态栏"，已作废）

---

## 一、依据与真值（全部引原文）

| 项 | 真值 | 出处 |
|---|---|---|
| 参考工程的分工 | SPI 屏 = 菜单列表；I2C 屏 = 当前项内容 | demo30 `App_OLED.c`：`task_2` 画菜单、`task_3` 画内容 |
| 菜单行格式 | `sprintf(strbuff, "%c %d %s", (i ? ' ' : '>'), index, title)` | demo30 `App_OLED.c` `task_2` |
| 内容页格式 | `I2C_OLED_ShowString(0, 0, menu_arr[cur].content, 16)` + `switch` 派发 | demo30 `App_OLED.c` `task_3` |
| 主屏坐标 | x = 像素列 0~127，y = **页** 0~7（1 页 = 8 像素行） | `SPI_OLED_address()` |
| 主屏中文 | `SPI_OLED_Display_GB2312_string(x, y, text)`；汉字 16 列宽 +2 字节，ASCII 8 列宽 +1 字节，**x 自动递增** | `spi_oled.c` 函数体（本次实读） |
| 主屏清屏成本 | 软件 SPI，整屏约 1.3ms | `UI重构设计稿.md` v1 |
| 副屏能力 | **只能 ASCII**（7 个固定索引汉字）；整行 30~40ms @400kHz | `硬件真值表.md` §3/§6 |

**⇒ 因此"中文菜单只能放主屏、内容用 ASCII 放副屏"是本板硬件决定的，不是偏好。**

---

## 二、主屏版面（已实现，S1）

```
y=0  ┌────────────────────────────────────────────┐
     │ > 0 时钟                                   │   光标 + 序号 + 任务名
y=2  │   1 闹钟                                   │
y=4  │   2 番茄钟                                 │
y=6  │   3 设置                                   │
     └────────────────────────────────────────────┘
       ↑                                          ↑
      x=0                                      x=63 之后全空着
```

| 项 | 值 |
|---|---|
| 行数 | **4 行**（8×16 字体，每行占 2 页） |
| 行高 | y = 0 / 2 / 4 / 6 |
| 每行宽度 | 光标 8 + 空格 8 + 序号 8 + 空格 8 + 中文名 16×2 = **64 列**（屏宽 128） |
| 光标 | 首列画 `>`，非光标行画空格；**字号与正文同宽，不会错位** |
| 滚动 | 任务数 > 4 时窗口跟着光标走（如光标在 idx=4 → 窗口显示 1..4） |
| 子页面 | 光标**停在父任务那一行**（闹钟编辑→第 1 行、响铃→第 0 行、游戏各子页→第 4 行） |

**右侧 64~127 列目前全空**，可以放状态（闹钟开启数 / 电压 / 当前页面名），待你定。

### 任务清单（当前 5 项）

| # | 主屏菜单名 | 对应顶层页面 | 备注 |
|---|---|---|---|
| 0 | 时钟 | `PAGE_HOME` | |
| 1 | 闹钟 | `PAGE_ALARM_LIST`（子页 `PAGE_ALARM_EDIT`） | |
| 2 | 番茄钟 | `PAGE_POMODORO` | |
| 3 | 设置 | `PAGE_SETTINGS` | |
| 4 | 游戏 | `PAGE_GAME_HALL`（M2，6 个子页已预留） | 现在进游戏是占位页 |

---

## 三、副屏版面（当前 → 目标）

副屏固定 4 行 × 8×16。**第 0 行放"任务名"（= 页面名），第 1~3 行放该任务的具体内容。**

| 行 | 当前（所有页面共用同一套） | 目标（随任务变化） |
|---|---|---|
| 0 | 页面名 `CLOCK` / `ALARM LIST` … | 同（已是任务名） |
| 1 | `14:38:55 FRI` | **按任务变** |
| 2 | `2026-09-19` | **按任务变** |
| 3 | `VOL 60% SONG 1`（或校时输入 / 响铃提示 / 震动提示） | **按任务变** |

### 各任务的副屏内容（目标）

| 任务 | 行 1 | 行 2 | 行 3 |
|---|---|---|---|
| 时钟 | `14:38:55 FRI` | `2026-09-19` | `T29.4C H57% A2`（温湿度 + 开启闹钟数） |
| 闹钟 | `07:00 WORKDAY`（选中项） | `12:30 ON` / `18:00 OFF` | `ALM 2/8 ON` |
| 番茄钟 | `24:59` | `FOCUS #2` | `K1 START K2 BACK` |
| 设置 | `VOL 60%` | `SONG 1` | `K1 NEXT K3/K4 SET` |
| 游戏 | `GAME HALL` | `SNAKE` / `BRICK` / … | `M2` |

> **刷新策略不变**（这是上一晚用血换来的，不能动）：
> `Clear` 只在切任务时做一次；页内**只写变化的那一行**且**固定宽度**；
> 控制器初始化只在开机做一次；**不做周期性整屏重画**。

---

## 四、键位设计（**待你决断**，见第六节）

### 现状（S1 沿用，未改）

| 页面 | KEY1 | KEY2 | KEY3 | KEY4 |
|---|---|---|---|---|
| 时钟 | → 闹钟列表 | → 番茄钟 | → 设置 | （长按→游戏） |
| 闹钟列表 | → 编辑 | 返回 | 上移选择 | 下移选择 |
| 闹钟编辑 | 下一字段 | 返回 | 值 − | 值 + |

问题：主屏已经是"菜单"，但按键还是"一个键跳一个页"，**光标会从第 0 行直接跳到第 3 行**，不是菜单该有的行为。

### 方案 A（推荐 · 照 demo30）：菜单导航

| 位置 | KEY1 | KEY2 | KEY3 | KEY4 |
|---|---|---|---|---|
| **顶层**（页面栈深度 = 1） | 进入当前任务 | 回到"时钟"任务 | **上一个任务** | **下一个任务** |
| **子页面**（深度 > 1） | 下一字段 / 确认 | 返回上一层 | 值 − / 上移 | 值 + / 下移 |

- 优点：与主屏菜单一致；以后加任务不用改键位
- 代价：**丢掉"一个键直达某页"的快捷**（原来 KEY1 一下进闹钟）

### 方案 B：保留快捷跳页，菜单只做显示

- 优点：现状不动，改动最小
- 代价：菜单只是"看得见"，不是"能操作"；从设置回时钟要按返回

---

## 五、实施步骤（每步单独编译 + 单独上机验证）

| 步 | 内容 | 状态 |
|---|---|---|
| **S1** | 主屏改任务菜单（光标跟随当前页面） | ✅ **已完成**，`0 Error / 0 Warning`，`UNRESOLVED=0`，`code 41579 → 41893` |
| **S2** | 键位改方案 A（KEY3/KEY4 切任务） | 待你确认 |
| **S3** | 副屏"时钟"任务内容（温湿度 + 闹钟数替换 VOL 那一行） | 待 |
| **S4** | 副屏闹钟 / 番茄钟 / 设置三个任务的内容 | 待 |
| **S5** | 清理死代码：32px 大字字模与三个绘制函数 | 待 S1 定性后 |
| **S6** | 解除蜂鸣器静音 | 待 |

---

## 六、需要你决断的三件事

### ① 大字时钟怎么处置？（**这条最关键**）

主屏改成 4 行菜单后，**16×32 大字时钟在这块屏上没有位置了**。它现在以两种形式存在：
- 主屏会以 8×16 ASCII 出现在**副屏**的"时钟"任务里（时间 + 日期两行）
- 代码里 `main_draw_bigtime()` / `main_draw_date()` / `main_draw_env()` 三个函数
  与 `App_Font.c` 的 `DIGIT32[13][64]`（**832 字节 Flash**）目前**已经没人调用**，是死代码

| 选项 | 说明 |
|---|---|
| **A（推荐）** | 接受让位，照 demo30 —— 时间在副屏以 8×16 显示。S5 把死代码删掉，省约 1KB Flash |
| B | 保留大字时钟：主屏改成"任务条"而不是列表 —— `y=0..3` 大字时钟 + `y=4..5` 当前任务名 + `y=6..7` 操作提示。**但这就不是 demo30 的列表菜单了** |

### ② 键位走方案 A 还是 B？（见第四节）

### ③ 菜单项 5 个对不对？

现在是：时钟 / 闹钟 / 番茄钟 / 设置 / 游戏。要不要增删？

---

## 七、代价与边界（如实列）

| 项 | 说明 |
|---|---|
| 我做不到 | **烧录后的实际显示必须你上机验证**。我只能做静态三重验证（编译 / 链接 / `.m51`） |
| 副屏仍是瓶颈 | 只能 ASCII，中文内容（如"工作日""专注中"）只能用缩写或拼音，或转成 1~2 个取模汉字 |
| 副屏刷新成本 | 一行 30~40ms @400kHz。所以**页内只写变化行、不做周期重画**这条必须守住 |
| 主屏右侧留白 | 每行只用到 64 列，右侧 64 列空着，可以加状态，但也可以先不加 |
| 死代码 | S1 之后约 1KB 死代码在工程里（见第六节 ①），等你定性后清理 |

---

# 修订记录 · 2026-09-19 15:50（用户三项决断 + S2 落地）

## 一、用户的三项决断

| # | 决断 |
|---|---|
| 1 | **键位**：KEY1 = 上移，KEY3 = 下移，KEY2 = 确认 / 进入下一级，KEY4 = 返回上一级 |
| 2 | **任务清单加两项**：超声波 → 叫「**测距仪**」；舵机 → 我定名「**云台**」（可一句话改） |
| 3 | **大字时钟仍在原处**：它是**第 0 级**（最初始一级）。按 KEY2 进任务清单；KEY4 逐级返回时回到大字时钟 |

## 二、主屏的最终模型（三级）

```
第 0 级  大字时钟        ← 开机就在这（PAGE_HOME 且菜单未打开）
   │ KEY2
第 1 级  任务清单        ← KEY1/KEY3 挪光标，KEY2 进入，KEY4 回第 0 级
   │ KEY2
第 2 级  任务内部        ← 页面栈；主屏仍画任务清单，光标停在父任务那一行
   │ KEY4
（逐级返回，第 1 级再按 KEY4 回到大字时钟）
```

## 三、任务清单（7 项）

| # | 菜单名 | 页面 | 状态 |
|---|---|---|---|
| 0 | 时钟 | `PAGE_HOME` | 已有（第 0 级大字时钟） |
| 1 | 闹钟 | `PAGE_ALARM_LIST` (+EDIT 子页) | 已有 |
| 2 | 番茄钟 | `PAGE_POMODORO` | 已有 |
| 3 | **测距仪** | `PAGE_RANGE` | **本轮新建，内容下一步** |
| 4 | **云台** | `PAGE_SERVO` | **本轮新建，内容下一步** |
| 5 | 设置 | `PAGE_SETTINGS` | 已有 |
| 6 | 游戏 | `PAGE_GAME_HALL` | M2，占位 |

7 项 > 4 行窗口 ⇒ 窗口随光标滚动（光标在第 6 项时，窗口显示 3~6）。

## 四、S2 实现说明（两处值得记下的设计）

### 4.1 按键置换：只在一处翻译，不逐个分支改

新键位与工程原有的键位（K1=进入 / K2=返回 / K3=上移 / K4=下移）**正好是一个置换**：

| 新键 | 新含义 | 等价的旧键 |
|---|---|---|
| KEY1 | 上移 | 旧 KEY3 |
| KEY2 | 确认 / 进入 | 旧 KEY1 |
| KEY3 | 下移 | 旧 KEY4 |
| KEY4 | 返回上一级 | 旧 KEY2 |

所以在 `Menu_OnEvent` 里加一次 `key_remap()` 就够，**5 个页面、十几个分支一行都不用动**，一个都不会漏。
（逐个分支去改，改必漏一处 —— 这是上一晚用 18 轮换来的教训。）

**位置很关键**：置换必须放在"第 0/1 级处理**之后**" ——
上面是新写的代码（用新编号），下面是旧代码（用旧编号）。

**例外**：响铃是紧急状态，键位单独定义、不过置换 —— `KEY2 = 停止`、`KEY4 = 贪睡`。

### 4.2 任务清单的表只有一份

清单（名字 + 对应页面）放在 `App_Menu.c`（导航层），通过
`Menu_TaskName()` / `Menu_TaskPage()` / `Menu_IndexOf()` / `Menu_IsOpen()` / `Menu_CursorIdx()` 暴露；
`App_Display.c` 只负责画。**两边不会再各维护一份表、进而对不上。**

## 五、去掉的东西（如实记）

| 项 | 说明 |
|---|---|
| 主界面的「铃声/震动一键互切」 | 原来在 KEY4；第 0 级现在只有 KEY2 有定义。该功能仍在**设置页**里 |
| 长按进游戏 | 游戏已经是菜单项，不需要长按快捷方式了 |
| 副屏响铃提示 | `1 STOP 2 SNOOZE` → `2 STOP 4 SNOOZE`（跟着新键位） |

## 六、验证（本次只有静态三重验证）

| 项 | 结果 |
|---|---|
| 编译 | `0 Error(s), 0 Warning(s)` |
| 链接 | `UNRESOLVED = 0`；浮点库引用 = 0 |
| 尺寸 | `code = 42316`（S1 后 41893 → +423） |
| 符号 | `KEY_REMAP` / `MENU_ENTER` / `MENU_TASKNAME` / `MENU_TASKPAGE` / `MENU_INDEXOF` / `MENU_ISOPEN` / `MENU_CURSORIDX` / `MAIN_DRAW_MENU` 均已进目标码 |
| **待你上机** | 大字时钟是否照旧、菜单能否滚动、键位是否顺手 |

## 七、真值表已同步

`硬件真值表.md` 新增 **1.12 超声波**、**1.13 舵机**，并在 §7 记了两条"参考代码不能照抄"的冲突：

| 冲突 | 参考代码 | 本板真值 | 为什么不能照抄 |
|---|---|---|---|
| 超声波引脚 | P4.7 / P3.3 | **P2.4 / P3.6** | P4.7 是主屏片选、P3.3 是 I2C 的 SDA |
| 舵机引脚/通道 | P0.2 / PWM7 | **P2.5 / PWM3N** | PWM7 属 PWMB，会与蜂鸣器(PWM5)、马达(PWM6) 抢同一组周期寄存器 |

**两条待上机确认**（我只能静态确认，已在真值表 §9.1 记录）：
1. 舵机 P2.5 = PWM3**N**（互补通道）——"只使能 N 通道能否单独输出"必须量 P2.5 有没有 50Hz 方波
2. 超声波 P2.4/P3.6 出自《02》设计文档（不是验证过的代码）—— 若测不到距离，先量 TRIG 有没有 20µs 脉冲

## 八、下一步

| 步 | 内容 |
|---|---|
| **S3** | 副屏改成"当前任务内容"：时钟页 = 时间/日期/温湿度+闹钟数 |
| **S4** | 测距仪：`Driver/Ultrasonic.c`（整数版）+ 页面实时显示 |
| **S5** | 云台：`Driver/Servo.c`（整数版）+ 页面角度控制 |
| **S6** | 各任务副屏内容补齐（闹钟/番茄钟/设置） |
| **S7** | 清理：`main_draw_placeholder` 等已无用代码；解除蜂鸣器静音 |

按规范：**一次只改一处，每步单独编译 + 单独上机验证**。
