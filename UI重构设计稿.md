# M1 UI 重构设计稿

> 定稿日期：2026-09-18
> 目标：**SPI 主屏做主界面（中文），I2C 副屏做极简状态栏 + 图片点缀**
> 约束：不新增热路径 printf；盯住任务栈；一次只改一个页面并单独验证

---

## 一、两块屏的分工原则

| | SPI 主屏（128×64，**带硬件字库**） | I2C 副屏（128×64，**无字库**） |
|---|---|---|
| 汉字 | ✅ 走屏幕自带字库 IC，**不占单片机 Flash**，随便用 | ❌ 需软件取模，32 字节/字，要省着用 |
| 刷新成本 | 软件 SPI，整屏约 1.3ms | 逐字节 I2C，整行约 30~40ms（400kHz） |
| 定位 | **主力交互界面**：菜单、编辑、提示、大数字 | **状态栏**：一行关键信息 + 一个取模图标 |
| 刷新频率 | 随内容变化，最高每秒 | **只在内容真变时刷**（压 I2C 占用） |

**核心原则：凡是"需要用户读的文字"都放 SPI；副屏只放"一眼扫到就够"的东西。**

---

## 二、SPI 主屏版面（每页一套，坐标单位：x = 像素列 0~127，y = 页 0~7）

### 页 1 · 主界面 HOME（保留《01》的大字时钟卖点）

```
y=0..3   ┌──────────────────────────────────────┐
         │        0 8 ： 3 0                     │  16×32 大字（沿用现有字模）
y=4..5   │ 2026-09-18 周五        ⏰ 2 组开启    │  8×16
y=6..7   │ 温度 29.0℃   湿度 47%                 │  8×16
         └──────────────────────────────────────┘
```

| 元素 | x | y | 字号 | 内容 |
|---|---|---|---|---|
| 大字时间 | 24 起，步长 18 | 0 | 16×32 | `HH:MM` |
| 日期行 | 0 | 4 | 8×16 | `2026-09-18 周五`（中文星期，字库出） |
| 闹钟数 | 88 | 4 | 8×16 | `⏰2`（用现成的"闹"字 + 数字） |
| 温湿度 | 0 | 6 | 8×16 | `温度 29.0℃  湿度 47%` |

> 关键改动：**把现在英文的 `T29.0C H47% ALM1` 换成中文标签**，一眼可读。

### 页 2 · 闹钟列表 ALARM LIST

```
y=0..1  ┌─ 闹钟列表 ─────────────── 2/8 开启 ─┐   反白标题栏
y=2..3  │ ▶ 07:00  工作日      开            │
y=4..5  │   12:30  周六日      关            │
y=6..7  │   18:00  每天        开            │
        └────────────────────────────────────┘
```

| 元素 | 说明 |
|---|---|
| 标题栏 | y=0..1 反白（黑底白字），右侧显示"n/8 开启" |
| 列表 | 每项占 2 页高；可见 3 项；▶ 指示当前项 |
| 每项内容 | `HH:MM` + 星期描述（工作日/周六日/每天/周一…）+ 开/关 |
| 滚动 | KEY3/KEY4 上移下移，选中项高亮（反白或前导 ▶） |

### 页 3 · 闹钟编辑 ALARM EDIT

```
y=0..1  ┌─ 编辑闹钟 3/8 ────────────────────┐
y=2..3  │      0 7 ： 0 0                    │  16×32 大字（沿用）
y=4..5  │  [时]  分   星期   开关   曲目      │  当前字段反白
y=6..7  │  工作日      开    两只老虎         │
        └────────────────────────────────────┘
```

| 元素 | 说明 |
|---|---|
| 大字区 | 实时显示正在编辑的 时:分 |
| 字段行 | 5 个字段名横排，**当前字段反白**（这是"显示不够智能"的核心修复） |
| 值行 | 当前字段的值 + 该组的星期描述与曲目名 |
| 操作提示 | 底部一行（与值行共用时右对齐）：`K3/K4 调整 K1 下一项` |

### 页 4 · 番茄钟 POMODORO

```
y=0..1  ┌─ 番茄钟 ──── 第 2 个 ─────────────┐
y=2..3  │          2 4 ： 5 9                 │  大字倒计时 MM:SS
y=4..5  │  专注中        25 分钟              │
y=6..7  │  K1 开始/暂停  K2 返回  K3/K4 时长   │
        └────────────────────────────────────┘
```

### 页 5 · 设置 SETTINGS（6 项，中文）

```
y=0..1  ┌─ 设置 ────────────────────────────┐
y=2..3  │ ▶ 音量          6 / 10             │
y=4..5  │   曲目          两只老虎            │
y=6..7  │   提醒方式      铃声                │
        └────────────────────────────────────┘
```

6 项：音量 / 曲目 / 提醒方式 / 主屏开关 / 日出唤醒 / 贪睡时长。**每页 3 项、滚动显示。**

### 页 6 · 校时 TIME SET（**新增，补掉当前缺口**）

```
y=0..1  ┌─ 校时 ─────────────── 2026-09-18 ┐
y=2..3  │   年 月 日    时 分 秒             │
y=4..5  │  2026 09 18   19 41 00            │  当前字段反白
y=6..7  │  K1 下一项  K3/K4 调整  K2 保存    │
        └────────────────────────────────────┘
```

**入口**：主界面 KEY1+KEY2 同时按下（或主界面长按 KEY2）。
**保存**：`Clock_Set()` 写芯片 + `Alarm_ApplyHardware()` 重算硬件闹钟。
**这页做完，日期就不再是 2000-01-01 了。**

### 页 7 · 响铃 RING

```
y=0..3  ┌──────────────────────────────────┐
        │        闹 钟 响 铃                 │  16×32 汉字，闪烁
y=4..5  │   07:00   工作日                   │
y=6..7  │  K1 停止        K2 贪睡 9 分钟      │
        └──────────────────────────────────┘
```

---

## 三、I2C 副屏版面（一页共 4 行，每行 16 像素高）

**统一结构：上 1 行状态 + 下 1 行提示，中间放取模图元。**

```
y=0..1  CLOCK              14:38:55        ← 页面名 + 时间（ASCII）
y=2..3  ┌──────────┐
        │  取模图标 │  48×32                    ← 图片点缀区
y=4..5  └──────────┘
y=4..5  ALM 2/8  VOL 6
y=6..7  K1 闹钟 K2 番茄 K3 设置
```

| 行 | 内容 | 刷新频率 |
|---|---|---|
| 第 1 行 | 页面名（ASCII）+ 当前时间 `HH:MM:SS` | 每秒 |
| 第 2~3 行 | **取模图标区**（48×32，每页一个图标） | 切页时 |
| 第 4 行 | 关键状态：`ALM 2/8  VOL 6` | 变化时 |
| 第 5 行 | 操作提示：`K1 闹钟 K2 番茄 K3 设置` | 切页时 |

### 取模图元清单（每页一个，共 6 个）

| 页面 | 图标 | 用途 |
|---|---|---|
| 主界面 | 时钟/铃铛 | 闹钟主题 |
| 闹钟列表 | 铃铛 | |
| 闹钟编辑 | 铅笔 | |
| 番茄钟 | 番茄 | |
| 设置 | 齿轮 | |
| 响铃 | 跳动铃铛（2 帧动画） | 响铃时闪动 |

**规格与成本**：
- 尺寸 **48×32** → 每张 = 48 列 × 4 页 = **192 字节**
- 6 张 + 响铃 2 帧 = 8 张 × 192 = **1536 字节**，占 Flash 余量的 7.5%
- 取模工具：`day11/Resource/中景园电子ZJY096S08Z0WG01技术资料/03-OLED取模教程/01-PCtoLCD2002完美版`
- 取模参数：**阴码、列行式、逆向、十六进制**（与副屏驱动的页寻址一致），生成后我核对首字节再入库

> **刷新策略（压 I2C 占用的关键）**：图标只在**切页时**写一次（192 字节 ≈ 8ms），
> 不参与每秒刷新。每秒只刷第 1 行的时间（约 224 次字节事务）。
> 相比现在"每秒整行清 + 全屏 10 秒重画"，I2C 占用再降约 40%。

---

## 四、实施步骤（每步单独编译 + 单独上机验证，不合并）

| 步 | 内容 | 验证点 |
|---|---|---|
| **S1** | 主界面改中文（日期行 `2026-09-18 周五`、温湿度行 `温度 29.0℃ 湿度 47%`、闹钟数） | 主屏四行中文正常显示，无乱码 |
| **S2** | 新增**校时页**（年月日时分秒）+ 入口 | 校时后日期正确、重启后保持 |
| **S3** | 闹钟列表改中文列表（标题栏 + 3 项滚动 + 开关状态） | 列表可读、选项切换正常 |
| **S4** | 闹钟编辑页加**字段反白** + 中文提示 | 一眼看出在编辑哪个字段 |
| **S5** | 番茄钟 / 设置 / 响铃三页中文化 | 各页可读 |
| **S6** | I2C 副屏改版：图标区 + 状态行 + 提示行 + 取模图片 | 图标正确、I2C 占用下降 |
| **S7** | 解除蜂鸣器静音并调好 | 提示音、闹铃、曲子正常 |

**风险控制**：
- 每步只动 `App_Display.c` 的对应绘制函数 + `App_Menu.c` 的对应页面分支，**不碰 I2C / Timer3 / 时钟链路**
- 每步完成后跑三重验证（编译 0/0、`UNRESOLVED = 0`、读 `.m51`）
- 新增字符串一律放**常量区**，不放栈上大数组
- **本阶段全程不新增热路径 printf**

---

## 五、需要你确认的三点

1. **版面是否认可**（尤其：主界面保留大字时钟、校时页入口用"主界面 KEY1+KEY2"）
2. **副屏取模图标**：6 个图标够不够？要不要加成 8 个（比如温度、湿度各一个）
3. **实施顺序**：默认按 S1→S7 走；如果你想先看到"校时页"（因为日期现在是错的），我可以把 S2 提到 S1 前面
