# 51_MCU_Alarm 交付说明

> 交付时间：2026-09-17
> 工程位置：`D:\Develop\embedded-project\51_MCU_Alarm\Project.uvproj`
> 打开方式：双击 `Project.uvproj`，Keil uVision5 打开后直接 Build（F7）即可
> 设备：STC8H8K64U（与你的现有工程一致）

---

## 一、三重验证结果（规范第二节，缺一不可）

| 层 | 结果 | 证据 |
|---|---|---|
| **编译** | `0 Error(s), 0 Warning(s)` | `build_log.txt` 末行 |
| **链接** | `UNRESOLVED EXTERNAL SYMBOL` 计数 = **0** | `Listings/Project.m51` 全文 grep |
| **尺寸与库** | `code = 38226`（37.3 KB）、`data = 57.7`、`xdata = 1696`；**`FPMUL / FPDIV / CASTF / FPADD / FPCONVERT` 引用数 = 0**；`C51FPL.LIB` **未被链接** | `Listings/Project.m51` |

产物：`Objects/Project.hex`（111,942 字节）

**尺寸判读**：程序区可用空间 = 64 KB − 4 KB(EEPROM, 0xF000~0xFFFF) = **60 KB**。
当前用 37.3 KB，**余量 22 KB**，不用像《02》预估的那样砍汉字或改英文菜单。
（《02》写的"红线 32 KB"是按"程序只用 32 KB"估的，实际硬上限是 60 KB。）

**浮点库的处置**：最初编译出来 code = 39803，`.m51` 里躺着 `?C?FPMUL / ?C?FPDIV / ?C?CASTF`。
根因是 `Driver/SPI_OLED/spi_oled.c` 里那个 `SPI_OLED_ShowNum(u8,u8,float,u8)`——
**C51 是模块级链接，同文件里没被调用的函数也会整段链入**，所以一个用不到的 float 形参
就把整个浮点库拖进来了。改成"形参 = 数值×100 的整数"后，浮点库归零，code 降到 38226。
这正是规范第二节第三层验证要抓的东西。

---

## 二、交付前自检清单（规范第五节，逐条对照）

| # | 检查项 | 结论 | 依据 |
|---|---|---|---|
| 1 | 马达（默认电平危险引脚）显式初始化到安全电平 | ✅ | `App_System.c` 的 `sys_init()` **第一句**就是 `Motor_SafeLevel()`（P0.1=0）。STC8H 复位后 P0 是准双向口且输出为 1 = 高电平震，不先按低，上电到 `Motor_Init()` 之间会一直震 |
| 2 | LED 极性、引脚顺序、总开关极性与例程逐字一致 | ✅ | `Driver/LED.h` 引脚与 v3.1 `App_LED.c` 完全一致：`LED_SW=P4.5`（低导通）、`LED1..8 = P2.7/P2.6/P1.5/P1.4/P2.3/P2.2/P2.1/P2.0`（低点亮） |
| 3 | 各外设初始化顺序与官方例程一致 | ✅ | `EAXSFR()` 最先；`UART → I2C → ADC → Buzzer → Motor`；外设全部配完才 `EA=1` |
| 4 | `EA=1` 等开中断语句照抄 | ✅ | 位置照抄 v3.1：`Buzzer_Init(); Motor_Init(); … EA = 1;` |
| 5 | 显示坐标单位（页 vs 像素）已从驱动源码确认 | ✅ | 两块屏都是 **x = 像素列 0..127，y = 页 0..7**，依据 `SPI_OLED_address()` / `OLED_Set_Pos()` 里的 `0xb0+y` |
| 6 | 库函数已读源码，确认无"无超时死等" | ✅ **并做了一处加固** | 见下方"主动加固"第 1 条 |
| 7 | 传参未超出函数参数类型范围 | ✅ | 全工程无 `delay_ms(u8)` 之类的越界传参（本项目未使用 `delay_ms`，延时统一走 `g_sysTick` 或 `NOP` 宏） |
| 8 | 无 float/printf 等会链入大库的用法 | ✅ | 浮点库引用 = 0（见上）。`printf` 保留，链的是标准库 `C51L.LIB (PRINTF)`，不是浮点库 |
| 9 | 工程注册了所有用到的 .c（UNRESOLVED = 0） | ✅ | 71 个文件全部注册，无重复；`UNRESOLVED = 0` |
| 10 | 诊断手段的输出不依赖被诊断对象 | ✅ | 见下方"主动加固"第 2 条 |

### 主动加固（三处，都超出规范的最低要求）

1. **给 DHT11 的等待循环加了超时**。
   v3.1 的 `wait_level_change` 宏是 `while(DHT == level){ … }`，**循环里没有超时判断**——
   传感器没插好时，等"响应低电平"会**死循环**，整个程序再也回不来。
   本工程改成 `cnt > max` 就提前退出并返回错误码（`DHT11.h` 里列了 5 个错误码）。
   规范第二节第 6 条点名的就是这类"无超时死等"。
2. **诊断输出立即生效，不经任务刷新**。
   `Display_Init()` 里主屏初始化完立刻写 `ALARM BOOT OK`、副屏写 `SUB SCREEN OK`；
   `Led_SetSingle()` 直接写 IO 口，不经过任何任务。
   这样"屏幕亮不亮"就能判断是屏的问题还是任务没跑起来的问题——
   规范第四节点名的那次踩坑（`Led_SetSingle` 只改掩码、真正输出在任务里）不会重演。
3. **I2C 总线锁带超时**。
   `I2C_Lock()` 等 500 ms 拿不到锁就强制接管并打印告警，不会把界面彻底锁死。

---

## 三、现有工程 `51_MCU_Project` 的 bug 修复对照表

> 你的原目录**全程只读**，`git status --porcelain` 输出为空，零改动。
> 基线编译是在副本 `_baseline\51_MCU_Project` 里做的。
> 下表 20 条全部在新工程里消除。

| # | 原工程问题 | 基线编译证据 | 新工程怎么修的 |
|---|---|---|---|
| 1 | `Lib/Delay.h` 找不到（IncludePath 缺 `.\Driver\LED`） | `main.c(4): warning C318` | 新工程 IncludePath = `.\Lib;.\User;.\Driver;.\App;.\Driver\SPI_OLED;.\Driver\I2C_OLED` |
| 2 | `LED_Init()` 有声明无实现（`LED.c` 整文件被注释） | `WARNING L1: UNRESOLVED EXTERNAL SYMBOL / LED_INIT` | `Driver/LED.c` 完整实现；诊断用 `Led_SetSingle` 立即写口 |
| 3 | `count` 只声明未定义 | `WARNING L1: … SYMBOL: COUNT` | 删除该变量，新工程零 UNRESOLVED |
| 4 | `LED.c` 在 .uvproj 里注册了两次 | `WARNING L7: MODULE NAME NOT UNIQUE` | 工程文件由脚本生成，每个文件只注册一次 |
| 5 | `main.c` 从未调用 `App_Init()`，I2C/ADC/PWM 全没初始化 | 代码走查 | 新工程把初始化集中在 `sys_init()`，`main_start()` 第一句就调 |
| 6 | `.uvproj` 漏注册 EEPROM.c / Exti.c / Exti_Isr.c 等 11 个文件 | 对比 FilePath 与 Lib 实际文件 | 71 个文件全部注册，含 EEPROM.c |
| 7 | `system_ms` 是 `u16`，65.5 秒回绕 | `Driver/Timers/Timers.c` | 改成 `volatile u32 g_sysTick`（`App_Public.c`），并提供 `SysTick_Get()` 双读校验 |
| 8 | `Key.c` 的 `key_change_ms[]` 也是 u16 | 同上 | 新 `Driver/Keys.c` 用 u32 时间戳 + `SysTick_Elapsed()` |
| 9 | `Nixie_Scan2ms()` 是空实现，判断完直接 return | `Driver/NIXIE/NIXIE.c` | 新的 `Driver/NixieScan.c` 由 Timer2 1kHz 中断真扫 |
| 10 | `NIXIE.h` 函数声明缺参数类型 | `void Nixie_show(num, idx);` | 不再使用那个头；`NIXIE.h` 用 v3.1 的完整签名 |
| 11 | `LED.c` 注释里 `P2 |= 0xCF` 掩码与引脚定义不吻合 | 代码走查 | 新实现用逐位写，不用整口掩码 |
| 12 | `App_oled.c` 用 `_task_ 2` 硬编码，与 `App_Nixie_Task_Id=2` 撞号 | 代码走查 | 任务号统一在 `App_Public.h` 里定义，全部用宏 |
| 13 | `I2C_OLED.h` 重复定义 `u8/u16/u32`，且 `u32` 定义成 `unsigned int` | 头文件 | 新工程不重复定义类型；`u32` 在 `Lib/Config.h` 里是 `unsigned long` |
| 14 | `EEPROM.h` 的 `MCU_Type` 是官方默认的 `STC8X1K08` | 头文件 | 改成 `STC8XxK60`（对应 64 KB，`MOVC_ShiftAddress = 0xF000`） |
| 15 | **马达 P0.1 的 GPIO 配置被注释掉 → 上电马达震** | 代码走查 | `sys_init()` 第一句按安全电平；`Motor_Init()` 显式配推挽 |
| 16 | 未配置 P0.4（NTC）为模拟输入 | 代码走查 | `NTC_Init()` 配高阻 |
| 17 | `Key.c` 注释写"三个按键"，实际 4 个 | 代码走查 | 新 `Keys.c` 注释与实现都是 4 个 |
| 18 | 空文件被注册进工程（`DHT1.c`、`Alarmclock.c` 均 0 字节） | `ls -l` | 新工程无空文件 |
| 19 | 浮点库 + printf 库已被链入 | `.m51` 有 `C51FPL.LIB (?C?FPMUL)` 等 | 浮点库引用 = 0 |
| 20 | code 已 20.6 KB 而功能未做 | `code=20642` | 新工程 37.3 KB，功能完整，余量 22 KB |

### 另外修掉的 4 个 v3.1 参考代码本身的缺陷

| 位置 | 问题 | 修法 |
|---|---|---|
| `Driver/PCF8563.c` `PCF8563_set_alarm()` | "周"分支的判据写成了 `if(alarm.day < 0)`，应该是 `alarm.week`。会导致写进硬件的"周"字段与预期不符 | 已改 |
| `Driver/PCF8563.c` `PCF8563_init()` | `GPIO_config()` 被注释掉 → P3.2/P3.3 没配成开漏、P3.7 没配成准双向口，I2C 根本没配好 | 已打开（照江文聪封装驱动） |
| `Driver/PCF8563.h` | `PCF8563_ALARM_ENABLE` 是 `DISABLE` → `PCF8563_int_call()` 函数体被 `#if` 整段裁掉，**闹钟中断从来没有被处理过** | 已改成 `ENABLE` |
| `Lib/Exti_Isr.c` `INT3_ISR_Handler` | 只写 `WakeUpSource = 4`，`PCF8563_int_call()` 全工程无人调用 | 改成只置 `g_rtcIrqFlag`，由 TASK_LOGIC 做 I2C 清标志 |

---

## 四、与文档不一致的地方（规范要求逐条列理由）

| # | 文档原话 | 本工程做法 | 理由 |
|---|---|---|---|
| 1 | 《02》"T0 渲染每 2 毫秒刷一位数码管" | **Timer2 1kHz 中断刷**（一帧 8 ms = 125 Hz） | RTX51 心跳 5 ms，`os_wait2` 最小粒度就是 5 ms，**2 ms 物理上不可实现**。用任务扫描只能到 25 Hz（v3.1 `App_Digital.c` 自己也标着 `// 5ms -> 40ms (25Hz)`）。Timer2 是 v3.1 在同一块板上真机跑通的方案 |
| 2 | 《02》/《05》"Timer2 留给超声波" | **M1 占用 Timer2** | 同上。且《02》第八节说超声波只能"轮询计时"（只支持下降沿触发），轮询可以挂在 Timer3 的 `g_sysTick` 上算差值，不冲突 |
| 3 | 《01》"一排小灯会越来越亮" | **逐颗点亮（0→8 颗）**，不做真实亮度渐变 | 8 颗灯全是纯 GPIO。其 PWM 备选通道属于 PWMA（舵机占 PWM3）与 PWMB（蜂鸣器占 PWM5、马达占 PWM6），要真亮度就得让蜂鸣器或马达让位。逐颗点亮是视觉上"渐亮"的可行近似，也是你确认过的方案 |
| 4 | 《04》"PCF8563_alarm_clear_flag 中断里第一件事就调" | **ISR 只置标志，T2 任务里做 I2C 清标志** | 清标志必须走 I2C，而 I2C 总线要和副屏共用。中断里做 I2C 会与副屏刷新抢总线，且此时总线锁机制无法生效。P3.7 是下降沿触发，不会反复进中断，延后几十毫秒清标志是安全的。**这是你 2026-09-17 确认过的方案** |
| 5 | 《04》F 角色"把小数运算改成整数" | ✅ 已做 | 见第一节浮点库说明 |
| 6 | 《04》C 角色"副屏不做全缓存，省 0.75KB" | **实际不需要缓存**：驱动是直接写屏，本来就没有帧缓存 | 《02》的 RAM 预算里给两块屏留了 1.25 KB 缓存，实际两份驱动都不缓存，这部分 RAM 直接省下来了（实测 data 57.7 B + xdata 1696 B） |
| 7 | 《04》C 角色"汉字准备约 80 个，每个 32 字节" | **不需要** | 你的板子上主屏是"带字库"的 OLED 模块，汉字点阵在屏幕上的独立字库 IC 里（晶联讯），走 P1.0/P1.1 读 ROM，**一个字节都不占单片机 Flash**。副屏（I2C）则根本没有汉字库 |
| 8 | 《04》B 角色"存数据前擦干净整页再写" | 设置与闹钟：一次擦一次写 ✓；温湿度记录：**按字节追加，只在进入新页时擦** | 记录区是顺序填充，擦除态本来就是 0xFF，可以直接按字节写。每 256 条才擦一次（约 42 小时），寿命完全不是问题 |

---

## 五、硬件真值表（动手前交付的那份）的实际落地

完整真值表见 `硬件真值表.md`。关键几条在代码里的落点：

| 真值 | 落点 |
|---|---|
| 主屏 SCLK=P5.0 / MOSI=P1.3 / DC=P1.6 / CS=P4.7(低有效) / MISO=P1.1 / 字库CS=P1.0(低有效) | `Driver/SPI_OLED/spi_oled.h` 的 sbit，逐字抄自 day11 |
| 副屏 SCL=P3.2 / SDA=P3.3，与 PCF8563 **共用** | `Driver/I2C_OLED/oled.h`；互斥由 `Driver/I2C_Lock.c` 管 |
| RTC 中断 P3.7 → INT3，**下降沿** | `Driver/PCF8563.c`：`EXT_MODE_Fall` |
| 数码管 DI=P4.4 / SCK=P4.2 / RCK=P4.3，段码低有效、位码高有效 | `Driver/NIXIE.c`（原样搬入，未改） |
| 矩阵键盘 列 P0.3/P0.6/P0.7/P1.7，行 P3.4/P3.5/P4.0/P4.1，准双向 | `Driver/MatrixKey.h/.c` |
| 独立按键 P5.1~P5.4，准双向，按下=0 | `Driver/Keys.h/.c` |
| 蜂鸣器 P0.0 + `PWM5_SW(PWM5_SW_P00)`，PWMB 组 | `Driver/Buzzer.c` |
| 马达 P0.1 + PWM6，**与蜂鸣器共用 PWMB 频率** | `Driver/Motor.c`（开震前先 `Buzzer_Stop()`） |
| 旋钮 P0.5/ADC13、NTC P0.4/ADC12、DHT11 P4.6 准双向 | 各自驱动 |
| `delay_ms` 形参是 u8（1~255） | 本工程未使用 `delay_ms`，延时走 `g_sysTick` 或 `NOP8/NOP2`，从根上避开这个坑 |

---

## 六、上位机通信协议（你要求 M1 就做）

串口1（P3.0/P3.1，经板上 CH340 到 USB），115200-8-N-1。

```
+------+------+------+------+-----------+------+
| 0xAA | 0x55 | LEN  | CMD  | DATA[..]  | SUM  |
+------+------+------+------+-----------+------+
LEN = CMD + DATA 的字节数
SUM = (LEN + CMD + DATA 各字节) 之和的低 8 位
```

| CMD | 名称 | 请求 DATA | 回复 DATA |
|---|---|---|---|
| 0x01 | 读时间 | — | 年H 年L 月 日 周 时 分 秒（8） |
| 0x02 | 校时 | 年H 年L 月 日 时 分 秒（7） | 状态（1） |
| 0x03 | 读设置 | — | 音量 提醒方式 曲目 专注 休息 屏开关 贪睡 日出（8） |
| 0x04 | 写设置 | 同上 8 字节 | 状态（1） |
| 0x05 | 读 8 组闹钟 | — | 8×{开关 时 分 星期掩码 曲目}（40） |
| 0x06 | 设一组闹钟 | 序号 开关 时 分 星期掩码 曲目（6） | 状态（1） |
| 0x07 | 删一组闹钟 | 序号（1） | 状态（1） |
| 0x08 | 读温湿度 | — | 温度H 温度L 湿度 有效标志（4） |
| 0x09 | 读记录条数 | — | 条数H 条数L（2） |
| 0x0A | 读第 n 条记录 | 序号H 序号L（2） | 温度 湿度（2） |
| 0x0B | 读版本 | — | 主 次 修订（3） |

状态：`0x00` = 成功，`0x01` = 失败。

星期掩码：`bit0=周一 … bit5=周六, bit6=周日`。`0x1F` = 工作日，`0x7F` = 每天。

> 注意：板子的调试 `printf` 和这个二进制帧**共用同一个串口**。
> 上位机解析时要么先按帧头 `AA 55` 同步，要么把 `printf` 关掉（`App_System.c` / 各任务里搜 `printf` 注释掉即可）。

---

## 七、必须由你真机验证的项目（我做不到）

我完成的是**静态三重验证**。以下**只能上机确认**：

1. **屏幕内容是否正确**——尤其"大字时间"的数字方向。8 位数码管的物理位序取决于接线，
   若实机看到的是 `65-43-21`（倒过来），把 `Driver/NixieScan.h` 里
   `#define NIXIE_POS_REVERSED 0` 改成 `1` 即可，其它都不用动。
2. **主屏汉字是否正常**——若显示乱码，说明源码编码被编辑器改过，
   重新跑一次 `encoding.py to-gbk`（见第八节）。
3. **蜂鸣器响度**——`Driver/Buzzer.h` 的 `BUZZER_DUTY_MAX_PCT` 当前是 50
   （v3.1 用的是 2%，江文聪老师用的是 50%）。偏响/偏轻只改这一个宏。
4. **马达与蜂鸣器不能同时响**——两者同属 PWMB 共用频率，代码里开震前会先停蜂鸣器；
   实际效果请确认。
5. **共用 I2C 总线是否有卡死**（《05》第四节列为"需实机确认"的那一项）。
6. **DHT11 读数是否稳定**——时序判据是照抄 v3.1 真机标定的值，
   但我给它加了超时保护，超时阈值是否合适需看实际读数。
7. **EEPROM 掉电保存**——拔电再插，闹钟与设置应还在。
8. **PCF8563 纽扣电池走时**——首次上电若芯片没校过时，`Clock_IsValid()` 会是 0，
   用上位机 0x02 命令校一次时即可。

---

## 八、源文件编码（重要，别被编辑器悄悄改掉）

工程里凡是通过 `SPI_OLED_Display_GB2312_string()` 显示中文的文件，
**字符串字面量必须是 GBK/GB18030 字节**。因为驱动是拿字节直接算字库 ROM 地址的：

```
Address = ((MSB - 0xB0) * 94 + (LSB - 0xA1) + 846) * 32
```

如果源码存成 UTF-8，一个汉字变 3 字节，算出来的地址是垃圾，屏上是乱码。
交付前已把全工程 `.c/.h` 统一转成 GB18030（共 48 个文件被转换）。

**如果你用 VS Code 之类默认 UTF-8 的编辑器改过源码，改完请重新转换**：

```
python _tools/encoding.py to-gbk
```

受影响的中文字面量：`App/App_Display.c`（星期日名、"掌机模式/M2 开发中/按KEY2返回主界面"）、
`App/App_System.c` 与各任务里的 `printf` 调试信息（纯注释不受影响）。

---

## 九、目录结构

```
51_MCU_Alarm/
├── Project.uvproj           Keil 工程（设备 STC8H8K64U，Compact 内存模型，继承 v3.1 配置）
├── User/main.c              任务0：sys_init + 建 5 个任务 + 自毁
├── App/                     业务层（17 个文件）
│   ├── App_Public.h/.c      任务ID宏、公共头文件、公共类型、全局对象
│   ├── App_System.h/.c      sys_init（含马达安全电平、EEPROM/时钟装载）
│   ├── App_Font.h/.c        16x32 大字数字字模（脚本生成，7 段风格）
│   ├── App_Input.h/.c       事件队列 + TASK_INPUT
│   ├── App_Clock.h/.c       时间服务（星期用 Sakamoto 算法自算）
│   ├── App_Alarm.h/.c       8 组闹钟：调度、响铃、日出唤醒
│   ├── App_Storage.h/.c     EEPROM 分区读写
│   ├── App_Sensor.h/.c      DHT11 + NTC + 记录 + TASK_SENSOR
│   ├── App_Music.h/.c       非阻塞音乐引擎 + TASK_MUSIC
│   ├── App_Songs.h/.c       3 首曲子（两只老虎 / 小星星 / 生日快乐）
│   ├── App_Display.h/.c     双屏渲染 + TASK_RENDER
│   ├── App_Menu.h/.c        页面栈、事件分发 + TASK_LOGIC
│   ├── App_Uart.h/.c        上位机协议
│   └── App_Game.h/.c        掌机模式占位（M2）
├── Driver/                  硬件驱动层
│   ├── spi_oled / I2C_OLED  两块屏（原样搬自 day11 与 v3.1）
│   ├── PCF8563.c/h          时钟芯片（含 3 处修补，见第三节）
│   ├── Keys.c/h             4 个独立键（新增 20ms 消抖 + 2 秒长按）
│   ├── MatrixKey.c/h        4x4 矩阵键（新增消抖 + 扫完恢复）
│   ├── NIXIE.c/h            数码管移位寄存器（原样）
│   ├── NixieScan.c/h        Timer2 1kHz 扫描（新增）
│   ├── NTC.c/h              热敏电阻查表测温（改整数）
│   ├── DHT11.c/h            温湿度（改整数 + 关中断 + 加超时）
│   ├── Buzzer.c/h           蜂鸣器 PWM5（去浮点 + 音量）
│   ├── LED.c/h              8 颗灯（新写）
│   ├── Motor.c/h            震动马达 PWM6（新写）
│   └── I2C_Lock.c/h         总线互斥（新写）
├── Lib/                     厂家基础库（原样搬自 v3.1，仅 Config.h 补类型别名、
│                            EEPROM.h 改型号、Timer_Isr.c 接两个回调）
└── OS/                      Conf_tny.A51 + RTX51TNY.LIB（原样）
```

---

## 十、给你留的接口（掌机模式）

页面编号已经在 `App_Public.h` 的 `UIPageId_t` 里占好：
`PAGE_GAME_HALL / SNAKE / BRICK / PLANE / DAILY / OVER`。
长按 KEY3 或 KEY4 就能进"掌机模式"，主屏会画出占位页（用主屏字库画的真中文），
副屏标题显示 `GAME HALL`，按 KEY2 返回。

M2 开工时只需要：
1. 把 `App_Game.c` 的 `Game_Enter()` 改成检查内存配额、`Game_OnEvent()` 填按键处理；
2. 页面编号直接用现成的；
3. EEPROM 的 **1 号区（0xF200）一直空着没被 M1 写过**，直接拿来存游戏存档；
4. **Timer4 一个中断都没注册**，留给游戏的弹幕节奏。
