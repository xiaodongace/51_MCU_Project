# 使用说明 · 一机两用终端（51_MCU_Alarm）

> 适用固件：`51_MCU_Alarm` 版本 **1.0.0**（2026-09-22 状态）
> 这份文档面向**拿到板子的人**：怎么接、怎么烧、按键怎么按、每个功能怎么操作、坏了怎么查。
> 想了解代码和原理看《02-技术方案》《04-分工规格》；想查编译数字看《交付说明》。

---

## 目录

1. [这是什么](#一这是什么)
2. [硬件准备与接线](#二硬件准备与接线)
3. [烧录固件](#三烧录固件)
4. [第一次上电会看到什么](#四第一次上电会看到什么)
5. [**按键总表（最重要）**](#五按键总表最重要)
6. [导航：主界面与任务清单](#六导航主界面与任务清单)
7. [逐个任务的操作](#七逐个任务的操作)
8. [闹钟响铃、贪睡、日出唤醒](#八闹钟响铃贪睡日出唤醒)
9. [连接电脑（串口）](#九连接电脑串口)
10. [出厂默认值一览](#十出厂默认值一览)
11. [快速上手（3 分钟）](#十一快速上手3-分钟)
12. [常见问题排查](#十二常见问题排查)
13. [已知限制（还没做的功能）](#十三已知限制还没做的功能)

---

## 一、这是什么

一块单片机小板子，**平时当桌面时钟 + 闹钟，想玩的时候当掌上游戏机**。
不用连手机，插上 USB 就能用；接两根线还能测距和转云台。

**主菜单共 7 项**：

| # | 任务 | 需要外接件吗 |
|---|---|---|
| 0 | 日期和时间 | 不需要 |
| 1 | 闹钟（3 组） | 不需要 |
| 2 | 番茄钟 | 不需要 |
| 3 | 测距仪 | **要超声波模块**（不接也能进，只是读不到数） |
| 4 | 云台 | **要舵机**（不接也能进，只是舵机不转） |
| 5 | 设置 | 不需要 |
| 6 | 游戏（3 个） | 不需要 |

**这块板子上有什么**（都在用，没有闲置的）：

- 两块 OLED 小屏（主屏带字库能显示中文，副屏只能显示英文数字）
- 8 位数码管（一直显示 `时-分-秒`）
- 4 个独立按键 + 16 键矩阵键盘
- 8 颗小灯、蜂鸣器、震动马达
- 旋钮（调音量 / 震动强度的百分比）
- 热敏电阻 + 温湿度传感器
- 时钟芯片（板上有纽扣电池，**断电时间不丢**）

---

## 二、硬件准备与接线

### 2.1 必备

| 项 | 说明 |
|---|---|
| 三块板 | 核心板（STC8H8K64U）+ 转接板 + 扩展板，插在一起 |
| USB 线 | 一头接板子、一头接电脑。**下载程序和通信都用这一根** |
| 5V 电源 | 一般 USB 供电就够；接舵机时建议独立 5V（见 2.3） |

### 2.2 外接模块（可选，不接不影响其他 5 个功能）

| 模块 | 接到哪 | 说明 |
|---|---|---|
| **超声波 HC-SR04** | `TRIG` → **P2.4**、`ECHO` → **P3.6**、`VCC` → 5V、`GND` → GND | 用在「3 测距仪」 |
| **舵机** | 信号线（橙/黄）→ **P2.5**、`VCC` → 5V、`GND` → GND | 用在「4 云台」 |

> ⚠️ **舵机供电**：舵机堵转时电流可达几百 mA，**不要直接从板子的 5V 取电**，
> 容易把整机电压拉低导致屏幕异常。用独立 5V（或至少并联一个大电容），**GND 必须共地**。

> ⚠️ **杜邦线要插紧**。超声波"读不到数"最常见的原因就是线松了——用屏上的 `RAW` 值判断（见第七节 3）。

### 2.3 转接板针脚位置

接线位置以《硬件真值表》第 2 节为准。**P2.4 / P2.5 / P3.6 都在板子边缘，旁边有丝印**。

---

## 三、烧录固件

1. 用 USB 线连接板子和电脑。
2. 打开 **STC-ISP**（或 AiCube-ISP），选择芯片型号 **STC8H8K64U**。
3. **两个必须检查的设置**：
   - **EEPROM 大小 = 4KB**（设错了会出现"编译能过、下载后跑不起来"）；
   - **取消勾选**"下次下载用户程序时擦除用户 EEPROM 区"
     （不取消的话，**每次下载都会清空你已存的闹钟和设置**）。
4. 串口波特率随便（下载用），通信协议用 **115200**。
5. 选择 `Objects/Project.hex`，点下载，然后给板子**断电再上电**（冷启动下载）。

> **下载后如果设置被重置了**：说明第 3 步的两个勾选没对，重新设置再下一次。

---

## 四、第一次上电会看到什么

### 4.1 上电顺序

1. **开机动画**在主屏上播放（这段时间屏幕上只有动画，其他设备都不会响应——这是故意的，为了让画面干净）；
2. 动画结束，**8 颗小灯全亮一下**（这是"系统自检进度条"，正常现象）；
3. 数码管出现 `00-00-00`，然后跳到当前时间；
4. 主屏出现**大字时间 + 日期 + 温湿度**；
5. 副屏显示时钟相关信息。

**正常上电大约 2 秒完成**（其中开机动画约 2 秒：由中心向外扩散 65 帧、再收缩 64 帧，
每帧 `delay_ms(15)`）。

> **觉得动画太慢/太快？** 改 `App_System.c` 里 `boot_animation()` 的两个数：
> `step`（台阶感）和 `delay_ms(15)`（帧率）。**注意别只改一个**——
> 注释里记着一次教训：把 `step` 改成 6 想提速，结果变成"一跳一跳"，观感更差。

### 4.2 如果卡住了：看小灯亮几颗

上电时 8 颗小灯是**进度条**。断电重上电，数一下亮了几颗：

| 亮几颗 | 说明卡在哪 |
|---|---|
| 全灭 | 卡在初始化的第 1 步之前（先查供电、USB 线） |
| 亮 1~3 颗 | 卡在开机动画附近（很可能主屏模块/排线问题） |
| 亮 4 颗 | 卡在蜂鸣器 / 马达 PWM 配置 |
| 亮 5 颗 | 卡在数码管扫描初始化 |
| 亮 6 颗 | 卡在 EEPROM 装载 |
| 亮 7 颗 | 卡在开全局中断之前 |
| **全亮 8 颗** | `sys_init` 全部走通（正常） |
| **只剩第 1 颗亮** | 五个任务也起来了（**这是正常运行状态**） |

> 亮 8 颗 → 只剩 1 颗，中间不需要重新上电，是连续的过程。

---

## 五、按键总表（最重要）

### 5.1 两套按键

| 按键 | 位置 | 用途 |
|---|---|---|
| **4 个独立按键** | 板子边缘，通常丝印 `K1`~`K4` | 日常导航（上下移动 / 确认 / 返回） |
| **16 键矩阵键盘** | 4×4 方阵 | 输数字；**玩游戏时的方向键** |

### 5.2 ★ 4 个独立按键的作用（**所有页面统一**）

| 板上的键 | 作用 | 编辑状态下的作用 |
|---|---|---|
| **K1** | **上移**（光标往上） | **值减小** |
| **K2** | **确认 / 进入下一级** | 确认 / 下一个字段 |
| **K3** | **下移**（光标往下） | **值增大** |
| **K4** | **返回上一级** | 返回（并保存） |

> **例外只有一个**：**闹钟正在响的时候**，键位单独定义 ——
> K2 = **停止闹铃**，K4 = **贪睡**，其余键无反应。见第八节。

> **不知道自己手上哪个键是 K1？** 做这个测试：
> 在主界面按 **K2**（会进入任务清单），然后按另外两个键 ——
> **往上走的是 K1，往下走的是 K3**；剩下那个是 K4。

### 5.3 ★ 矩阵键盘编号（玩游戏要看）

矩阵键盘的编号规则是 `行 × 4 + 列`，**从 1 数起**：

```
        第1列  第2列  第3列  第4列
       ┌──────┬──────┬──────┬──────┐
第1行  │  0   │  1   │  2   │  3★  │   ★ = 游戏强制退出键
       ├──────┼──────┼──────┼──────┤
第2行  │  4   │  5   │  6   │  7   │
       ├──────┼──────┼──────┼──────┤
第3行  │  8   │  9↑  │ 10   │ 11   │   ↑ = 游戏方向「上」
       ├──────┼──────┼──────┼──────┤
第4行  │ 12←  │ 13↓  │ 14→  │ 15   │   ←↓→ = 游戏方向
       └──────┴──────┴──────┴──────┘
```

**游戏里只用 5 个键**：`3`（退出）、`9`（上）、`12`（左）、`13`（下）、`14`（右）。

> 记法：**退出键在右上角**；方向键在**右下 2×3 的区域**，是一个"倒 T"形。

---

## 六、导航：主界面与任务清单

主界面有**两个层级**：

```
第 0 级  大字时钟          ← 开机就在这（大号时间 + 日期 + 温湿度）
   │  K2
第 1 级  任务清单（7 项）   ← K1/K3 移动光标
   │  K2
第 2 级  进入某个任务       ← K4 返回上一级
```

| 层级 | 按键 | 效果 |
|---|---|---|
| 第 0 级（大字时钟） | **K2** | 进入任务清单（第 1 级）。**其他键无反应** |
| 第 1 级（任务清单） | **K1 / K3** | 光标上移 / 下移（到顶、到底会**回绕**） |
| | **K2** | 进入光标所指的任务 |
| | **K4** | 回到第 0 级的大字时钟 |
| 第 2 级（任务内） | **K4** | 返回任务清单 |

### 6.1 空闲会自动返回

**只在"任务清单"这一层**，停 **30 秒**不动会自动回到大字时钟。
**进了任务之后就不会被自动弹走了** —— 要回任务清单请自己按 K4。

> 这是用户明确要求的行为："只有在第二级菜单时，才会触发长时间不操作返回主界面。"

---

## 七、逐个任务的操作

### 7.1 「0 日期和时间」

**两个状态：先选改什么，再输数字。**

| 状态 | 屏幕 | 按键 |
|---|---|---|
| **选项态** | 主屏显示两个选项：**改年月日 / 改时分** | **K1 或 K3** = 在两个选项间切换；**K2** = 确认进入输入；**K4** = 返回任务清单 |
| **输入态** | 主屏只显示当前选项；副屏显示你输到第几位 | **矩阵键盘 0~9** = 输入数字；**K4** = **删掉最后一位**；**独立键 K1/K2/K3 无效** |

**输入格式**：

| 选项 | 格式 | 说明 |
|---|---|---|
| 改年月日 | **8 位** `YYYYMMDD` | 例：2026 年 9 月 22 日 → 按 `2 0 2 6 0 9 2 2` |
| 改时分 | **4 位** `HHMM` | 例：下午 3 点 40 → 按 `1 5 4 0` |

**几条要知道的规则**：

- **输满位数自动提交**（不需要按确认键）；
- 输满之后再按数字**不会被接受**（防止越输越长）——错了就按 **K4 删一位**再改；
- **K4 一直按到"已输入位数 = 0"（把输的全删光），就取消修改、退回选项态**；
  所以"输入全是 0"也要按对应次数才删得完（这是有意设计的）；
- 提交时如果时间非法（比如 13 月、25 时），会提示"非法"，按 K4 删一位改正即可；
- **改完日期会自动算出星期**，不用手动设周几。

### 7.2 「1 闹钟」（3 组）

**两个状态：列表态 / 编辑态。**

#### 列表态

主屏列出 3 组闹钟，光标所在那一组带 `>`。

| 按键 | 效果 |
|---|---|
| **K1 / K3** | 光标上移 / 下移 |
| **K2 短按** | **开 / 关当前这一组**（不用进编辑就能开闹钟） |
| **K2 长按**（按住约 2 秒） | **进入编辑态**，改这一组 |
| **矩阵键盘 0 / 1 / 2** | 直接跳到第 1 / 2 / 3 组 |
| **K4** | 返回任务清单 |

> **出厂默认 3 组都是"关"**。想用哪组就选中它，短按 K2 打开。

#### 编辑态

主屏大字显示正在编辑的时间，下面一行显示正在改的字段。
**5 个字段依次是：时 → 分 → 星期 → 开关 → 曲目。**

| 按键 | 效果 |
|---|---|
| **K2** | **切到下一个字段**；在最后一个字段再按 K2 = **保存并返回任务清单** |
| **K1 / K3** | 当前字段的值 **减 / 加**（时会回绕到 23、分会回绕到 59） |
| **矩阵键盘 0~9** | **只在"时/分"字段上有效**：直接输数字，比用 K1/K3 一个个加快得多 |
| **K4** | **返回并把改动存下来**（退出编辑态） |

> **曲目字段**：0 = 跟随设置里的全局曲目；1~3 = 指定第 1/2/3 首。
> **星期字段**：用 K1/K3 循环切换各种组合（工作日 / 周末 / 每天 / 单日…）。
> **开关字段**：K1/K3 都是切换开与关。

### 7.3 「2 番茄钟」

**两个状态：列表态 / 编辑态。列表 3 项：专注时长 / 休息时长 / 运行。**

| 状态 | 按键 | 效果 |
|---|---|---|
| **列表态** | **K1 / K3** | 光标上移 / 下移 |
| | **K2** | 光标在"运行"项 → **开始 / 暂停**；光标在前两项 → 进入编辑态 |
| | **K4** | 保存并返回任务清单（**运行会停止**） |
| **编辑态** | **K1 / K3** | 当前项的时长 **减 / 加**（1~99 分钟） |
| | **K2** | 确认，回到列表态 |
| | **K4** | 保存并返回任务清单 |

**默认 25 分钟专注 + 5 分钟休息。** 到点有提示音，专注结束自动切到休息、休息结束切回专注。

### 7.4 「3 测距仪」（需要超声波）

进页面后**自动开始测距**，约 150ms 更新一次。

| 屏幕 | 显示什么 |
|---|---|
| 主屏 | 距离值 `nnn cm` + 一行 **`RAW nnnn`**（未换算的原始计数） |
| 副屏 | `RANGE` + 距离 |

| 按键 | 效果 |
|---|---|
| **K4** | 返回任务清单（同时**拉低触发脚**，模块不再发波、省电） |
| 其他键 | **无反应**（这一页没有别的操作） |

**量程 2~400 cm**。超出量程或测不到时显示失败状态（不显示乱码）。

> **`RAW` 那一行是干什么的？** 它是**本次测量的原始计数**，用来判断准不准、以及卡在哪一步。
>
> **正常值**（尺子对准模块，`RAW` 应该接近下表）：
>
> | 实际距离 | `RAW` 应约 | 实际距离 | `RAW` 应约 |
> |---|---|---|---|
> | 5 cm | 28 | 50 cm | 280 |
> | 10 cm | 56 | 100 cm | 560 |
> | 20 cm | 112 | 200 cm | 1120 |
> | 30 cm | 168 | 400 cm | 2241 |
>
> **两个异常值一看就知道卡在哪**（这两个数是超时阈值，不是距离）：
> - **`RAW = 500`** → 等回波**起跳**超时（5ms 内 ECHO 没变高）。
>   先查杜邦线（TRIG→P2.4、ECHO→P3.6 插紧），再确认模块供电 5V、和板子共地。
> - **`RAW = 3000`** → 回波高电平超过 30ms（超出 400cm 量程，或 ECHO 一直高）。
>   把挡板放近一点再看。
>
> **如果 `RAW` 稳定、但算出来的厘米数比例不对** → 用一次标定修：
>
>     每厘米计数 = RAW ÷ 实际厘米数
>
> 当前理论值是 **5.60**（= `SR04_CM_DEN / SR04_CM_NUM` = 28 / 5），
> 换算就是 `厘米 = 计数 × 5 ÷ 28`。
> 例：尺子放 30cm，`RAW` 读到 200（理论是 168）⇒ 每厘米 = 200 ÷ 30 = 6.67
> ⇒ 把 `HC_SR04.h` 里的 `SR04_CM_NUM` 改成 **3**、`SR04_CM_DEN` 改成 **20**（20/3 = 6.67）。

### 7.5 「4 云台」（需要舵机）

| 按键 | 效果 |
|---|---|
| **K1 / K3** | 角度 **减 / 加 10°**（0° ~ 180°） |
| **K2** | **回中位 90°** |
| **K4** | 返回任务清单（**同时关掉舵机输出**，舵机不再有信号） |

副屏显示半圆仪表盘 + 指针，主屏显示当前角度。

> **上电时舵机是不动的**，只有进这个页面才初始化舵机 —— 这样它不会一直嗡嗡响或抖。

### 7.6 「5 设置」（7 项）

**两个状态：列表态 / 编辑态。**

#### 列表态

7 项依次是：

| # | 设置项 | 取值范围 |
|---|---|---|
| 1 | 音量 | 0 ~ 100 % |
| 2 | 曲目 | 1 ~ 3 首（切换时会试听） |
| 3 | 提醒 | 铃声 / 震动（切换时按当前选择给一次反馈） |
| 4 | 主屏 | 开 / 关（关掉主屏省电） |
| 5 | 日出 | 开 / 关（闹钟响前 10 分钟小灯渐亮） |
| 6 | 贪睡 | **1 ~ 30 分钟**（循环） |
| 7 | 震动 | 0 ~ 100 %（调的时候会自动震一下，方便感受） |

| 按键 | 效果 |
|---|---|
| **K1 / K3** | 光标上移 / 下移（到顶、到底回绕） |
| **K2** | 进入编辑态 |
| **K4** | **保存并返回任务清单** |

#### 编辑态

| 按键 | 效果 |
|---|---|
| **K1 / K3** | 值 **减 / 加** |
| **旋钮** | **"音量"和"震动强度"两项还能用旋钮调**（每 1% 一步，转起来比按键快） |
| **K4** | **保存（写进 EEPROM）**并返回任务清单 |

> **两条要注意的**：
> 1. **只有在设置页里转旋钮才会改设置值**。在别的页面转旋钮**不会**影响已设好的值
>    （防止误碰改掉音量）；
> 2. 按 K4 返回时才**存进 EEPROM**（也就是"固化成固定值"）。所以设完一定要按 K4 退出。

### 7.7 「6 游戏」

**同一页里两个状态：先选游戏，再玩。**

#### 列表态

主屏列出 3 个游戏：`> 1 贪吃蛇 / 2 打砖块 / 3 飞机大战`，第 4 行是按键提示 `K2 开始  K4 返回`。

| 按键 | 效果 |
|---|---|
| **K1 / K3** | 光标上移 / 下移 |
| **K2** | **开始玩这个游戏** |
| **K4** | 回任务清单 |

#### 游玩态

**这一段和别的页面完全不同，务必看清：**

| 项 | 说明 |
|---|---|
| **独立按键** | ⛔ **全部失效**（包括 K4）。这是故意的，防止误触退出 |
| **矩阵键盘** | ✅ **唯一可用的输入** |
| **主屏** | 游戏画面（游戏自己刷屏） |
| **副屏** | **自动关闭**（省带宽，也避免和游戏抢刷新） |
| **退出方式** | **按矩阵键盘「第一行第四个」（编号 3，右上角）** |

| 矩阵键盘位置 | 作用 |
|---|---|
| 第三行第二个（9） | **上** |
| 第四行第一个（12） | **左** |
| 第四行第二个（13） | **下** |
| 第四行第三个（14） | **右** |
| **第一行第四个（3）** | **强制退出，回到游戏列表** |

**三个游戏的玩法与节奏**：

| 游戏 | 玩法 | 操作 | 节奏 |
|---|---|---|---|
| **贪吃蛇** | 吃东西变长，撞墙或撞自己就结束 | 方向键改方向 | **150ms 走一步**（放慢过，不然来不及反应） |
| **打砖块** | 反弹球打砖块，别让球掉下去 | **左 / 右**移动挡板（上 / 下不动） | 30ms 一帧 |
| **飞机大战** | 躲敌机、打敌机 | 方向键移动；**子弹自动发射**（每 90ms 一发，不需要按射击键） | 30ms 一帧 |

**游戏结束后自动回列表**（显示约 1.5 秒的 GameOver）。

> **退出键按了没反应？** 三个可能：① 按错了键（要按**第一行第四个**）；
> ② 键盘那一行的引脚被压住了；③ 你人已经进了"结算画面"——等 1.5 秒它会自动回列表。

---

## 八、闹钟响铃、贪睡、日出唤醒

### 8.1 到点会发生什么

1. **响铃前 10 分钟**：如果"日出"设置是**开**，8 颗小灯开始从暗到亮（每 100ms 调一级）；
2. **到点**：按"提醒"设置 —— **铃声**（蜂鸣器放曲子）或 **震动**（马达震）；
3. 屏幕自动切到**响铃页**；
4. 一直响 **120 秒**如果没人理，会自动静音（界面仍停在响铃页）。

### 8.2 响铃时的按键（**和平时不一样**）

| 按键 | 效果 |
|---|---|
| **K2** | **停止闹铃**，回主界面 |
| **K4** | **贪睡**（默认 9 分钟后再响），回主界面 |
| 其他键 | 无反应 |

> 记法：**K2（确认）= 起来，K4（返回）= 再睡会儿**。

### 8.3 贪睡

按 K4 贪睡后，会在**设置里那个"贪睡"分钟数**之后重新响。
贪睡期间**主界面照常能用**，到点会再切回响铃页。

---

## 九、连接电脑（串口）

### 9.1 为什么要连电脑

板子自己就能设闹钟（用按键），连电脑的好处是**快**：在电脑上填表设闹钟、看温湿度历史曲线。

### 9.2 怎么连

1. USB 线连接板子和电脑；
2. 装 CH340 驱动（板载芯片），电脑会出现一个串口；
3. 用串口助手打开：**115200，8 数据位，无校验，1 停止位**；
4. 按下面的二进制帧格式收发。

> ⚠️ **板子的固件里已经没有任何调试打印**，所以串口上出来的全是协议数据，
> 可以直接按帧头解析，不需要过滤文本。

### 9.3 帧格式

```
+------+------+------+------+-----------+------+
| 0xAA | 0x55 | LEN  | CMD  | DATA[..]  | SUM  |
+------+------+------+------+-----------+------+

LEN = CMD + DATA 的字节数（不含帧头与校验）
SUM = (LEN + CMD + DATA 各字节之和) 的低 8 位
```

### 9.4 命令表

| CMD | 名称 | 请求 DATA | 回复 DATA |
|---|---|---|---|
| 0x01 | 读时间 | — | 年H 年L 月 日 周 时 分 秒（8） |
| 0x02 | 校时 | 年H 年L 月 日 时 分 秒（7） | 状态（1） |
| 0x03 | 读设置 | — | 音量 提醒方式 曲目 专注 休息 屏开关 贪睡 日出（8） |
| 0x04 | 写设置 | 同上 8 字节 | 状态（1） |
| 0x05 | 读全部闹钟 | — | 3 × {开关 时 分 星期掩码 曲目}（15） |
| 0x06 | 设一组闹钟 | 序号 开关 时 分 星期掩码 曲目（6） | 状态（1） |
| 0x07 | 删一组闹钟 | 序号（1） | 状态（1） |
| 0x08 | 读温湿度 | — | 温度H 温度L 湿度 有效标志（4） |
| 0x09 | 读记录条数 | — | 条数H 条数L（2） |
| 0x0A | 读第 n 条记录 | 序号H 序号L（2） | 温度 湿度（2） |
| 0x0B | 读版本 | — | 主 次 修订（3） |

**状态**：`0x00` = 成功，`0x01` = 失败。

**星期掩码**：`bit0=周一, bit1=周二, …, bit5=周六, bit6=周日`。
`0x1F` = 工作日，`0x7F` = 每天，`0x00` = 不响（非法配置）。

**举个例子**（读版本号，命令 `0x0B`）：

| 方向 | 字节 | 怎么算出来的 |
|---|---|---|
| 发送 | `AA 55 01 0B 0C` | `LEN = 1`（只有 CMD，没有 DATA）；`SUM = (0x01 + 0x0B) & 0xFF = 0x0C` |
| 收到 | `AA 55 04 0B 01 00 00 10` | `LEN = 4`（CMD 1 + DATA 3）；DATA = 主1 次0 修订0；`SUM = (0x04 + 0x0B + 0x01 + 0x00 + 0x00) & 0xFF = 0x10` |

> **上位机界面（Python）还没做**，目前只能自己拼字节，或用串口助手手动发。

---

## 十、出厂默认值一览

**第一次上电**（或 EEPROM 数据版本不匹配）时，全部恢复成下面这些值：

| 项 | 默认值 |
|---|---|
| 音量 | **1 %** |
| 震动强度 | **25 %** |
| 提醒方式 | 铃声 |
| 默认曲目 | 第 1 首 |
| 番茄钟 | **25 分钟专注 + 5 分钟休息** |
| 主屏 | 开 |
| 日出唤醒 | **开** |
| 贪睡时长 | **9 分钟** |
| **3 组闹钟** | **全部关闭**（每组的默认时间是 07:00、工作日） |
| 温湿度记录 | 从 0 条开始 |

> **怎么恢复出厂默认？** 用 STC-ISP 下载时**勾选**"擦除用户 EEPROM 区"再下一次即可
> （注意：这会清掉你存的所有闹钟和设置、以及温湿度记录）。

**掉电保存的东西**：时间（靠纽扣电池走）、3 组闹钟、全部设置项、温湿度记录。
**拔电一分钟再插上，这些都不会丢。**

---

## 十一、快速上手（3 分钟）

**第一次拿到板子，按这个顺序做一遍就全会了：**

1. **认键**：上电后按 **K2**（进任务清单），然后按另外两个键看光标 ——
   **往上走的是 K1，往下走的是 K3**，剩一个就是 K4。
2. **设一个 5 分钟后的闹钟**：
   - K4 回到大字时钟 → K2 进任务清单 → 用 K1/K3 选到「1 闹钟」→ K2 进入；
   - 光标在第 1 组上，**长按 K2**（约 2 秒）进编辑态；
   - 用**矩阵键盘直接输** `1145`（11:45）之类，或用 K1/K3 调到想要的时分；
   - 按 **K2** 逐字段跳过（时→分→星期→开关），**到"开关"时用 K1 或 K3 打开**；
   - 最后按 **K4** 返回（改动会自动保存）。
3. **等它响**：到点屏幕自动切到响铃页 → 按 **K2** 停止，或按 **K4** 贪睡 9 分钟。
4. **玩一局贪吃蛇**：K2 进任务清单 → 选到「6 游戏」→ K2 进入 → 光标在"贪吃蛇"上按 **K2** 开始
   → 用矩阵键盘 **9/12/13/14** 控制方向 → 想退出按 **第一行第四个键**。
5. **试一下测距**（如果接了超声波）：任务清单选到「3 测距仪」，把手放在模块前面，看数字变化。

---

## 十二、常见问题排查

### 12.1 屏幕 / 显示

| 现象 | 可能原因与处理 |
|---|---|
| **主屏全黑** | ① 排线/连接松动；② 上电时数小灯亮几颗，看卡在哪一步（第四节）；③ 主屏是软件 SPI，6 根线接触不良会全黑 |
| **副屏花屏、出现竖线或整屏滚动** | 断电重上电一次。若经常出现，检查副屏的 I2C 两根线（P3.2/P3.3）是否和别的线缠绕干扰 |
| **副屏黑屏** | ⚠️ **玩游戏时副屏会主动关闭，这是正常的**，退出游戏就回来。不是故障的话，检查接线 |
| **数码管不亮** | 上电时看小灯是否亮到第 5 颗；不亮说明卡在数码管初始化之前（供电/排线） |
| **数码管显示 `00-00-00` 不变** | 说明系统时间没在走 —— 检查时钟芯片（PCF8563）的纽扣电池，或重新校一次时间 |
| **主屏中文变乱码方块** | 源码编码问题（改代码的人用 UTF-8 编辑器改过）。重新转 GBK 后重编译重烧 |
| **屏幕偶尔闪一下乱码，随后自己恢复** | 已知的残余现象（单次写入被任务切换打断）。不影响使用 |

### 12.2 按键 / 输入

| 现象 | 可能原因与处理 |
|---|---|
| **按什么键都没反应** | ① 先看屏幕是否在正常刷新（在刷新说明 CPU 在跑，问题在按键）；② 独立按键在 P5.1~P5.4、矩阵键盘在 P0/P1/P3/P4，检查对应的排线 |
| **在游戏里按独立键没反应** | ✅ **正常**。游玩态下独立按键全部失效，只能用矩阵键盘（退出键 = 第一行第四个） |
| **在游戏里按方向键，游戏没动** | 确认是不是已经进了"结算画面"（等 1.5 秒会自动回列表） |
| **矩阵键盘输数字输不进去** | 数字键只在两个地方有效：**「0 日期和时间」的输入态** 和 **闹钟编辑的"时/分"字段** |
| **日期/时间输满了还在按** | 输满会锁住不再接受（防越输越长）。按 **K4 删一位**再改 |
| **闹钟列表里按键不灵** | 注意区分：**短按 K2 = 开/关闹钟**，**长按 K2 = 进编辑**。要进编辑必须按住约 2 秒 |

### 12.3 闹钟 / 时间

| 现象 | 可能原因与处理 |
|---|---|
| **闹钟不响** | ① 那一组是不是"关"着（列表里看开关状态）；② 星期的组合今天是否命中；③ 音量是不是 0；④ 提醒方式是不是设成了"震动"（那就只有马达震、不会响） |
| **时间不对 / 每次上电重置** | 时钟芯片的纽扣电池没电或没装。装上后重新校一次时间即可 |
| **贪睡按了没反应** | 贪睡只在**响铃页/响铃中**有效，而且是按 **K4**（不是 K2） |
| **日出唤醒灯不渐亮** | 设置里的「日出」要是"开"；并且只在闹钟响前 10 分钟内才渐亮 |

### 12.4 测距仪 / 云台

| 现象 | 可能原因与处理 |
|---|---|
| **测距一直是 0 / 失败** | ① **先查杜邦线**（TRIG→P2.4、ECHO→P3.6 插紧）；② 确认模块供电 5V、GND 共地；③ 看屏上 `RAW`：`500` = 等起跳超时、`3000` = 回波太长、长期 `0` = 没收到任何回波 |
| **测距数值偏大且越测越大** | 属于计时逻辑问题（不是标定问题），需要改固件 |
| **测距数值稳定但比例不对** | 用 `每厘米计数 = RAW ÷ 实际厘米数` 标定一次（第七节 7.4） |
| **舵机不转** | ① 信号线是否接 **P2.5**；② **舵机供电是否够**（别从板子 5V 取电，会拉低电压）；③ 出页面时舵机会被关掉输出，**必须在这个页面里操作** |
| **舵机抖动/乱转** | 供电不足的典型表现。换独立 5V 电源，并在舵机电源两端并一个大电容 |

### 12.5 编译 / 烧录（给要改代码的人）

| 现象 | 原因 |
|---|---|
| **下载时提示程序超出范围** | 程序上限 **61440 字节**（64KB 减 EEPROM 4KB）。⚠️ **Keil 编译时不会报这个错**（它的芯片定义以为能用满 64KB），所以要么自己看 `Program Size` 的 `code`，要么交给 ISP 判断。当前已用 58064，只剩 3376 字节 |
| 编译后**功能没变** | ① 忘了重新编译（Build）就下载；② 下载时没冷启动（断电重上电） |
| **下载后设置被清空** | STC-ISP 里勾了"擦除用户 EEPROM 区"。取消这个勾选 |
| 编译报 **UNRESOLVED EXTERNAL SYMBOL** | 有 `.c` 没注册进工程，或者删了某个还在用的文件。**按报错里的符号名反查** |
| 加了点代码就**编译不过（空间不足）** | 现在是 5.5% 余量，加功能前先做减法（见《交付说明》第十节） |

---

## 十三、已知限制（还没做的功能）

**这一节很重要 —— 老文档（《01-项目介绍》的早期版本）里写过一些功能，最终没做。**
别按老文档找它们：

| 老文档里说的 | 实际 |
|---|---|
| 一个单独的"游戏大厅"页面 | ❌ 没做。改成「6 游戏」页内的两个状态（先列表选，再原地开玩） |
| 每日挑战（用日期当随机种子） | ❌ 没做 |
| 玩游戏时副屏显示分数 / 最高纪录 | ❌ 没做。**游玩时副屏直接关闭** |
| 小灯当"生命值"（掉血灭一颗） | ❌ 没做。小灯只用于日出唤醒和上电进度指示 |
| 旋钮控制打砖块挡板 | ❌ 改成矩阵键盘方向键 |
| 游戏音效（吃、撞、过关各不同） | ❌ 没做，只有通用的按键提示音 |
| 撞击时马达震一下 | ❌ 没做 |
| 游戏最高分 / 存档 | ❌ 没做（EEPROM 里给游戏留的区一直空着） |
| 蓝牙模块 | ❌ 没做，改走 USB 串口 |
| 超声波"挥手停闹钟" | ⚠️ 没接进闹钟流程。超声波做成了独立的「3 测距仪」页面 |
| 舵机"物理敲铃" | ⚠️ 没接进闹钟流程。舵机做成了独立的「4 云台」页面 |
| 电脑上的 Python 界面 | ❌ 没做，只做了板子这侧的通信协议 |

**其他硬限制**：

| 限制 | 说明 |
|---|---|
| **程序空间只剩 5.5%**（3376 字节） | 再加功能必须先做减法 |
| 副屏**不能显示中文** | 副屏只有一个 7 条目的固定汉字表，中文菜单/提示全走主屏 |
| 副屏刷新慢（整屏约 350ms） | 所以运行期不做周期性整屏重画，只在换页时整块重画 |

---

## 附：一页速查卡

```
【导航】  大字时钟 --K2--> 任务清单 --K2--> 任务内 --K4--> 返回
任务清单：K1 上移 / K3 下移 / K2 进入 / K4 回大字时钟
任务内：  K1 上移 / K3 下移 / K2 确认 / K4 返回（多数页面 K4 会顺手保存）

【响铃时】K2 = 停止    K4 = 贪睡（其余键无效）

【校时】任务清单 → 0 日期和时间 → K1/K3 选「改年月日/改时分」→ K2 进入
        矩阵键盘输数字：年月日 8 位、时分 4 位（输满自动提交）
        K4 = 删最后一位；删光 = 取消并退回

【闹钟】K1/K3 选组 → K2 短按开关 / 长按进编辑 → 编辑态 K2 下一字段 K4 保存退出

【游戏】列表：K1/K3 选 → K2 开始 → K4 返回
        游玩：矩阵键 9=上 12=左 13=下 14=右
              ★ 退出 = 矩阵键「第一行第四个」
        游玩时独立按键全部失效、副屏关闭

【测距仪】进页面自动测；只有 K4 返回。RAW 在 20cm 处应约 112
【云台】  K1/K3 每次 ±10°，K2 回中位 90°，K4 返回（关舵机）

【出问题先数小灯】上电时 8 颗小灯是进度条：全亮=初始化通过、只剩 1 颗=系统正常在跑
```
