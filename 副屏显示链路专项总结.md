# 副屏显示链路专项总结（第 5 ~ 29 轮）

> 工程：`D:\Develop\embedded-project\51_MCU_Alarm`
> 时间段：2026-09-18 00:00 ~ 2026-09-19 01:29
> 当前状态：连按按键 / 连拧电位器 **5 分钟无花屏、无黑屏**；
> 最严重的残留现象是"某一行瞬态闪乱码，下次刷新即恢复"
> 编译：`0 Error(s), 0 Warning(s)`，`UNRESOLVED = 0`，浮点库 = 0，`code = 41579`

---

## 一、一句话结论

**副屏花屏/黑屏的根因是架构问题，不是参数问题：**

> **两个任务通过一把带超时强夺的锁，轮流访问同一块 I2C 控制器。**
> 锁一旦超时强夺，两个任务就会同时往总线发字节 → 字节流错位
> → 数据字节被 SSD1306 当成控制字节 → 控制器寄存器被写坏
> → 花屏（起始行/寻址模式）或黑屏，且 **MCU 重启救不回来**（OLED 未断电、控制器自锁）。

**修法只有一条：把所有 I2C 访问收进同一个任务**，从而根本不需要锁。

**这一晚最终撤掉了我在显示链路上加的全部自定义**：总线锁、限流、安全恢复序列、
周期性整屏重画、周期性控制器重初始化、按页分段清屏、只清尾部……
**全部回到"参考工程（demo30）怎么做就怎么做"。**

---

## 二、根因链（完整）

```
两个任务共用一个 I2C 控制器
        │  TASK_RENDER 刷屏  +  TASK_LOGIC 读时钟
        ▼
靠 I2C_Lock 交替 ──→ 一次整屏重画约 350ms > RTX51 时间片 25ms
        │             （必然"持锁被换出去"）
        ▼
锁超时（4s）→ 强制夺锁
        ▼
两个任务同时往同一控制器发字节
        ▼
字节流错位：数据字节被从机当控制字节
        ├─ 落进 0x40~0x7F  = 设置显示起始行 → 整屏垂直滚动
        ├─ 落进 0x20        = 设置寻址模式   → 一次写入跨页蔓延（"最后两行被拉宽占满整屏"）
        └─ 列地址错位                        → "左 1/3 正常、中间黑、右边光点"
        ▼
控制器状态被写坏 → 花屏 / 黑屏
        ▼
MCU 重启只是重跑 MCU，OLED 一直带电 → 控制器停在坏状态 → 重启救不回
只有拔插 USB（给模块断电）才恢复
```

---

## 三、最终修法（四条，全部照 demo30）

| # | 做法 | 依据 |
|---|---|---|
| 1 | **全工程 I2C 只在一个任务里访问**（本工程 = `TASK_RENDER`）。其余任务只置请求标志，由渲染任务统一执行 | demo30 的 `task_3` 里连 `RTC_show()` 读时钟都在（`case 3: RTC_show();`），天然串行 |
| 2 | **`I2C_OLED_Clear()` 只在切页时做一次**，运行期不做任何周期性整屏重画 | demo30 运行期没有周期性刷新 |
| 3 | **页内只写"内容变化的那一行"**，且**每行固定宽度**（补空格到 16 字符 = 128 列满宽） | demo30 是 `I2C_OLED_ShowString(0, 2, buf, 16)` = 32 字节；它的行字符串宽度全是死的 |
| 4 | **控制器初始化只在开机做一次**，运行期只写显存 | demo30 的控制器 init 只在开机 |

实现要点：`Clock_Set()` / `Alarm_ApplyHardware()` 改成"只登记请求"
（真正干活的改名 `Clock_SetNow()` / `Alarm_ApplyHardwareNow()`），
**这样所有既有调用点一行都不用改，自动变成"发请求"**；
`Display_Poll()` 开头统一执行 5 个请求标志。

---

## 四、时间线：哪些方向被真机数据否掉了

| 轮次 | 当时的假设 / 做法 | 被什么否掉 | 判定 |
|---|---|---|---|
| 第 5 轮 | 整屏持锁 80~90ms > 时间片 25ms，且锁 500ms 超时强夺 | — | 部分成立（改按页持锁），但不是根本 |
| 第 6 轮 | 每字节一次 I2C 事务 ⇒ 事务数太多 ⇒ 丢字节；改成整页批量写 | 第 7 轮：副屏全乱码、主屏环境行也停更 ⇒ 回退 | 方向对，实现失败 |
| S0 | 硬件 I2C 外设状态机会被任务切换搅乱 ⇒ **换软件 I2C** | 第 25 轮链接报 `_I2C_WRITENBYTE` 未解析 ⇒ **demo30 用的就是硬件 I2C** | ❌ **决定错误**，拆散了原驱动接线 |
| 第 12 轮 | 花屏不恢复 ⇒ 自愈时**重跑 `I2C_OLED_Init()`** 复位控制器 | S0 后续：init 第一条命令是 `0xAE`（关显示），跑不完就停在"已关显示" = **全黑** | ❌ 这条自愈是黑屏的直接原因 |
| 第 15 轮 | 按键/旋钮触发整屏重画形成"I2C 洪流" | 第 21 轮限流真正生效后**仍然黑屏** | 不是根因 |
| 第 17 轮 | 怀疑 `TASK_RENDER` 卡死 | 第 18 轮 `[DISP] poll` 每 5 秒**精确 +1000** | ❌ 否掉，任务一直是健康的 |
| 第 19 轮 | 限流 300ms < 一次重画 350ms = 没限住 | 修正为 1200ms，第 21 轮确认生效 | 附属问题 |
| 第 22 轮 | 负载极低仍黑屏 ⇒ "软件侧已穷尽，指向电气/模块" | 第 25~26 轮架构改造后症状大幅改善 | ❌ **推论过头** |
| 第 23 轮 | 蜂鸣器静音却有"嗡嗡"声 ⇒ 震动马达百毫安级负载拉低 3.3V | 第 24 轮整段日志**没有** `[MOTOR] Motor_On() called` | ❌ 否掉 |
| 第 24 轮 | 闲置状态（无任何按键）也会黑屏 ⇒ 差异不在负载 | — | ✅ 成立，指向"并发访问"而非"负载" |
| 第 25~26 轮 | **全部 I2C 收进一个任务** | 第 27 轮：5 分钟压测无花屏/黑屏 | ✅ **生效，根因确认** |
| 第 27 轮 | 删周期整屏重画 + 只重画变化行 | 第 28 轮发现行尾残影 | 方向对，缺"固定宽度" |
| 第 28 轮 | 行写入补空格到 16 字符满宽 | 待用户验证 | 已修 |
| 第 29 轮 | "demo30 是事件驱动架构" | grep 证实 8 个任务里只有 2 个用信号 | ❌ **概括错误，已补正** |

**统计：被真机数据否掉的假设 6 个，另有过头推论 1 个。**

---

## 五、可复用的诊断手法（本次真正管用的）

**① 参照系诊断**
找一个**不依赖待查对象**的计数器当基准。
本次 `[TICK] g_sysTick` 放在 `TASK_SENSOR` 里打印——它的 1 秒由 RTX51 的 `os_wait2` 数出来，
**不依赖 `g_sysTick` 自己**，所以是天然可靠的参照系。
一行"每 5 秒只 +23（应 +5000）"就定死了 Timer3 漏赋 `TIM_PS`。

**② 把内部状态打印出来，一次否掉"任务卡死"**
`Display_Poll` 入口计 `poll`、副屏刷新计 `sub`、整屏重画计 `full`。
`poll` 精确 +1000 ⇒ 任务健康，问题不在任务层。

**③ 用"算术可能性"判断内存被踩**
BCD 掩码后月份最大 25、小时最大 45。日志出现 `y=259 mo=93 94:08:07` ⇒ 一定是内存被踩，别再查外设。

**④ A/B 对照实验**
把**已验证的参考工程**烧进同一块板、用同样手法操作。
用户做的这个实验（demo30 连按按键 90 秒完全正常）是整段排查里最关键的一步。

**⑤ 先写判读表，再等数据**
每个改动交付时都要给出："看到 X ⇒ 结论 A；看到 Y ⇒ 结论 B（则此方向作废）"。
没有判读表的改动，只能换来"还是不行"，白烧一轮。

---

## 六、本次会话的其他改动（与副屏无关）

| 项 | 内容 | 依据 |
|---|---|---|
| 电位器量程 | 改用**固定满量程 2740**，开机即生效；删除"运行期自学习量程" | demo30 `App_Motor.c`：`PWM6_Duty = PERIOD * adc / 2740` |
| 矩阵键盘校时 | 从 `PAGE_HOME` 分支**搬到全局位置**（排除闹钟编辑页） | 用户现象："一按独立键就不能调时间" |
| VOL 显示 | 改为**实时旋钮档位百分比** `VOL%4d%%`（0~100%） | 照 demo30 `"Duty:%6.2f%%"` 的固定宽度风格 |
| 响铃提示行 | `"STOP:KEY1 SNOOZE:KEY2"`（21 字符，超宽被截）→ `"1 STOP 2 SNOOZE"`（16 字符） | 屏宽 128 列 = 16 字符 |
| 蜂鸣器 / 马达 | 各自加总开关宏（`BUZZER_ENABLE` / `MOTOR_ENABLE`），当前均关闭 | 用户要求"最后再处理" |

---

## 七、剩余未解项（非阻塞）

1. **矩阵键盘校时**：搬位置后待真机确认是否彻底解决。
2. **副屏"某行瞬态闪乱码"**：架构改造后已从"永久损坏"降级为"瞬态自愈"，
   但未彻底归零。属于**单次 32 字节写入被任务切换打断**的残余概率，
   不是控制器被写坏（因为它下一次刷新就恢复）。
3. **唤醒方式**：本工程仍是 5ms 轮询，demo30 的 2 个屏幕任务是 `os_wait1(K_SIG)`。
   **经评估不建议切换**（收益约 0.2% CPU，代价是引入"漏发信号 → 显示静默冻结"的失败模式）。
   理由与证据见 `demo30参考代码分析.md` §2.1 / §4.1。
4. **日期**：显示 2000-01-01（主界面校时只设时/分），完整年月日校时留到 UI 重构。

---

## 八、这一晚的代价（记下来）

| 项 | 数 |
|---|---|
| 副屏专项跨度 | 第 5 轮 → 第 29 轮 |
| 被真机数据否掉的假设 | 6 个（另有过头推论 1 个） |
| 因"跨项目混抄"白烧的固件 | 约 3 轮（S0 软 I2C 及其回退） |
| 最终结论 | **显示链路上所有自定义（锁/限流/安全恢复/周期刷新）全部撤掉** |

**最重要的一条教训：**

> **说"照抄"就要照抄同一个项目的同一套实现。**
> 不能从 A 项目抄驱动、从 B 项目抄总线、再自己接线。
> 这一晚最严重的错误就是：拿另一个目录的"软件 I2C"方案，
> 去改造一个本来用硬件 I2C、而且真机跑得好好的驱动。

---

## 九、相关文档

| 文档 | 内容 |
|---|---|
| `demo30参考代码分析.md` | demo30 的四条设计原则、逐项对照表、唤醒方式权衡（§2.1 / §4.1） |
| `真机联调记录-1.md` / `-2.md` | 第 1~2 轮的详细记录 |
| `M1真机联调总结.md` | 第 1~13 轮（九个真问题 + 两次弯路） |
| `硬件真值表.md` | 动手前的门禁表（引脚/电平/坐标单位/库函数签名） |
