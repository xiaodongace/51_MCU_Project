# 一机两面终端 · 整体框架

> **工程**：`D:\Develop\embedded-project\51_MCU_Alarm`（`Project.uvproj`）
> **芯片**：STC8H8K64U @ 24MHz ｜ **OS**：RTX51 Tiny ｜ **编译器**：Keil C51（Optimize 8，Large 模型）
> **版本**：`APP_VERSION_STR` = `"1.0.0"`
> **本文性质**：现状描述。所有结构、数字、行为都从代码与编译产物实测，
> **不是设计意图**。凡"为什么这么做"的地方，都给了代码里的证据。
> 最后核对：2026-09-23（`code=58424 / xdata=3822 / data=46.1`）

---

## 一、一句话概括

一台**双屏**桌面终端：主屏（SPI，带字库）做**任务切换与汉字界面**，副屏（I2C，无字库）做**当前任务的数值/图形**；
7 个任务页（日期时间 / 闹钟 / 番茄钟 / 测距仪 / 云台 / 设置 / 游戏）；
由 5 个常驻 RTX51 任务按**职责**分工驱动，全部外设走"状态 → 刷新层 → 两块屏"单向数据流。

---

## 二、规模与目录

### 2.1 五层结构

```
51_MCU_Alarm/
├── User/           程序入口（只有 main.c）
├── App/            业务层：页面、功能、显示
│   └── Game/       掌机三个小游戏（自成一块，不依赖 App 其他模块）
├── Driver/         器件层：一个器件一个文件
│   ├── SPI_OLED/   主屏（带硬件字库）
│   └── I2C_OLED/   副屏（含 ASCII 字库 oledfont.h）
├── Lib/            STC 官方库（已按本工程裁剪）
└── OS/             Conf_tny.A51（RTX51 Tiny 配置）
```

**分层规矩**（`App_Public.h` 头部原文）：

- `Lib/` 放寄存器操作与官方外设驱动，**不含任何业务**；
- `Driver/` 把某个器件封装成函数，**不含页面概念**；
- `App/` 只 include `App_Public.h` 一个入口，**不直接碰寄存器**。

> **一句话判据**：想知道"某个操作该写在哪一层"——看它有没有"页面/任务"的语义。
> 有就是 App，没有就是 Driver。

### 2.2 规模

| | 数量 |
|---|---|
| 源文件 | **98 个**（`.c` + `.h` + `Conf_tny.A51`） |
| 代码行数 | **18704 行**（`.c` 13952 + `.h` 4752） |
| 程序占用 | `code 58424` / 上限 61440 = **95.1%** |
| 数据占用 | `xdata 3822` / 8192、`data 46.1` / 256 |

**最大的 5 个文件**（按行数）：

| 文件 | 行数 | 职责 |
|---|---|---|
| `App/App_Menu.c` | 1984 | 页面栈 + 全部页面的按键处理 + 掌机模式 |
| `App/App_Display.c` | 1909 | 两块屏的所有版面 |
| `App/App_Icons.c` | 656 | 7 张 128×64 任务图标（纯数据） |
| `App/App_Alarm.c` | 619 | 闹钟调度（全项目唯一的"真算法"） |
| `Driver/SPI_OLED/spi_oled.c` | 524 | 主屏驱动 |

---

## 三、启动流程

### 3.1 `sys_init()` 的 8 步（`App/App_System.c`）

顺序**不能随意调**，每一步都有理由：

| 步 | 做什么 | 为什么是这个位置 |
|---|---|---|
| 1 | `Motor_SafeLevel()` / `Buzzer_SafeLevel()` / `Servo_SafeLevel()` | **先按安全电平、再配端口模式** —— 此时端口还是准双向口，写 0 能可靠拉低。舵机信号脚若上电为高，等于一个超长脉宽，舵机会甩到极限位置 |
| 1 | `Led_Init()` | **故意只配端口 + 全灭，不开总开关** —— 总开关关着，8 颗灯物理上不可能亮（用户要求"开机动画期间其它设备不能有反应"） |
| 2 | `EAXSFR()` → `Timers_Init()` | 扩展寄存器使能必须在写扩展 SFR 之前；**1ms 时基必须最早**，后面一切"看钟算差值"的逻辑都依赖它 |
| 3 | `GPIO_config()` / `UART_config()` / `I2C_config()` / `ADC_config()` | 通信与模拟外设 |
| 3.5 | `SPI_OLED_Init()` → `boot_animation()` → `Led_Power(1)` | 开机动画卡在"全系统最干净的窗口"：`EA=0`（三个中断都不会来）、灯总开关关着、蜂鸣器/马达/数码管/舵机都没 Init |
| 4 | `Buzzer_Init()` / `Motor_Init()` | 两者同属 PWMB，通道 5 与 6 |
| 5 | `Nixie_ScanInit()` + `Nixie_SetTime(0,0,0)` | 数码管由 Timer2 中断刷，配在这里让数码管与其它外设同时就绪 |
| 6 | `Storage_Load()` | 只读不写，**不做任何 I2C 访问** |
| 7 | `EA = 1` | 开总中断。**在它之前，Timer2/Timer3/INT3 一个都不会来** |

### 3.2 上电进度指示（`BOOT_CRUMB`）—— 唯一的"疑难杂症"诊断手段

`sys_init()` 每一步调一次 `BOOT_CRUMB(n)`，把 8 颗小灯当进度条。
它**直接写 IO 口**，不经过任务、不排队、不依赖任何外设初始化 —— 符合"诊断输出不能依赖被诊断对象"这条硬规矩。

| 现象 | 含义 |
|---|---|
| 亮 k 颗 | `sys_init()` 走到第 k 步停住 → 问题在第 k+1 步 |
| 亮 8 颗 | `sys_init()` 全部走通（任务已创建） |
| **只剩第 1 颗** | 五个任务都跑起来了，系统正常 |
| 全灭 | 卡在第 1 步之前 |

交付正式固件时把 `App_Public.h` 的 `BOOT_CRUMB_ENABLE` 改成 0 即可全部去掉。

---

## 四、任务模型（RTX51 Tiny）

### 4.1 五个任务 + 一个初始化任务

| 任务号 | 名字 | 周期 | 职责 | 文件 |
|---|---|---|---|---|
| 0 | `TASK_MAIN` | 一次性 | 配外设、建其它任务、**自毁**（`os_delete_task(0)`） | `User/main.c` |
| 1 | `TASK_RENDER` | **5ms** | 刷两块屏；**全工程唯一的 I2C 执行者** | `App/App_Display.c` |
| 2 | `TASK_INPUT` | **10ms** | 按键 / 矩阵键盘 / 旋钮 → **事件队列** | `App/App_Input.c` |
| 3 | `TASK_LOGIC` | **10ms** | 串口协议、闹钟调度、页面栈、中断标志清理 | `App/App_Menu.c` |
| 4 | `TASK_MUSIC` | **5ms** | 换音符、马达计时、每 100ms 调一次日出亮度 | `App/App_Music.c` |
| 5 | `TASK_SENSOR` | **1s** | 读 DHT11 / NTC，每 10 分钟存一条记录 | `App/App_Sensor.c` |

**RTX51 配置**（`OS/Conf_tny.A51`）：心跳 **5ms**（`INT_CLOCK = 10000`，Timer0 12T）、时间片 25ms。

> **关键点：任务按"职责"分，不按"外设"分。**
> 每个任务要服务**全部 7 个页面** —— `TASK_RENDER` 画所有页、`TASK_INPUT` 收所有键。
> 所以这 5 个任务只要系统在跑就必须一直在，**不能按页面创建/销毁**。
> （参考工程 `02_扩展板自检程序v3.1` 是"一个外设一个任务 + 动态创建/销毁"，那是自检程序的模型。）

### 4.2 任务间通信只走两条路

**① 事件队列**（`App_Input.c`，16 条，`Event_t` 结构 2 字节）

```
Driver 层的按键扫描（Keys/MatrixKey 回调）
        ↓  立即上报，不阻塞
TASK_INPUT 的 evt_push()  →  s_queue[16]（xdata 环形队列）
        ↓  TASK_LOGIC 每 10ms 取一条
Menu_OnEvent(evt)
```

- **队列满时丢最旧的一条**（不是丢新的）—— 代码原话："保证最新的操作不丢，比丢新的更友好"。
- 独立按键**短按在抬手时上报**、长按在按满 2 秒时上报，两者不互相污染；矩阵键盘只在按下时产生事件。

**② I2C 请求标志**（`App_Public.h`，5 个 `volatile bit`）

```
任何任务想动 I2C  →  只置一个标志（g_reqClockRefresh / g_reqClockSet /
                    g_reqClockRecover / g_reqAlarmApply / g_reqRtcIrq）
        ↓  TASK_RENDER 看到标志才真正执行 I2C 操作
```

这条是**全工程最重要的架构决定**，见 §5.3。

### 4.3 为什么数码管不在任务里

数码管由 **Timer2 的 1kHz 中断**直接刷（`Driver/NixieScan.c`），不走任务。

原因：RTX51 心跳是 5ms，**任务最快也只能 5ms 醒一次**，而设计要求"每 2ms 刷一位"在 RTX51 下做不到。
所以扫描放在中断里，任务只负责"往缓冲里填要显示的数字"（`Nixie_SetTime`）。

---

## 五、三条数据流

### 5.1 输入链路（按键 → 页面动作）

```
物理键
 ├─ 4 个独立按键  → Driver/Keys.c（20ms 非阻塞消抖 + 2 秒长按）
 ├─ 4x4 矩阵键盘  → Driver/MatrixKey.c（先拼 16 位再比较 + 逐键消抖）
 └─ 旋钮（电位器）→ Lib/ADC.c → App_Input 连读 8 次取平均、变化 <2 档忽略
        ↓  回调
App_Input（打包成 EVT_*）
        ↓  队列
TASK_LOGIC → Menu_OnEvent(evt)
        ↓
App_Menu 的页面分派（一个巨型 switch，按 Menu_Current()）
        ↓
改状态（App_Menu / App_Alarm / App_Settings …）
        ↓  置"要重画"或"要动 I2C"的请求
TASK_RENDER → 两块屏
```

**事件类型**（`App_Public.h`）：`EVT_KEY1..4`（短按）、`EVT_KEY1_LONG..4_LONG`、`EVT_MATRIX`（param 0..15）、`EVT_POT`（param 档位）。

### 5.2 显示链路（状态 → 两块屏）

```
各模块的状态（g_clock / g_settings / g_alarms / 各页面的 s_xxx）
        ↓
App_Display：main_redraw() / sub_redraw()
        ↓
主屏：main_draw_line_fit()   ← 每次写满 128px 整行
副屏：sub_draw_line()        ← 每次写满 16 字符
        ↓
Display_Refresh(clearScreen)
```

**`Display_Refresh` 的参数是这套框架的核心开关**：

| 参数 | 行为 | 什么时候用 |
|---|---|---|
| `1` | **允许清屏**（整屏重画） | 切页、内容形态变了 |
| `0` | **只重画、绝不清屏** | 数值在变（时间、测距、音量…） |

> **两个硬约束**（都是真机换来的）：
> 1. **`clearScreen=1` 的请求会走 100ms 执行点限流**（防止"洪流"）。
>    所以"数值刷新"绝不能传 1 —— 那会让界面卡顿、闪黑。
> 2. **不清屏就必须保证"写入覆盖整屏"**，二者必居其一。
>    这就是为什么主屏的 `main_draw_line_fit()` 会把每行补空格到 128px 满宽。

### 5.3 I2C 串行化（方案 B）—— 全工程最关键的一条

**问题**：主屏走 SPI，副屏和时钟芯片（PCF8563）**共用 I2C 总线**。
如果两个任务各自去碰 I2C，就可能"A 发到一半被 B 插进来" → 字节流错位 → **副屏花屏/黑屏、控制器被写坏**。

**做法**（照 demo30）：**所有 I2C 访问都收在 `TASK_RENDER` 一个任务里**。

```
TASK_LOGIC（读时间、校时、写闹钟、清中断标志）
TASK_INPUT / 按键
串口协议处理
        ↓  都只置标志，绝不直接碰 I2C
TASK_RENDER → 真正执行 I2C（读时钟 / 写时钟 / 写闹钟 / 写副屏）
```

- 因为**串行**，根本不需要总线锁 —— 也就不会有"锁竞争 → 超时强夺 → 两个任务同时发字节"这条链。
- `Driver/I2C_Lock.c` 仍然保留（`I2C_Lock/Unlock`），作为"将来改动"的安全网 ——
  `App_Alarm.c` 的注释原话："**保留但永不争用**"。

**副作用（要记住）**：`Clock_Init()` 也放在 `TASK_RENDER` 里，因为初始化要读 PCF8563。

---

## 六、页面与菜单

### 6.1 页面栈（`App_Menu.c`）

"像一摞盘子"——进一页往上摞，返回就拿掉最上面的。深度上限 `MENU_STACK_MAX = 4`。

| API | 行为 |
|---|---|
| `Menu_Push(page)` | 先 `menu_task_enter(page)`，再压栈，然后 `Display_SetPage + Refresh(1)` |
| `Menu_Back()` | 先 `menu_task_leave(当前页)`，再弹栈；落到 `PAGE_HOME` 时**停在任务清单**（不是大字时钟）、且**光标停在刚离开的那个任务上** |
| `Menu_Home()` | **把栈里每一层都逐层 `leave`**（不能只看当前页），清到只剩 `PAGE_HOME` |

> **`Menu_Home` 逐层收尾的原因**（真机踩过）：
> 在云台页被闹钟打断 → 跳到响铃页 → 停止后回主页。
> 此刻"当前页"是响铃页，如果只关当前页，**云台页被弹出去了却没人关它的舵机 PWM**，
> 舵机就一直挂在总线上（现象："没进云台页舵机也会动"）。

### 6.2 进入 / 离开钩子（只有一对入口）

**原则（照参考工程）**：进入 = **完整初始化**，离开 = **彻底收尾**，不依赖上一次留下的任何状态。

| 钩子 | 谁需要挂钩子 |
|---|---|
| `menu_task_enter()` | **独占型外设**：云台（PWMA 通道 3）、测距仪（P2.4/P3.6） |
| `menu_task_leave()` | `PAGE_SERVO` → `Servo_Off()`；`PAGE_RANGE` → `SR04_Stop()`；`PAGE_HOME` → 退出"日期和时间"子模式；`PAGE_GAME_HALL` → 清游戏状态 |

纯读传感器的页面（时钟、温湿度）**不需要** —— 它们的外设在 `sys_init()` 里配一次就够。

### 6.3 任务清单（第 1 级）与页面映射

**屏上编号 1~7**（`App_Display.c` 里 `sprintf(..., k + 1, ...)`，内部下标从 0 算）。
`Menu_IndexOf()` 是"页面 → 第几项"的反查。

| 屏上编号 | 任务名 | 对应页面 | 内容位置 |
|---|---|---|---|
| 1 | 日期和时间 | `PAGE_HOME`（子模式，**不压栈**） | 主屏选项 + 副屏图标 |
| 2 | 闹钟 | `PAGE_ALARM_LIST` / `PAGE_ALARM_EDIT` | 列表 3 组 |
| 3 | 番茄钟 | `PAGE_POMODORO` | 主屏数值 + 副屏进度 |
| 4 | 测距仪 | `PAGE_RANGE` | **距离只在副屏**（主屏只有提示） |
| 5 | 云台 | `PAGE_SERVO` | 主屏角度 + 副屏半圆仪表盘 |
| 6 | 设置 | `PAGE_SETTINGS` | 7 个设置项 |
| 7 | 游戏 | `PAGE_GAME_HALL` | 页内 3 个状态 |

**第 0 级**是"大字时钟"（`s_menuOpen == 0`），不是任务页。
所以"第 1 级任务清单"在用户口语里叫"第二级菜单"。

### 6.4 空闲返回

`Menu_CheckIdle()`：**只认"任务清单"这一层**才触发 30 秒空闲返回主界面。

判据是 `Menu_Current() == PAGE_HOME && Menu_IsOpen()`（两个条件缺一不可）——
因为 `menu_enter()` 第一句就把 `s_menuOpen` 清 0，进了任务页之后这个标志已经是 0，单看它会把任务页错判。

### 6.5 掌机模式（第 7 项）

**没有独立的大厅页面** —— `PAGE_GAME_HALL` 就是任务页本身，页内 3 个状态：

| 状态 | 表现 |
|---|---|
| 0 列表 | 主屏列 3 个游戏 + `K2 开始 K4 返回`；副屏亮 |
| 1 游玩 | 主屏由**游戏自己**画（显示层直接 return）；**副屏自动关**（`DisplayOff`）；独立按键全部屏蔽 |
| 2 结算 | 显示 `GAME OVER` + `SCORE:n`，停 3 秒后自动回列表 |

| 操作 | 键 |
|---|---|
| 上 / 左 / 下 / 右 | 矩阵键盘 `9 / 12 / 13 / 14` |
| 飞机大战发射 | `13`（**与"下"同键**，先判发射） |
| 强制退出 | 矩阵键盘 `3`（第一行第四个） |

**外壳驱动三个游戏**（`Game_Poll`，在 `TASK_LOGIC` 里）：节拍按 **`g_sysTick` 的毫秒差**算，不数"任务被调用的次数"。

| 宏 | 值 | 含义 |
|---|---|---|
| `GAME_FRAME_MS` | 30 | 打砖块 / 飞机，每帧 30ms |
| `GAME_STEP_MS_SNAKE` | 250 | 贪吃蛇每步 250ms（它每步走 8 像素） |
| `GAME_FIRE_CD_MS` | 3000 | 发射冷却 3 秒 |
| `GAME_OVER_HOLD_MS` | 3000 | 结算画面停留 3 秒 |

---

## 七、双屏分工与刷新策略

| | 主屏（SPI） | 副屏（I2C） |
|---|---|---|
| 器件 | SPI OLED，**带硬件汉字字库** | SSD1306，字库在 MCU 里（`oledfont.h`） |
| 能显示 | 汉字、图标、大字数字 | **只有 ASCII**（Hzk 表里仅 7 个固定汉字字模） |
| 角色 | 任务切换 + 汉字界面 | 当前任务的数值 / 图形 |
| 寻址 | 按**页**（16px 汉字占 2 页，写一行必须用**偶数页**） | `line*2` 已在驱动内部处理 |
| 行写入 | `main_draw_line_fit()`（补满 128px） | `sub_draw_line()`（补满 16 字符） |
| 图标 | 7 张 **128×64 整屏**（`App_Icons.c`，各 1024 字节） | — |

**为什么副屏必须"固定宽度"**：它的刷新粒度是"只重画变化的那一行"。
如果新字符串比上一帧短，旧内容的尾巴会留在屏上 —— 真机现象：`VOL 10/10 SONG 1`(15 字)
降到 `VOL 0/10 SONG 1`(14 字)，显示成 `VOL 0/10 SONG 11`（多出来那个 `1` 是残留）。

---

## 八、外设与资源分配

### 8.1 定时器（5 个全有主，**没有空闲的**）

| 定时器 | 用途 | 配置 | 谁配的 |
|---|---|---|---|
| **Timer0** | RTX51 Tiny 心跳，**5ms** | 12T（`AUXR` 未动，复位默认） | `Conf_tny.A51` |
| **Timer1** | UART1 波特率发生器 | 16 位自动重载 + 1T | `Lib/UART.c`（`ET1=0` 不开中断） |
| **Timer2** | 数码管动态扫描，**1kHz** | 16 位自动重载 + **1T** | `Driver/NixieScan.c` |
| **Timer3** | **1ms 系统时基**（`g_sysTick`） | 16 位自动重载 + **1T** | `Driver/Timers.c` |
| **Timer4** | **空闲**（曾给超声波当计数器，改回软件延时后释放） | — | — |

> **"看起来没用"的陷阱**：Timer1 被占用的证据在 **`Lib/UART.c`** 里，不在 App 层；
> 它 `ET1 = 0` 不产生中断，所以搜"Timer1 中断函数 / `Timer_Inilize`"全都搜不到，
> 但它确实在跑（`TR1 = 1`、TH1/TL1 和自动重载模式全归波特率逻辑）。
> **判"某定时器是否空闲"要看寄存器与配置，不能看有没有中断代码。**

### 8.2 中断（4 个使能，2 个显式禁止）

| 中断 | 状态 | 优先级 | 用途 |
|---|---|---|---|
| Timer2 | **ENABLE** | 0 | 数码管扫描 |
| Timer3 | **ENABLE** | 0 | 1ms 时基 |
| INT3 | **ENABLE** | 0 | PCF8563 闹钟到点（**只置一位标志 `g_rtcIrqFlag`**） |
| UART1 | **ENABLE** | 1 | 串口收发缓冲 |
| I2C | **DISABLE** | 0 | 收发走**查询式**，不用中断 |
| ADC | **DISABLE** | 0 | NTC 用查询式 |

**中断里只做最少的活**：INT3 只置标志，I2C 清标志与业务派发都留到 `TASK_LOGIC`。

### 8.3 PWM 通道

| 通道 | 用途 | 引脚 | 开关宏 |
|---|---|---|---|
| PWMA 通道 3 | 舵机云台 | **P2.5** | `PWM3_USE_P24P25()` + `ENO3N` |
| PWMB 通道 5 | 无源蜂鸣器 | **P0.0** | `PWM5_SW(PWM5_SW_P00)` |
| PWMB 通道 6 | 振动马达 | **P0.1** | `PWM6_SW(PWM6_SW_P01)` |

> `Lib/STC8H_PWM.c` 与 `Lib/Timer.c`、`Lib/GPIO.c` 已按"只用到的通道/分支"裁剪
> （`PWM_Configuration` 只留 PWMA/PWMB，`UpdatePwm` 只留 PWM3/PWM6）。

### 8.4 引脚分配（全部从各驱动的 `.h` 实测）

| 器件 | 引脚 | 说明 |
|---|---|---|
| **主屏 SPI OLED** | SCL=**P5.0**、SDA=**P1.3**、DC=**P1.6**、CS=**P4.7** | 另两根给屏上字库 IC：ROM_CS=**P1.0**、READ_FS0=**P1.1** |
| **副屏 I2C OLED** | SCL=**P3.2**、SDA=**P3.3** | 硬件 I2C，`I2C_SW(I2C_P33_P32)` |
| **时钟 PCF8563** | 同上 I2C 总线 | 到点中断走 **INT3** |
| **数码管（8 位，595 驱动）** | 控制线 **P4.2 / P4.3 / P4.4**（推挽） | 位选/段选由 595 串出，Timer2 1kHz 中断扫描 |
| **超声波** | TRIG=**P2.4**（推挽输出）、ECHO=**P3.6**（高阻输入） | |
| **舵机** | **P2.5** | PWMA 通道 3（`PWM3_USE_P24P25()` + `ENO3N`） |
| **蜂鸣器** | **P0.0** | PWMB 通道 5（`PWM5_SW(PWM5_SW_P00)`） |
| **振动马达** | **P0.1** | PWMB 通道 6（`PWM6_SW(PWM6_SW_P01)`） |
| **独立按键 K1~K4** | **P5.1 / P5.2 / P5.3 / P5.4**（准双向） | 20ms 非阻塞消抖 + 2s 长按 |
| **矩阵键盘 4×4** | 列 COL1~4 = **P0.3 / P0.6 / P0.7 / P1.7**；行 ROW1~4 = **P3.4 / P3.5 / P4.0 / P4.1**（全准双向） | 先拼 16 位再比较 |
| **NTC 热敏电阻** | **P0.4**（`ADC_CH12`） | 查询式 ADC，不用中断 |
| **DHT11 温湿度** | **P4.6**（准双向） | 单总线时序 |
| **8 颗小灯** | LED1~8 = **P2.7 / P2.6 / P1.5 / P1.4 / P2.3 / P2.2 / P2.1 / P2.0** | 进度指示 + 状态；总开关 `LED_SW` = **P4.5** |
| **串口 UART1** | **P3.0 (RxD) / P3.1 (TxD)** | 经板载 CH340N 到 USB |

> - 引脚宏**都写在各自驱动的 `.h` 里**（`XXX_GPIO_INIT()` / `sbit` / `#define` 形式），
>   改引脚只改头文件，`.c` 不动。
> - **`P2.4` 是超声波 TRIG，不是小灯** —— 舵机的 `PWM3_USE_P24P25()` 只改引脚映射、
>   不改 `PWMA_ENO`，而 `PWM_Configuration()` 里走 else 分支会把 `ENO3P` 清 0，
>   所以 P2.4 保持普通 IO（代码级证据见《硬件真值表》1.13）。
> - 完整的引脚出处与冲突清单见 **《硬件真值表》**。

---

## 九、存储与通信

### 9.1 片内 EEPROM（4KB @ `0xF000`，8 个 512 字节区）

| 区 | 地址 | 内容 |
|---|---|---|
| 0 | `0xF000` | `Settings_t` + `AlarmItem_t[ALARM_MAX]`（3 组） |
| 1 | `0xF200` | **空着**（预留给游戏存档，当前一个字节都不写） |
| 2~7 | `0xF400`~ | 温湿度记录，**2 字节/条**（`s8` 整数℃ + `u8` 湿度），共 **1536 条** ≈ 10.7 天 |

**为什么要这样切**：

- 设置与闹钟放**同一区** → 一次擦除、一次写回，中间不会出现"擦了没写"的窗口。
- 记录区是**先擦后写的顺序填充**（擦除后是 `0xFF`，湿度不可能等于 `0xFF`，用它当空位标记），
  可以直接按字节写，不必每写一条擦一次整页。
- 写指针存在 `logCount` 里，但**只在"每 32 条"或"设置变更"时**才回写 0 号区 ——
  避免 10 分钟擦一次（那样 100k 次寿命只够两年）。
- 设置结构有 `magic` + `version` 双校验：版本不匹配就当"首次上电"重新填默认值。

**注意**：EEPROM 占了 `0xF000~0xFFFF`，所以**真实程序上限是 61440 字节**，
不是 64KB（Keil 的 `IROM` 定义写的是 `0xFFF8`，编到 60KB 以上它不会报错，但 ISP 下载会拒收）。

### 9.2 串口协议（`App/App_Uart.c`，UART1）

```
+------+------+------+------+-----------+------+
| 0xAA | 0x55 | LEN  | CMD  | DATA[..]  | SUM  |
+------+------+------+------+-----------+------+
```

用途：Python 上位机设置闹钟（替代蓝牙 + App）。当前**没有上位机界面**，只能自己拼字节。

---

## 十、模块清单与职责

### App 层（含 Game）

| 文件 | 职责 | 关键接口 |
|---|---|---|
| `App_Public.h/.c` | **全 App 层唯一入口**：任务 ID、公共类型、跨模块全局量 | `g_sysTick` / `g_clock` / `g_settings` / `g_alarms` |
| `App_System.c` | 系统初始化（§3） | `sys_init()` |
| `App_Menu.c` | **页面栈 + 全部页面的按键处理 + 掌机模式** | `Menu_Push/Back/Home` / `Menu_OnEvent` / `Game_Poll` |
| `App_Display.c` | 两块屏的**全部版面** | `Display_Refresh` / `Display_SetPage` / `Display_Poll` |
| `App_Input.c` | 事件队列 | `Input_GetEvent` / `Input_Scan10ms` |
| `App_Clock.c` | 时间服务（PCF8563 之上的薄封装） | `Clock_Refresh` / `Clock_Set` |
| `App_Alarm.c` | **闹钟调度（全项目唯一的真算法）** | `Alarm_Tick1s` / `Alarm_ApplyHardware` |
| `App_Storage.c` | 掉电保存 | `Storage_Load` / `Storage_SaveAll` |
| `App_Music.c` | **非阻塞音乐引擎**（音符表 + 看钟换音） | `Music_Play` / `Music_Tick` |
| `App_Sensor.c` | 采集任务 | `Sensor_Tick1s` |
| `App_Uart.c` | 上位机协议 | `Uart_Poll` |
| `App_Font.c` | 16×32 大字数字字模 | — |
| `App_Icons.c` | 7 张 128×64 任务图标（数据） | `Icon_Get` |
| `Game/App_GmSnake.c` | 贪吃蛇（链表 + 静态节点池） | `Snake_Update/Draw/HandleInput` |
| `Game/App_GmBrick.c` | 打砖块 | `Brick_Update/Draw/MovePaddle` |
| `Game/App_GmPlane.c` | 飞机大战 | `PlaneGame_Update/Draw/HandleKey/Shoot` |

### Driver 层（一个器件一个文件）

| 文件 | 器件 | 接口风格 |
|---|---|---|
| `Keys.c` | 4 个独立按键 | `Keys_Scan` + **回调** `Keys_on_keydown/keyup/longpress`（由 App 实现） |
| `MatrixKey.c` | 4×4 矩阵键盘 | `MK_Scan` + 回调 `MK_on_keydown/keyup` |
| `SPI_OLED/spi_oled.c` | 主屏 | `SPI_OLED_Display_GB2312_string` 等 |
| `I2C_OLED/I2C_OLED.c` | 副屏 | `I2C_OLED_ShowString` / `DisplayOn/Off` |
| `NIXIE.c` + `NixieScan.c` | 数码管段码表 + Timer2 扫描 | `Nixie_SetTime` / `Nixie_Scan1ms` |
| `PCF8563.c` | 时钟芯片 | `PCF8563_get_clock` / `set_alarm` / INT3 |
| `HC_SR04.c` | 超声波 | `SR04_Measure` / `SR04_Stop` |
| `Servo.c` | 舵机（PWMA 3） | `Servo_SetAngle` / `Servo_Off` |
| `Buzzer.c` | 蜂鸣器（PWMB 5） | `Buzzer_Play` / `Buzzer_Stop` |
| `Motor.c` | 马达（PWMB 6） | `Motor_Vibrate` / `Motor_Tick` |
| `LED.c` | 8 颗小灯 | `Led_SetSingle` / `Led_SetMask` |
| `NTC.c` / `DHT11.c` | 温度 / 温湿度 | `NTC_GetTempX10` / `DHT11_Read` |
| `Timers.c` | 1ms 系统时基 | `Timers_Init` |
| `I2C_Lock.c` | 总线互斥（当前"保留但永不争用"） | `I2C_Lock` / `Unlock` |

> **Driver 层的一个统一约定**："默认电平危险"的外设都有 `XXX_SafeLevel()`，
> 必须在 `sys_init()` 最开头调（马达、蜂鸣器、舵机、小灯）。

### Lib 层（已裁剪）

保留：`GPIO` / `NVIC`（只剩 7 个函数）/ `Timer` / `STC8H_PWM`（只剩 PWMA/PWMB + PWM3/PWM6）/
`UART`（只开 UART1）/ `I2C` / `ADC` / `EEPROM` / `Exti` / `Delay` / `Config` / `Switch`。

**已废弃并清空**（文件保留、不含函数）：`Soft_I2C.c` / `ADC_Isr.c` / `I2C_Isr.c`。

---

## 十一、关键约定与硬约束（改动前必读）

| # | 约定 | 违反的后果 |
|---|---|---|
| 1 | **源码必须是 GBK，且不能用 GB18030 四字节字符**（`⇒ ⚠ ↔ −` 等） | Keil 显示乱码；若漏进显示字符串，字库地址算错 → 屏上乱码 |
| 2 | **中文串不能进副屏**（I2C） | 副屏字库只有 ASCII + 7 个固定汉字 |
| 3 | **"清屏"和"覆盖整屏"必居其一** | 留残影 |
| 4 | **数值刷新用 `Display_Refresh(0)`** | 传 1 会走 100ms 限流 → 卡顿 + 闪黑 |
| 5 | **不许在热路径放 `printf`** | 拖慢任务、占 Flash |
| 6 | **I2C 只能由 `TASK_RENDER` 碰** | 两任务争总线 → 副屏花屏/黑屏 |
| 7 | **中断里只置标志**，业务留到任务 | 中断里做 I2C 会与副屏抢总线 |
| 8 | **进页面 = 完整初始化，离页面 = 彻底收尾** | 外设留着不关（舵机一直挂在总线上） |
| 9 | **C51 是模块级链接**：工程里没被调用的函数照样占 Flash | 白占空间 |
| 10 | **`char` 默认无符号**；表示 -1/+1 要用 `s8` | `if (x < 0)` 恒为假 |
| 11 | **倒序循环不能用无符号变量** | `for (i=n; i>=0; i--)` 死循环 |
| 12 | **函数/变量名不要用 C51 关键字** | 编译不过 |
| 13 | **`App/` 只 include `App_Public.h`**，不直接碰寄存器 | 破坏分层 |
| 14 | 交付前 `BOOT_CRUMB_ENABLE` 改 0 | 上电时灯会闪进度条 |

---

## 十二、资源占用（2026-09-23 实测）

```
code  :  58424 / 61440 = 95.1%    余  3016 字节   ← 唯一真正吃紧的
xdata :   3822 /  8192 = 46.7%    余  4370 字节
data  :   46.1 /   256 = 18.0%    余  209.9 字节
```

**CODE 占用最大的 10 个模块**：

| 模块 | 字节 | 说明 |
|---|---|---|
| `APP_DISPLAY` | 8767 | 两块屏全部版面（最大） |
| `APP_ICONS` | **7239** | 7 张 128×64 图标。**口径说明**：图标数据本身是 7×1024 = **7168** 字节，m51 里的 7239 = 7168 + `Icon_Get()` 函数（约 71 字节）。占 code 的 12.4% |
| `APP_MENU` | 5282 | 页面栈 + 全部按键处理 |
| `I2C_OLED` | 3836 | 副屏驱动 + 字库 |
| `SPI_OLED` | 2619 | 主屏驱动 |
| `APP_GMPLANE` | 2419 | 飞机大战 |
| `APP_ALARM` | 2198 | 闹钟调度 |
| `APP_UART` | 1908 | 串口协议 |
| `LIB_CODE` | 1814 | C51 库代码 |
| `APP_CLOCK` | 1556 | 时间服务 |

**XDATA 占用前 5**：`SPI_OLED` 1066（屏幕缓冲）、`APP_DISPLAY` 1021、中断/绝对段 333、`UART` 261、`APP_GMSNAKE` 212。

> **余量只有 4.9%**。要加功能前必须先做减法。
> 最值得动的两块：**图标 7239 字节**（非零字节只占 22%~44%，段式压缩可省约 4500），
> 以及 `PRINTF` 869 字节（`printf` 与 `sprintf` 共用同一段，只要还有一处 `sprintf` 就省不掉）。

---

## 十三、改动指引（想改 X，动哪里）

| 想做的事 | 改哪里 | 要注意 |
|---|---|---|
| 加/改一个任务页 | `App_Menu.h` 的 `MENU_TASK_COUNT` + `Menu_TaskName/TaskPage` + `App_Display.c` 的 `main_draw_*` / `sub_draw_*` + `Menu_OnEvent` 加分支 | 4 处要一起改 |
| 改某页的按键 | `Menu_OnEvent` 里对应的 `case` | 注意 `key_remap()` 的置换（代码里的 `EVT_KEY3` 对应板上 K1）；**响铃页不走置换** |
| 改屏上内容 | `App_Display.c` 的对应 `main_draw_*` / `sub_draw_*` | 主屏必须补满 128px、副屏补满 16 字符 |
| 改引脚 | 对应驱动 `.h` 的 `XXX_GPIO_INIT()` / 引脚宏 | 一处改全跟着变 |
| 加一个设置项 | `Settings_t`（`App_Public.h`）+ `SET_ITEM_COUNT` + `App_Display.c` 的设置版面 + `App_Storage.c` 的默认值 | **`SETTINGS_VERSION` 要 +1**，否则旧 EEPROM 数据不兼容 |
| 改闹钟组数 | `ALARM_MAX`（`App_Public.h`） | 数据层/存储/协议都按这个宏走；`SETTINGS_VERSION` 要 +1 |
| 加一个游戏 | `App/Game/` 新增文件 + `App_Menu.c` 的 `GAME_COUNT`/`game_frame`/`game_input`/`game_is_over` | 注意 Flash 余量只剩 4.9% |
| 改任务周期 | 各 `task_xxx` 里的 `os_wait2(K_TMO, n)` | n × 5ms；**改节拍要连带检查所有"数拍数"的逻辑** |
| 省 Flash | 见 §12 | C51 是模块级链接，优先"删没用到的" |

---

## 附：与三份文档的关系

| 文档 | 面向谁 | 回答什么问题 |
|---|---|---|
| **本文（整体框架）** | 要**理解工程怎么搭起来**的人 | 分层、任务、数据流、资源分配 |
| `使用说明.md` | **拿到板子的人** | 怎么接线、怎么烧录、每个键干什么、出问题怎么查 |
| `交付说明.md` | **验收的人** | 做过什么、验过什么、还有什么没做 |
| `硬件真值表.md` | 要**改硬件/查引脚**的人 | 每个引脚、每处引用的出处与冲突清单 |
| `02-技术方案.md` | 要**看设计取舍**的人 | 当初怎么定的、哪些被实现推翻了 |
