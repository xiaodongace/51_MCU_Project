# 真机联调记录 · 第 1 轮

## 一、你报的现象

| # | 现象 |
|---|---|
| 1 | SPI 主屏没有亮起来 |
| 2 | 数码管一个都没有亮 |
| 3 | I2C 副屏有画面，但按任何按键都没反馈，画面也不动 |

## 二、已确认的根因（静态可证）

### RC-1 · 数码管不亮 —— `Nixie_ScanInit()` 从未被调用 ✅ 已修

`grep` 全工程只找到定义处，`App/`、`User/` 里零调用。后果：
- `Timer2` 从未配置 → 1kHz 扫描中断不存在
- `NIXIE_init()` 从未执行 → P4.2/P4.3/P4.4 没配成推挽
- 扫描缓冲区全是"空白" → 8 位全灭

**修法**：在 `App_System.c` 的 `sys_init()` 第 5 步调用 `Nixie_ScanInit()`，
紧接 `Nixie_SetTime(0,0,0)` 让屏上先出现 `00-00-00`，便于判断"走到了哪一步"。

### RC-2 · `Lib/I2C.c` 的 `Wait()` 是无超时死等 ✅ 已修

原厂原文（就是你那份规范第一节点名的那句）：

```c
void Wait()
{
    while (!(I2CMSST & 0x40));   // 没有超时
    I2CMSST &= ~0x40;
}
```

只要 I2C 主机命令因为任何原因（从机拉住 SCL、总线被复位打断、电压跌落）
没有完成，这个循环就永远出不来。

**修法**：加计数上限 10000（约几十毫秒），超时强制收尾放行，
由上层自己的合法性检查拦（`Clock_Refresh()` 本来就会返回错误码）。

### RC-3 · `Clock_Init()` 放在 `sys_init()` 里，是"整机假死"的高危结构 ✅ 已改

原调用序：

```
GPIO/UART/I2C/ADC 配置 → Buzzer_Init → Motor_Init → Storage_Load → Clock_Init → EA=1
                                                                     ↑
                                                        这里会走 I2C 读 PCF8563
```

`Clock_Init()` 卡住 → `sys_init()` 永不返回 → **`os_create_task()` 一句都没执行**
→ 一个任务都建不出来 → 三块屏全无反应、按键全无响应。

**这一条能一次性解释你报的全部三个现象**，所以我把它作为首要嫌疑。

**修法**：`Clock_Init()` 移到 `TASK_LOGIC` 里（任务起来之后）。
最坏情况变成"时间读不到，界面显示占位值"，系统照常活着。

### RC-4 · RTC 的 I2C 访问没加总线锁 ✅ 已修

`Clock_Refresh()` / `Clock_Set()` / `Alarm_ApplyHardware()` / `Alarm_OnRtcIrq()`
直接调 `PCF8563_*`，而副屏刷屏也走同一条 I2C。
两个任务同时在同一个 I2C 控制器上发命令，会把控制器状态机搅乱 → 配合 RC-2 就是死锁。

**修法**：这 4 处全部包上 `I2C_Lock()/I2C_Unlock()`。

### RC-5 · 小灯总开关从未打开 ✅ 已修

`Led_Power(1)` 全工程无人调用 → `LED_SW` 一直是 1（关）→ 日出唤醒的 8 颗灯永远不会亮。

**修法**：`sys_init()` 里打开（诊断进度条要用），`Alarm_Init()` 里保证是开的。

### RC-6 · 副屏每秒整屏重画，长时间独占总线 ✅ 已优化

`Display_Poll()` 每秒调 `sub_full_redraw()` = 4 行 × 256 字节 I2C ≈ **80~90ms**，
期间总线锁一直被占，`TASK_LOGIC` 读时间要排队。

**修法**：每秒只重画"时间那一行"（约 20ms），整屏重画只留在切页面时。

## 三、为了不再盲猜，加了上电进度指示（BOOT_CRUMB）

真机上"屏全黑"这类现象无法判断程序跑到哪一步。所以这次加了**用 8 颗小灯当进度条**：

| 走到的位置 | 亮几颗灯 |
|---|---|
| 进入 `sys_init`，安全电平 + 小灯配好 | 1 |
| `EAXSFR()` 完成 | 2 |
| GPIO / UART / I2C / ADC 配置完成 | 3 |
| 蜂鸣器 + 马达 PWM 配置完成 | 4 |
| **数码管扫描（Timer2 1kHz）启动完成** | 5 |
| EEPROM 装载完成 | 6 |
| `EA = 1` 完成 | 7 |
| `sys_init` 全部走完（准备创建任务） | 8（全亮） |
| 五个任务都起来了（`TASK_LOGIC` 初始化完成） | 只有第 1 颗亮 |

设计上满足规范第四节的硬要求：**它直接写 IO 口，不经过任何任务、不依赖任何外设初始化、
甚至不依赖 RTX51 跑起来**。只要 CPU 还在执行指令，灯就会变。

正式固件里把 `App/App_Public.h` 的 `#define BOOT_CRUMB_ENABLE 1` 改成 `0` 就全部去掉。

## 四、请按这个表回报（这一轮的信息量最大）

**上电后立刻观察，然后把这 5 项告诉我：**

| # | 看什么 | 怎么记 |
|---|---|---|
| 1 | **8 颗小灯亮了几颗**（数颗数，不用管方向） | 例如"亮了 5 颗" |
| 2 | **数码管有没有显示 `00-00-00`** | 有 / 没有 |
| 3 | **SPI 主屏有没有任何内容** | 有（写了什么）/ 依然是黑的 |
| 4 | **I2C 副屏上显示的文字，逐字念给我** | ⚠️ **最关键** —— 见下面说明 |
| 5 | 串口 115200，有没有打印 `51_MCU_Alarm build 1.0.0` 和 `sys_init done` | 有 / 没有 |

第 4 项为什么最关键：

- 如果副屏写的是 **`SUB SCREEN OK`** → 说明 `Display_Init()` 跑过了
  → 任务确实起来了 → 那主屏黑就是 SPI 自己的问题，方向完全不同；
- 如果是 **`CLOCK` / 时间 / 日期** → 说明 `Display_Poll()` 也跑通了 → 系统基本是好的；
- 如果是 **一堆随机亮点/花屏** → 说明我们从来没写过副屏 → 印证"任务没建出来"
  （SSD1306 上电时显存是随机的）。

## 五、这一轮改动的文件

| 文件 | 改了什么 |
|---|---|
| `App/App_Public.h` | 新增 `BOOT_CRUMB` 宏与 `Boot_Crumb()` 声明 |
| `App/App_System.c` | 新增 `Boot_Crumb()` 实现；`sys_init()` 全线加进度标记；**补上 `Nixie_ScanInit()`**；`Clock_Init()` 移出；开头加 `Led_Init()+Led_Power(1)` |
| `App/App_Menu.c` | `TASK_LOGIC` 里调用 `Clock_Init()`；初始化完成后收掉进度条、留第 1 颗灯 |
| `App/App_Clock.c` | `Clock_Set()` / `Clock_Refresh()` 包 `I2C_Lock` |
| `App/App_Alarm.c` | `Alarm_ApplyHardware()` / `Alarm_OnRtcIrq()` 包 `I2C_Lock`；`Alarm_Init()` 改用 `Led_Power(1)`，不再 `Led_Init()`（免得清掉进度条、关掉总开关） |
| `App/App_Display.c` | 副屏每秒只重画时间那一行，不再整屏重画 |
| `Lib/I2C.c` | `Wait()` 加超时 |

**重新编译验证：`0 Error(s), 0 Warning(s)`，`UNRESOLVED = 0`，浮点库引用 = 0，`code = 38549`。**

## 六、我这次没做的事（避免掩盖问题）

- **没有**把 `Display_Init()` 往前提、**没有**加"轮询兜底"之类的东西。
  现在的目标是先确定卡在哪一步，一次 flash 拿到答案，而不是堆补丁。
- 如果这一轮的现象变化了（比如数码管亮了 `00-00-00`），那本身就是证据：
  **数码管亮 = CPU 在跑 + Timer2 中断正常 + `sys_init` 至少走到第 5 步**。
