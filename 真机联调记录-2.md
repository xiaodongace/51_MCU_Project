# 真机联调记录 · 第 2 轮

## 一、你报的数据说明了什么

| 你的观察 | 它证明了什么 |
|---|---|
| 小灯只剩第 1 颗 | `sys_init` **全部走完**，五个任务**全部创建成功** |
| 数码管 `00-00-00` | Timer2 1kHz 中断正常，`NIXIE_init` 正常，**CPU 在跑** |
| 串口 `sys_init done` / `[ALARM] init done` / `[LOGIC] task ready, clock valid=1` | `sys_init` 与 TASK_LOGIC 都跑到位；**PCF8563 的 I2C 读是通的**（`clock valid=1`） |
| `[SENSOR] DHT11 fail x5` 出现在开机后 5 秒 | TASK_SENSOR 在跑；RTX51 的 5ms 心跳（Timer0）正常 |
| 副屏 4 行内容齐全、时间 23:24:45 | 显示链路 + 布局全对 |

**结论：我上一轮把 RC-3（`sys_init` 卡死）当首要嫌疑是错的。系统是活的。**

---

## 二、按键无反馈的真正根因（RC-7）—— Timer3 从未被初始化

### 证据

```
grep 全工程 "Timer_Inilize|NVIC_Timer3_Init"：
  Driver/NixieScan.c:35:  Timer_Inilize(Timer2, &TIM_InitStructure);
  Driver/NixieScan.c:37:  NVIC_Timer2_Init(ENABLE, Priority_0);
  —— 只有 Timer2，没有 Timer3

grep 全工程 "g_sysTick" 的写入点：
  Lib/Timer_Isr.c:71:  g_sysTick++;      ← Timer3 中断服务函数里
  —— 而这个中断永远不会触发
```

### 后果链（一条根因解释一串现象）

```
Timer3 没配 → 中断不产生 → g_sysTick 恒为 0 → SysTick_Elapsed() 恒为 0
   ├─ Keys_Scan():  (now - t0) >= 20ms  永远不成立 → 按键永远不产生事件   ← 症状 6
   ├─ TASK_LOGIC:   (now - t0) >= 1000ms 永远不成立 → 每秒逻辑从不执行
   │     → Clock_Refresh() 只在 Clock_Init() 那一次跑过
   │     → 副屏时间冻结在 23:24:45（正是开机时刻）        ← 你截图里的时间
   │     → Alarm_Tick1s() / Alarm_TickSunrise() 从不执行 → 闹钟永远不会响
   └─ Music_Tick(): 音符时长判断永远不成立 → 曲子卡在第一个音
```

### 为什么漏了

`Driver/Timers.c` 是**你自己写的那份**（在 `51_MCU_Project` 里），
它以 `Timer_Inilize(Timer3, ...)` + `NVIC_Timer3_Init(ENABLE, Priority_0)` 配置 1ms 节拍。
我以 day17 v3.1 为基座搬 Lib/Driver 时，v3.1 里**没有**这个文件（它是你后加的），于是整个
1ms 时钟的初始化就丢了。这是我的疏漏。

### 修法

新增 `Driver/Timers.h` / `Driver/Timers.c`，函数体**逐字照抄你现有工程的写法**
（那份已经在这块板上验证过），只把 `system_ms` 换成全局 `g_sysTick`。
在 `sys_init()` 的第 2 步（紧接 `EAXSFR()`）调用 —— 后面所有"看钟算差值"的逻辑都依赖它。

验证：`.m51` 里已出现 `?PR?TIMER3_ISR_HANDLER?TIMER_ISR` 与 `_NVIC_Timer3_Init`。
另外把数码管的显示内容也接上了（`Menu_Tick1s` 里 `Nixie_SetTime(...)`），
之前它只在启动时被设过一次 `00-00-00`，所以永远显示 00-00-00。

---

## 三、SPI 主屏不亮 —— 已改，但仍是**待验证的假设**

### 我的判断

这 6 根线是**软件模拟 SPI**（IO 口一位一位翻转），不是硬件 SPI 外设。

原厂驱动和 v3.1 **都没有配置它们的端口模式**，也就是一直停在复位默认的**准双向口**：
输出高电平靠内部弱上拉（几十 kΩ）。对按键那种慢信号够用，但对 SCL 时钟线不行 ——
本驱动的 SCL 由 `for` 循环翻转产生，周期只有几百纳秒（约 1~3MHz），
弱上拉遇上线路电容，**上升沿爬不到高位门限**，SSD1306 就收不到有效时序。

对照：I2C 那两条线（P3.2/P3.3）被显式配成了开漏（外部有上拉），所以副屏正常。
蜂鸣器 P0.0、马达 P0.1 也被显式配成推挽。**唯独屏的 6 根线没人管。**

### 改法

在 `SPI_OLED_Init()` 最前面加端口模式配置：

```c
P5_MODE_OUT_PP(GPIO_Pin_0);                            /* SCLK */
P1_MODE_OUT_PP(GPIO_Pin_0 | GPIO_Pin_3 | GPIO_Pin_6);  /* 字库CS / MOSI / DC */
P4_MODE_OUT_PP(GPIO_Pin_7);                            /* 屏片选 */
```

留 `P1.1 = MISO` 不动（它是从字库 IC 读数据的输入线）。

### 同时加了一个能自证通路的诊断

`Display_Init()` 里初始化完屏之后，**从屏上那块晶联讯字库 IC 按地址读回 ASCII '8' 的 16 字节点阵**
并打印到串口：

```
[SPI] ROM readback ('8'): xx xx xx xx xx xx xx xx xx xx xx xx xx xx xx xx
[SPI] main screen init done
```

判读：

| 现象 | 含义 |
|---|---|
| 打印出 16 个像点阵的字节（不是全 00、也不是全 FF） | **SPI 的 SCL/MOSI/MISO/片选全部通了** → 主屏黑就只剩"屏本身/供电/对比度"这一类可能 |
| 全 `00` 或全 `FF` | SPI 通路本身没建立起来 → 接着查引脚模式/线序 |
| 串口没有这两行 | `Display_Init()` 根本没跑到 → 那就是 TASK_RENDER 的问题 |

这一步只读不写屏，不影响任何显示逻辑。

> **我不打算假装这条一定对。** v3.1 用准双向口也"应该"跑通过 SPI 菜单，
> 所以它只是目前最合理的一条假设。上面那个字库读回就是用来一次判定的。

---

## 四、顺手修掉的 DHT11 时序问题

你的日志：`[SENSOR] DHT11 fail x5, err=252`（252 = -4 = `DHT11_ERR_RESP_LOW`），
说明响应低电平时长的实测计数不在照抄来的窗口 `[70, 88]` 里。

根因：v3.1 的 `delay_1us()` 是 `NOP8()`（8 个机器周期 = 0.33us @24MHz），
但一个循环还有函数调用、`u16` 自增、位测试的开销，实际约 1.2~1.3us。
**"计数"和"微秒"之间存在一个未知换算系数**，照抄以微秒写死的窗口必然偏 ——
按 1.25us 估算，83us 的响应低电平只会数到约 66，低于下限 70，于是报 -4。

改法（`Driver/DHT11.c`）：

1. 响应阶段只做宽松合理性检查（挡住"没接传感器"），窗口从 `[70,88]` 放宽到 `[10,250]`；
2. **真正的 0/1 区分改成"比高低电平长度"**：每位先量低电平计数 `lowCnt`（固定约 54us），
   再量高电平计数 `highCnt`（bit0 约 24us、bit1 约 71us），
   `highCnt > lowCnt` 判为 1。24/54 = 0.44 与 71/54 = 1.31 分居 1.0 两侧，余量足够，
   **且完全不需要知道一次计数等于多少微秒**。

---

## 五、这一轮的改动清单

| 文件 | 改动 |
|---|---|
| **`Driver/Timers.h` / `Driver/Timers.c`** | **新增。Timer3 1ms 系统时钟初始化（照抄你现有工程的写法）** |
| `App/App_System.c` | 第 2 步加 `Timers_Init()` |
| `Driver/SPI_OLED/spi_oled.c` | `SPI_OLED_Init()` 开头加 6 根线的端口模式配置 |
| `App/App_Display.c` | 加"字库 IC 读回"诊断 + `[SPI] main screen init done` 打印 |
| `App/App_Menu.c` | 每秒把时间写进数码管；旋钮事件后主动请求重画；按键事件点亮第 2 颗灯 + 打印 `[EVT]` |
| `Driver/DHT11.c` | 时序判据改为"比高低电平长度"的相对判据 |
| `Project.uvproj` | 注册 `Driver/Timers.c`（现 73 个文件） |

**重新编译：`0 Error(s), 0 Warning(s)`，`UNRESOLVED = 0`，浮点库引用 = 0，`code = 38955`。**
全工程 109 个源文件编码一致性校验通过（统一 GB18030，无混编码）。

---

## 六、这一轮请重点观察（预期现象）

| # | 观察项 | 预期（如果我的判断对） |
|---|---|---|
| 1 | **按键** | 按任意独立键 → **第 2 颗灯翻转**，且串口打印 `[EVT] type=1 page=0` 之类；主界面按 KEY1 副屏标题变 `ALARM LIST` |
| 2 | **副屏时间** | **开始逐秒走动**（不再是冻结的 23:24:45） |
| 3 | **数码管** | 显示**当前时间**而不是 00-00-00 |
| 4 | **串口新增两行** | `[SPI] ROM readback ('8'): ...` 与 `[SPI] main screen init done` |
| 5 | **SPI 主屏** | 有内容（若仍黑，把上面那行 readback 的 16 个字节原样发我） |
| 6 | 温湿度 | 副屏/主屏的湿度不再是 `--` |

第 4 项那 16 个字节这一轮最关键 —— 它直接决定主屏问题往哪个方向查。
