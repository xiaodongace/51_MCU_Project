# 硬件真值表（动手前门禁 · 每项引用例程原文）

> 生成时间：2026-09-17
> 唯一真值来源（按优先级）：
> 1. `D:\TooL\Share_SyncHome\嵌入式3阶段\day17\Code\02_STC8_扩展板自检程序v3.1_基于RTX51`（下称 **v3.1**，真机跑通的自检固件）
> 2. `D:\TooL\Share_SyncHome\嵌入式4阶段\stc8上课代码\自己封装的驱动`（下称 **JC**，江文聪老师封装驱动）
> 3. `D:\Develop\Keil_v5\材料\STC8G-STC8H-LIB-DEMO-CODE_优化`（下称 **官方优化版**）
> 4. `D:\TooL\Share_SyncHome\嵌入式3阶段\day11\Code\05_0.96OLED显示屏_SPI带字库_移植`（下称 **day11**）
>
> 凡本文与上述来源不一致处，一律以来源为准并在第 7 节列出。
> 表格中“未验证”字样表示我没有拿到可直接引用的原文，需你确认——**不猜、不凭记忆填写**。

---

## 1. 引脚与极性

### 1.1 主屏 SPI OLED（带硬件字库）

| 项 | 真值 | 出处（原文） |
|---|---|---|
| SCLK | P5.0 | day11 `Driver/OLED/oled.h`：`sbit OLED_SCL=P5^0;   // SCLK` |
| MOSI | P1.3 | 同上：`sbit OLED_SDA=P1^3;   // MOSI` |
| DC | P1.6 | 同上：`sbit OLED_DC=P1^6;` |
| CS | P4.7，**低有效** | 同上：`sbit OLED_CS=P4^7;`；v3.1 `spi_oled.h`：`// 屏幕片选线 (拉低有效)` |
| MISO | P1.1 | 同上：`sbit OLED_READ_FS0=P1^1; // MISO` |
| 字库 ROM CS | P1.0，**低有效** | 同上：`sbit OLED_ROM_CS=P1^0;`；v3.1：`// 字库片选线 (拉低有效)` |
| 通信方式 | 软件模拟 SPI（6 根线全为普通 IO） | 《02-技术方案》2.1 |

### 1.2 副屏 I2C OLED

| 项 | 真值 | 出处 |
|---|---|---|
| SCL | P3.2 | v3.1 `Driver/I2C_OLED/oled.h`：`sbit OLED_SCL = P3^2;//SCL` |
| SDA | P3.3 | 同上：`sbit OLED_SDA = P3^3;//SDA` |
| 结构 | **无硬件字库**，只能 ASCII + 固定索引汉字 | 见第 6 节 |

### 1.3 时钟芯片 PCF8563

| 项 | 真值 | 出处（原文） |
|---|---|---|
| I2C SCL/SDA | P3.2 / P3.3（**与副屏共用同一对线**） | v3.1 `PCF8563.c`：`P3_MODE_OUT_OD(GPIO_Pin_2 \| GPIO_Pin_3);` 注释 `// I2C   P32  P33 开漏输出   有外部上拉`（JC 版原文） |
| 中断脚 | P3.7，**准双向口** | v3.1 `PCF8563.c`：`GPIO_InitStructure.Pin = GPIO_Pin_7; … Mode = GPIO_PullUp;` |
| 中断源 | 外部中断 3（INT3） | v3.1 `PCF8563.c`：`Ext_Inilize(EXT_INT3,&Exti_InitStructure);` |
| 触发沿 | **下降沿** | v3.1：`Exti_InitStructure.EXTI_Mode = EXT_MODE_Fall;`（JC 版为 `EXT_MODE_RiseFall`，见第 7 节） |
| 设备写地址 | 0xA2 | v3.1：`#define PCF8563_ADDR (0x51 << 1)  // 设备写地址 0xA2 == 0x51 << 1` |
| 时间寄存器首址 | 0x02 | v3.1：`#define PCF8563_REG (0x02) // 寄存器地址: 秒钟寄存器开始读` |
| 闹钟寄存器 | 0x09 分 / 0x0A 时 / 0x0B 日 / 0x0C 周 | v3.1 `PCF8563.c` 注释：`3. 设置闹铃时间: 09h分钟, 0Ah小时, 0BH天, 0CH周(最高配置0: 启用)` |
| 控制寄存器 CS2 | 0x01 | v3.1 注释：`4. 配置控制寄存器2, CS2 , AF=0, AIE=1启用闹铃` |
| 定时器频率/计数 | 0x0E / 0x0F | v3.1 `PCF8563_set_timer()`：`I2C_WRITE(PCF8563_ADDR, 0x0E, &p, 1);` / `0x0F` |
| BCD 转换 | 读需转十进制的“压缩版十进制” | JC `PCF8563.h`：`#define WRITE_BCD(val) (val % 10) + ((val / 10) << 4)` / `#define READ_BCD(val) (val & 0x0f) + ((val>> 4) * 10)` |
| 定时器频率禁用项 | **不要用 HZ1**，实测不稳定 | JC `PCF8563.h`：`// 国产芯片的HZ1可能有问题，不要使用，建议使用HZ64` |
| 世纪位 | 月寄存器第 7 位：0→20xx，1→21xx | JC `PCF8563.c` `PCF8563_get_clock()` 注释 |

### 1.4 数码管（74HC595 移位寄存器）

| 项 | 真值 | 出处 |
|---|---|---|
| 数据输入 DI | P4.4 | JC `Nixie.h`：`#define NIX_DI P44 // 数据输入` |
| 移位时钟 SCK | P4.2 | JC：`#define NIX_SCK P42 // 移位寄存器` |
| 锁存 RCK | P4.3 | JC：`#define NIX_RCK P43 // 锁存寄存器` |
| 输出模式 | 推挽 | 同上（JC 版加注释 `// 推挽  驱动能力强`） |
| 段码极性 | **低有效**（0 亮） | JC `Nixie.c` 码表 `0xC0,0xF9,…` 为共阳段码 |
| 位选极性 | **高有效**（1 亮） | JC `Nixie.c` `NIXIE_show()` 注释：`b_idx 哪几个位显示(1是点亮, 0是熄灭)` |
| 位号 | `NIXIE_display(num_id, pos)`，pos ∈ [0,7] | v3.1 `NIXIE.h`：`@param pos 显示的位置 [0, 7]` |
| 显示顺序 | 先发段码，再发位码，最后锁存 | JC `Nixie.c` `Nixie_show()`：先 `NIXIE_out(num)` 再 `NIXIE_out(idx)` 再 `RCK_ACTION()` |

### 1.5 矩阵键盘（4×4=16 键）

| 项 | 真值 | 出处 |
|---|---|---|
| 列 COL1..COL4 | P0.3 / P0.6 / P0.7 / P1.7 | v3.1 `MatrixKey.h`：`#define COL1 P03` … `#define COL4 P17` |
| 行 ROW1..ROW4 | P3.4 / P3.5 / P4.0 / P4.1 | 同上：`#define ROW1 P34` … `#define ROW4 P41` |
| 引脚模式 | 全部**准双向口** | 同上 `MK_GPIO_INIT()`：`P0_MODE_IO_PU(…); P1_MODE_IO_PU(…); P3_MODE_IO_PU(…); P4_MODE_IO_PU(…)` |
| 扫描方法 | 逐行拉低，读列 | v3.1 `MatrixKey.c`：`ROW_OUT(row)` + `COL_IN(col)`，`ROW_OUT` 里 `ROW1 = (row == 0) ? 0 : 1;` |
| 稳定等待 | 拉低行后需 `NOP2()` | v3.1 注释：`等待 GPIO 引脚信号稳定` |
| 按键编号 | `pos = row*4 + col`，0..15 | v3.1：`pos = (row * COL_NUM) + col;` |
| 电平语义 | 按下=0（低），抬起=1（高） | v3.1：`#define DOWN 0` / `#define UP 1` |

### 1.6 独立按键（4 个）

| 项 | 真值 | 出处 |
|---|---|---|
| KEY1..KEY4 | P5.1 / P5.2 / P5.3 / P5.4 | v3.1 `Keys.h`：`#define KEY1 P51` … `#define KEY4 P54` |
| 模式 | 准双向口 | 同上 `KEYS_GPIO_INIT() P5_MODE_IO_PU(GPIO_Pin_1 \| … \| GPIO_Pin_4)` |
| 电平语义 | 按下=0，抬起=1 | JC `Keys.c`：`// 按下是低，抬起是高` `#define DOWN 0` / `#define UP 1` |
| P5.4 兼复位脚 | 已验证可正常当按键 | 《02-技术方案》第八节 |

### 1.7 小灯 ×8 + 总开关

| 项 | 真值 | 出处 |
|---|---|---|
| 总开关 | P4.5，**低电平导通（打开）** | v3.1 `App_LED.c`：`#define LED_SW P45`；注释 `// 打开总开关： 拉低P45`，`LED_SW = 0;` |
| LED1..LED8 | P2.7 / P2.6 / P1.5 / P1.4 / P2.3 / P2.2 / P2.1 / P2.0 | v3.1 `App_LED.c`：`#define LED1 P27` `#define LED2 P26` `#define LED3 P15` `#define LED4 P14` `#define LED5 P23` `#define LED6 P22` `#define LED7 P21` `#define LED8 P20` |
| 点亮极性 | **低电平点亮** | v3.1：`LED1 = LED2 = … = LED8 = 1;` 注释 `// 全部熄灭` |
| 输出模式 | 推挽 | v3.1 `GPIO_config()`：`P1_MODE_OUT_PP` / `P2_MODE_OUT_PP` / `P4.5 OUT_PP` |
| ⚠ 与文档差异 | 《02-技术方案》写作 `P2.7 P1.5 P1.4 P2.2 P2.6 P2.1 P2.0 P2.3` | 引脚集合相同，**编号顺序不同**；本文采用 v3.1（真机验证）的顺序，见第 7 节 |

### 1.8 蜂鸣器

| 项 | 真值 | 出处 |
|---|---|---|
| 引脚 | P0.0 | JC `Buzzer.c`：`// P00 蜂鸣器` `P0_MODE_OUT_PP(GPIO_Pin_0);` |
| 通道切换 | `PWM5_SW(PWM5_SW_P00)` | JC `Buzzer.c`：`// 切换PWM5选择` `PWM5_SW(PWM5_SW_P00);` |
| PWM 组 | **PWMB（B 组）** | 同上：`PWM_Configuration(PWMB, …)` |
| 周期 | `period = (MAIN_Fosc / hz)`，写入 `PWM_Period = period - 1` | 同上 |
| 占空比 | `0.5 * period`（50%，最响） | 同上 |
| 停止方式 | `PWM_EnoSelect = hz != 0 ? ENO5P : 0`（通道不选=不发声） | 同上：`// hz不为0, 选择PWM5通道，否则， 通道为0，不选择，蜂鸣不工作` |
| 初始安全电平 | `P00 = 0;` | JC：`P00 = 0; // 默认不工作，低电平` |
| 频率下限 | ≥ 367 Hz | JC：`// 周期  频率 >= 367 即可` |
| PWM 中断 | `NVIC_PWM_Init(PWMB, DISABLE, Priority_0)` | 同上 |

### 1.9 震动马达（M2 功能，但引脚需安全初始化）

| 项 | 真值 | 出处 |
|---|---|---|
| 引脚 | P0.1 | 现有工程 `App.c`：`PWM6_SW(PWM6_SW_P01);` |
| PWM 通道 | PWM6，**与蜂鸣器同属 PWMB 组 → 共用频率** | v3.1 `App_Motor.c`：`ENO6P` + `PWM_Configuration(PWMB, …)`；《02-技术方案》3.2 |
| 频率 | `PREQ = 1000` → `PERIOD = MAIN_Fosc/1000` = 1 kHz | v3.1 `App_Motor.c`：`#define PREQ 1000` |
| 不工作电平 | 占空比 0（引脚低），**高电平震动 → 上电默认状态危险** | v3.1 `App_Motor.c`：`PWMx_InitStructure.PWM_Duty = 0;`；《AI项目生成规范》示例行：“马达 `P0.1`” / “`P01=0 不工作（高电平震）`” |
| ⚠ 结论 | **必须在 sys_init 最前面显式 `P01 = 0`**，不能等 PWM_config 才置低 | 见第 5 节 |

### 1.10 模拟量

| 外设 | 引脚 | ADC 通道 | 出处 |
|---|---|---|---|
| 旋钮（电位器） | P0.5 | `ADC_CH13` | v3.1 `App_Motor.c`：`// 读取13通道的结果（P05）` `Get_ADCResult(ADC_CH13);` |
| NTC 热敏电阻 | P0.4 | `ADC_CH12` | v3.1 `NTC.h`：`#define NTC_ADC_CH ADC_CH12` + `#define NTC_GPIO_PIN GPIO_Pin_4` |
| DHT11 | P4.6，**准双向口** | — | v3.1 `DHT11.c`（JC 版原文）：`info.Mode = GPIO_PullUp; // 准双向口`；`info.Pin = GPIO_Pin_6;` |

### 1.11 串口与预留针脚

| 项 | 真值 | 出处 |
|---|---|---|
| UART1 | P3.0 RxD / P3.1 TxD | v3.1 `App_System.c`：`UART1_SW(UART1_SW_P30_P31);` |
| 波特率 | 115200，`UART_8bit_BRTx`，`BRT_Timer1`，`BaudRateDouble = DISABLE` | v3.1 `UART_config()` |
| 超声波 TRIG / ECHO | P2.4 / P3.6 | 《02-技术方案》2.2 |
| 舵机 | P2.5 | 同上 |
| 空闲备份 | P0.2 | 同上 |


### 1.12 超声波（"测距仪"任务 · 2026-09-19 新增）

| 项 | 真值 | 出处（原文） |
|---|---|---|
| TRIG | **P2.4** | 《02-技术方案》56 行：`| 超声波·触发 | P2.4 | 输出一个脉冲"喊一声" |` |
| ECHO | **P3.6** | 同上 57 行：`| 超声波·回波 | P3.6 | 接收"回声"，靠时间算距离 |` |
| TRIG 模式 | **推挽输出**，初始化后拉低 | 参考代码 `Driver/Ultrasonic.c`：`P4_MODE_OUT_PP(GPIO_Pin_7); TRIG = 0;` |
| ECHO 模式 | **高阻输入** | 同上：`P3_MODE_IN_HIZ(GPIO_Pin_3);` |
| 测距算法 | `distance = cnt * 0.01 * 34 / 2`（cnt = 高电平的 10µs 计数）⇒ 每 cnt 合 **0.17 cm** | 同上函数尾注释 |
| 超时保护 | 等 ECHO 变高超过 500 次(5ms) → 返回 −1；高电平超过 3000 次(30ms) → 返回 −2；距离不在 2~400cm → 返回 −3 | 同上：`while(ECHO == 0 && cnt < 500)` / `while(ECHO == 1 && cnt < 3000)` |
| 延时基准 | `Delay10us()` = `i=78; while(--i);` @24MHz | 同上 |
| 中断 / 定时器 | **都不用**，纯轮询计时 | 《02》239 行：`超声波能不能用中断测 → ⚠️ 不行（它只支持下降沿触发），改用"轮询计时"的办法` |
| 与 Timer2 的关系 | Timer2 **已被数码管 1kHz 扫描占用**（`Driver/NixieScan.c`），不能给超声波用；超声波用轮询，正好不冲突 | 《02》101 行原计划"Timer2 留着备用给超声波"，已被后来的数码管方案取代 |
| ⚠ 参考工程引脚 | TRIG=P4.7 / ECHO=P3.3 | **不能照抄**：本板 P4.7 是**主屏片选**、P3.3 是 **I2C 的 SDA** |

**⇒ 关于"参考工程在另一块扩展板上"（2026-09-19 核实）**

用户指的两个目录（`嵌入式4阶段/01_小车{常规功能、超声波}`、`02_STC8{超声波、电机驱动、循迹}`）
里面放的是**资料**（原理图 PDF、HC-SR04 说明书、课堂笔记），**没有源码**。
真正可编译的那份在 `嵌入式4阶段/stc8上课代码/05_小车/05_小车_超声波/`。

该工程自带的 `项目开发文档-STC8H.xlsx` 的 sheet3 原文写着：

```
超声波 | P3.3 | ECHO | 读取超声波 | GPIO IN
       | P4.7 | TRIG | 发送超声波 | GPIO OUT
```

⇒ **那是小车扩展板（天空星）的接线表，不是我们这块板。**
我们这块板按《02-技术方案》用 **P2.4 / P3.6**（《02》第 56/57 行）。
**两张表都对，只是板子不同 —— 引用前必须说清是哪一块板。**

### 1.13 舵机（"云台"任务 · 2026-09-19 新增）

| 项 | 真值 | 出处（原文） |
|---|---|---|
| 引脚 | **P2.5** | 《02-技术方案》58 行：`| 舵机 | P2.5 | 硬件 PWM 输出 |` |
| PWM 通道 | **PWM3N**（`PWM3_USE_P24P25()` → PWM3P=P2.4、PWM3N=P2.5） | `Lib/STC8H_PWM.h:67` |
| PWM 组 | **PWMA（A 组）**，独占 50Hz | 《02》82 行：`舵机在 A 组，独占 50Hz，不受影响——查过 A 组没有其他 PWM 用户。` |
| 与蜂鸣器/马达的关系 | **互不影响**：蜂鸣器 PWM5、马达 PWM6 都属 **PWMB** 组（共用周期寄存器，约 1kHz）；舵机在 PWMA，两组的 ARR/PSCR 是独立寄存器 | `Lib/STC8H_PWM.h:75~88`（PWM5/6/7/8 均为 PWMB_PS 系列） |
| 频率 / 周期 | 50 Hz；`PRESCALER=10`、`PERIOD = MAIN_Fosc/10/50 = 48000` | 参考 `Servo.c`：`#define PRESCALER 10`、`#define FREQ 50`，注释 `确保分母 PRESCALER * FREQ >= 367 即可以正确输出` |
| 角度 → 脉宽 | `duty_us = 500 + angle * 2000 / 180`，范围 **500~2500µs** | 同上 `Serve_set_angle()` |
| 占空比寄存器值 | `PWMx_Duty = PERIOD * duty_us / 20000`（20ms = 20000µs） | 同上 |
| float | 参考实现用 `float`，**本工程必须改整数** | 规范 §三.1：float 会链入整个 `C51FPL.LIB` |
| ⚠ 参考工程引脚/通道 | P0.2 / **PWM7（PWMB）** | **不能照抄**（理由见下） |

#### ⇒ 结论：用 P2.5（PWM3N / PWMA），**不要**用参考工程的 PWM7 / P0.2

用户问"如果不同时使用是否可以照抄 PWM7"。**答案是：不行，因为"不同时使用"这个前提在本工程不成立。**

理由（逐条可查）：

1. **PWM7 属 PWMB 组。** `Lib/STC8H_PWM.h:75~88` 里 PWM5/6/7/8 的全部引脚切换宏都是
   `PWMB_PS` 系列 —— 四个通道**共用同一个周期寄存器 `PWMB_ARR`**。
2. **蜂鸣器每次换音都会重写这个周期寄存器。**
   `Driver/Buzzer.c`（真值表 1.8 已录）：`PWM_Configuration(PWMB, …)` +
   `period = MAIN_Fosc / hz`、`PWM_Period = period - 1`。
3. **蜂鸣器和舵机必然要"同时使用"。** 本工程页面切换、开关闹钟、设置改项
   都会调 `Music_Beep()` 发 40ms 提示音。
   也就是说：**一进"云台"页按一下键，蜂鸣器就响，PWMB 的周期当场被改成 kHz 级，
   舵机的 50Hz 立刻丢失**（表现是舵机抽搐/乱转），而且舵机自己还不知道。
4. **P2.5 = PWM3N，属于 PWMA 组** —— 与 PWMB 的 ARR/PSCR 是**两组独立寄存器**，
   50Hz 和 1kHz 可以同时存在。

**芯片级证据**（不依赖是哪块板）：`Lib/STC8H_PWM.h:67` 的
`PWM3_USE_P24P25()` ⇒ `PWM3P = P2.4`、`PWM3N = P2.5`。
`项目开发文档-STC8H.xlsx` 的引脚复用表也写着
`P2.4 | PWM3P_2 | …` / `P2.5 | PWM3N_2 | …`，与库头文件一致。

**待上机确认**：PWM3**N** 是互补通道。只使能 `ENO3N` 而不使能 `ENO3P` 时能否单独输出，
我只能静态推断，**必须上机量 P2.5 有没有 50Hz 方波**。
（若只出 N 通道，相位与 P 通道相反 —— 对舵机只是 0°/180° 方向互换，不影响功能。）

---

## 2. 初始化顺序

| 顺序约束 | 真值 | 出处（原文） |
|---|---|---|
| 扩展寄存器访问必须最先 | `EAXSFR();` 在所有外设配置之前 | v3.1 `App_System.c` `sys_init()`：`EAXSFR(); /* 扩展寄存器访问使能 */` 为第一句 |
| 全局中断开启位置 | 外设配置**全部完成后**再 `EA = 1;` | v3.1 `sys_init()`：`Buzzer_init();` → `EA = 1; /* 开启全局中断 */` → `printf(...)` |
| v3.1 配置顺序 | GPIO → UART → I2C → ADC → Buzzer → EA=1 | v3.1 `sys_init()` 调用序 |
| PCF8563 初始化 | 只做 `Exti_config()`；I2C 与 GPIO 由外部统一配置 | v3.1 `PCF8563.c` `PCF8563_init()`：`// GPIO_config(); // I2C_config();` `Exti_config();` |
| PCF8563 与屏幕的时序冲突 | **时间读取和屏幕刷新不能同时进行，容易卡死** | v3.1 `App_RTC.c`：`// 时间读取和屏幕刷新不能同时进行， 容易卡死，所以延时一会再启动` `os_wait2(K_TMO, 100);` |
| 数码管初始化后应清屏 | `Nixie_clear()` → 全灭 | JC `Nixie.c`：`Nixie_clear(); // 初始化完，所有数码管默认是灭的` |
| ADC 采样参数 | `ADC_SMPduty = 31`（**不能小于 10**），`CsSetup=0`，`CsHold=1`，`SPEED_2X16T`，右对齐 | v3.1 `ADC_config()` 原文注释 |
| I2C 速率参数 | `I2C_Speed = 13` → 400 kbit（@24 MHz） | v3.1 `I2C_config()`：`// Speed = 13  => 400Kbit` |
| I2C 引脚切换 | `I2C_SW(I2C_P33_P32);` | 同上 |
| I2C 中断 | 主模式 + `DISABLE`（不使能中断） | 同上：`NVIC_I2C_Init(I2C_Mode_Master, DISABLE, Priority_0)` |
| 主时钟 | `#define MAIN_Fosc 24000000L` | v3.1 `Lib/Config.h` |

---

## 3. 显示坐标与单位

| 屏幕 | 真值 | 出处（原文） |
|---|---|---|
| **主屏** | `(x, y)`：**x = 像素列 0–127，y = 页 0–7**（1 页 = 8 像素行） | `SPI_OLED_address()`：`SPI_OLED_WR_Byte(0xb0+y, SPI_OLED_CMD); //设置页地址` |
| **副屏** | 同上 | `I2C_OLED_Set_Pos()`：`I2C_OLED_WR_Byte(0xb0+y, I2C_OLED_CMD);` |
| 汉字占用 | 16×16 汉字 = **2 页高 × 16 像素列** | `SPI_OLED_Display_GB2312_string()`：`SPI_OLED_Display_16x16(x, y, fontbuf);` 后 `x += 16;` |
| 字库地址公式 | `Address = ((MSB-0xB0)*94 + (LSB-0xA1) + 846) * 32` | `SPI_OLED_Display_GB2312_string()` 原文注释：`//国标简体（GB2312）汉字在晶联讯字库IC中的地址由以下公式来计算` |
| 显示 8×16 的调用形参 | **第三个参数是点阵指针，不是字符串** | `void SPI_OLED_Display_8x16(u8 x,u8 y,u8 *dp);` |
| 副屏字符串函数 | `I2C_OLED_ShowString(x, y, u8 *chr, u8 sizey)` | v3.1 `Driver/I2C_OLED/oled.h` |

---

## 4. 库函数签名与已知陷阱

| 函数 | 签名真值 | 陷阱 | 出处 |
|---|---|---|---|
| `delay_ms` | `void delay_ms(unsigned char ms);` | **参数是 u8**，传 400 会溢出成 144；只支持 1–255 ms | `Lib/Delay.c`：`// 参数: ms,要延时的ms数, 这里只支持1~255ms` |
| `delay_us`（JC DHT11 内） | `void delay_us()` **无 static** | 直接移植会与其它模块撞名，**必须加 `static`** | JC `DHT11.c`；《04-M1分工规格》F 角色注意事项 |
| `I2C_WriteNbyte` / `I2C_ReadNbyte` | 无超时死等 | STC 官方 I2C 库等待标志位无超时 | 见第 8 节待办 |
| `SPI_OLED_ShowNum` | `void SPI_OLED_ShowNum(u8 x,u8 y,float num,u8 len);` | **形参是 float**，只要在工程里被调用就会链入 `C51FPL.LIB` | 实测见第 8 节 `.m51` 证据 |
| `sprintf` / `printf` | — | 链入 `PRINTF` 模块（v3.1 用 `printf` 打印调试信息） | 同上 |
| `EEPROM_read_n/write_n` | `void EEPROM_read_n(u16 EE_address, u8 *DataAddress, u16 number);` | 写前需整扇区擦除（**512 字节**）；`EEPROM_Trig()` 内部 `EA=0` 保护 | 官方优化版 `EEPROM.c`：`IAP_TRIG = 0x5A; IAP_TRIG = 0xA5;` + `EA = 0; //禁止中断, 避免触发命令无效` |
| `os_wait2(K_TMO, n)` | 等待 `n × 心跳` | 心跳 = 5 ms（`Conf_tny.A51`）→ `n=200` 是 1 秒 | v3.1 `App_RTC.c`：`os_wait2(K_TMO, 200);` 注释 `// 每秒一次` |
| `Timer_Inilize` 1000 Hz 写法 | `TIM_Value = 65536UL - (MAIN_Fosc / 1000)` | 注意 `65536UL` 的 UL 后缀 | v3.1 `App_Digital.c` `Timer_config()` |
| `NIXIE_show(a_num, b_idx)` | 段码 + 位掩码，**不是 (num, pos)** | 与 `NIXIE_display(num_id, pos)` 语义不同，混用会全屏乱 | v3.1 `NIXIE.h` |

---

## 5. 上电安全电平（规范第五节检查项 1）

| 引脚 | 默认复位后状态 | 后果 | 处置 |
|---|---|---|---|
| **P0.1（马达）** | 准双向口，输出 1 = 高 | **马达震动** | 在 `sys_init()` 第一条就把 `P01 = 0`，再配为推挽输出低 |
| P4.5（LED 总开关） | 准双向口，输出 1 | 低导通 → 总开关**关**，8 灯不受控 | 显式配推挽 + 初始 `LED_SW = 1`（关），需要时再拉低 |
| P0.0（蜂鸣器） | 准双向口，输出 1 | PWM 未配置前引脚为高 → 无源蜂鸣器需 PWM 才响，静默；但仍是未知态 | 显式配推挽 + `P00 = 0`（照抄 JC） |
| 数码管 NIX_DI/SCK/RCK | 准双向口 | 上电瞬间移位寄存器内容随机 | `Nixie_init()` 后立即 `Nixie_clear()`（照抄 JC） |

---

## 6. 字库与中文显示能力（硬约束）

| 屏幕 | 能否显示任意汉字 | 依据 |
|---|---|---|
| **主屏（SPI，带字库）** | **能**。汉字点阵来自屏上独立的字库 IC（晶联讯），通过 P1.0/P1.1 读 ROM，**不占单片机 Flash** | `SPI_OLED_Display_GB2312_string(x, y, "二三四,数字,上升");`（day11 `main.c` 实机示例）；ROM 地址公式见第 3 节 |
| **副屏（I2C）** | **不能**。只有 7 条固定索引汉字 + ASCII | `I2C_OLED/oledfont.h` 中 `unsigned char code Hzk[][32]` 实测**仅 7 条目**；`I2C_OLED_ShowChinese(x,y,no,sizey)` 的 `no` 是数组下标，不是区位码 |

**结论**：菜单/提示的中文只能走主屏；副屏只能英文/数字/图标。

---

## 7. 文档与代码的显式冲突清单（逐条列理由）

| # | 冲突点 | 文档说法 | 代码说法 | 我采用 | 理由 |
|---|---|---|---|---|---|
| 1 | LED 编号顺序 | 《02》`P2.7 P1.5 P1.4 P2.2 P2.6 P1.5…` | v3.1 `P27 P26 P15 P14 P23 P22 P21 P20` | **v3.1** | v3.1 为真机跑通固件；引脚集合两者一致，仅编号顺序不同。若“日出唤醒”需要固定方向，请你指认哪一端是 LED1 |
| 2 | PCF8563 中断触发沿 | 《02》未提 | v3.1 `EXT_MODE_Fall`；JC `EXT_MODE_RiseFall` | **v3.1 的 `EXT_MODE_Fall`** | 《02》第八节：“时钟芯片的‘到点信号’是低电平，正好匹配下降沿触发”，与 v3.1 一致 |
| 3 | PCF8563 API 命名 | 《04》用 `PCF8563_Init/GetTime/SetAlarm/ClearAlarmFlag`（驼峰） | v3.1 与 JC 均为下划线式 `PCF8563_init/get_clock/set_alarm/clear_alarm` | **v3.1/JC 下划线式** | 《04》是接口清单草案，代码是既有实现；且《00》说明文档只作参考 |
| 4 | 数码管位号基准 | 《04》`Nixie_display(num, idx)` | v3.1 `pos ∈ [0,7]`；JC `idx ∈ [1,8]` 且内部 `1 << (idx-1)` | **v3.1 的 `[0,7]` + 位掩码 `1<<pos`** | 采用同一来源避免混用；第 4 节已列为陷阱 |
| 5 | 温湿度返回类型 | — | v3.1 `int8 DHT11_get_info(float*, float*)`；JC `int DHT11_get_humidity_temperature(int*, float*)` | **改为整数**（见第 9 节待你确认） | 《04》F 角色：“讲义里的参考代码用了小数运算，**要改成整数运算**”；且 `.m51` 已证 float 会链入整个 `C51FPL.LIB` |
| 7 | 超声波引脚 | 《02》`P2.4 / P3.6` | 参考工程 `P4.7 / P3.3` | **《02》的 P2.4/P3.6** | P4.7 是本板**主屏片选**、P3.3 是 **I2C 的 SDA**，照抄必撞；参考工程是别的板子 |
| 8 | 舵机引脚 / 通道 | 《02》`P2.5` | 参考工程 `P0.2` / **PWM7** | **《02》的 P2.5（对应 PWM3N）** | PWM7 属 **PWMB** 组，会与蜂鸣器(PWM5)、马达(PWM6) 抢同一组周期寄存器；P2.5 对应 PWM3N 属 **PWMA**，独立 50Hz |
| 6 | 显示刷新分工 | 《04》C 角色：T0 任务排队刷两块屏 | v3.1 由各业务任务自己调 `App_I2C_OLED_Refresh()` | **待你裁决** | v3.1 是自检固件（单业务），闹钟要 5 任务并发，沿用会与《02》6.1 冲突 |

---

## 8. 现有工程 `51_MCU_Project` 的 bug（带编译/链接证据）

> 方法：把整个目录**复制**到 `D:\Develop\WorkBuddyDATA\2026-09-17-21-52-01\_baseline\51_MCU_Project` 后编译，
> 你的原目录未做任何修改（`git status --porcelain` 输出为空）。
> 命令：`UV4.exe -r project.uvproj -j0 -o build_log.txt`

**结果**：`0 Error(s), 7 Warning(s)`，`Program Size: data=57.7 xdata=1551 code=20642`

| # | 严重度 | 问题 | 证据（原文） |
|---|---|---|---|
| 1 | **致命** | `Lib/Delay.h` 找不到 → 编译告警 | `User\main.c(4): warning C318: can't open file 'LED.h'`；`.uvproj` 的 `IncludePath` 缺 `.\Driver\LED` |
| 2 | **致命** | `LED_Init()` 有声明无实现（`LED.c` 整文件被注释掉） | `*** WARNING L1: UNRESOLVED EXTERNAL SYMBOL / SYMBOL: LED_INIT / MODULE: .\Objects\main.obj` |
| 3 | **致命** | `count` 变量只声明未定义 | `*** WARNING L1: UNRESOLVED EXTERNAL SYMBOL / SYMBOL: COUNT / MODULE: .\Objects\App_oled.obj`（`App.h` 有 `extern int8 count;`，全工程无定义） |
| 4 | **致命** | `LED.c` 在 `.uvproj` 里**注册了两次** | `*** WARNING L7: MODULE NAME NOT UNIQUE / MODULE: .\Objects\LED.obj (LED)`；`FilePath` 列表中 `.\Driver\LED\LED.c` 出现 2 次 |
| 5 | **致命** | `User/main.c` 从未调用 `App_Init()`，而 `App_Init()` 才是做 GPIO/UART/I2C/ADC/PWM 配置的地方 → **I2C 屏、PWM 全部未初始化** | `main.c` 的 `sys_init()` 只有 `EA=1; EAXSFR(); LED_Init(); printf`；`App.c` 的 `App_Init()` 未被任何文件调用 |
| 6 | **致命** | `.uvproj` 漏注册多个必需源文件：`EEPROM.c`、`Exti.c`、`Exti_Isr.c`、`Compare.c`、`WDT.c`、`STC8H_RTC.c`、`Soft_I2C.c`、`Soft_UART.c`、`STC8G_PCA.c`、`STC8H_DMA.c`、`isr.asm` | 对比 `FilePath` 列表 vs `Lib/` 实际文件；**与规范第二节点名的踩坑完全同型** |
| 7 | 高 | `system_ms` 为 `u16` → 65.5 秒回绕，番茄钟 25 分钟不可用 | `Driver/Timers/Timers.c`：`volatile u16 system_ms = 0;`；`u16 Timers_GetSystemMs(void)` |
| 8 | 高 | `Key.c` 的 `key_change_ms[]` 也是 `u16`，与 7 联动必须一起改 u32，否则消抖计时错乱 | `Key.c`：`static u16 key_change_ms[KEY_INPUT_COUNT];` |
| 9 | 高 | `Nixie_Scan2ms()` 空实现：判断后直接 `return`，**没有任何扫描动作** | `NIXIE.c`：`if(now - Nixie_change_ms == 2){ Nixie_change_ms = now; return; }` |
| 10 | 高 | `NIXIE.h` 函数声明**缺参数类型** | `void Nixie_show(num, idx);` / `void Nixie_display(num, idx);` |
| 11 | 高 | `LED.c` 注释里 `LED_AllOff()` 用 `P2 |= 0xCF` — 掩码与 `LED.h` 的引脚定义不吻合（P26/P23 位） | `LED.c` 注释段 `P2 |= 0xCF; // 置 P27 P26 P23 P22 P21 P20`，缺 P2.3 |
| 12 | 中 | `App_oled.c` 用 `_task_ 2` 硬编码任务号，与 `App_Public.h` 的 `App_Nixie_Task_Id = 2` **撞号** | `App_oled.c`：`void task_2() _task_ 2 {` |
| 13 | 中 | `I2C_OLED.h` 重复定义 `u8/u16/u32`，且 `#define u32 unsigned int`（应为 `unsigned long`） | `I2C_OLED.h`：`#define u8 unsigned char` / `#define u32 unsigned int` |
| 14 | 中 | `EEPROM.h` 的 `MCU_Type` 保持官方示例默认值 `STC8X1K08`，与本芯片 64 KB 不符（正确应为 `STC8XxK60` → `0xF000`） | `EEPROM.h`：`#define MCU_Type STC8X1K08`；实测 `MOVC_ShiftAddress` 仅在 `EEPROM.h` 定义、**无 .c 引用**，故当前无功能影响，但属配置错误 |
| 15 | 中 | `App.c` 的 `GPIO_config()` 把 P0.1（马达）配置**注释掉了** → 上电默认高电平，马达会震 | `App.c`：`//    GPIO_InitStructure.Pin  = GPIO_Pin_1;` `//    …GPIO_InOut_PP…` 整段注释 |
| 16 | 中 | `GPIO_config()` 未配置 P0.4（NTC），NTC 无法读数 | `App.c` 只配了 P0.5（旋钮）为高阻 |
| 17 | 低 | `Key.c` 注释写“三个按键”，实际 4 个 | `Key.c`：`/* 三个按键分别保存自己的状态和最近变化时间互不影响 */` |
| 18 | 低 | 空文件被注册进工程：`Driver/DHT_1/DHT1.c`、`Driver/RTC_Alarm_Clock/Alarmclock.c`（均 0 字节） | `ls -l` 显示 0 字节 |
| 19 | 观察 | **浮点库 + printf 库已被链入**：`C51FPL.LIB (?C?FPMUL)`、`(?C?FPDIV)`、`(?C?FPADD)`、`(?C?CASTF)`、`(PRINTF)` | `Listings/project.m51` 第 57–68 行。来源：`SPI_OLED_ShowNum(float)` 与 `sprintf/printf` |
| 20 | 观察 | 当前 `code = 20642`（20.2 KB）。《02》红线 32 KB，剩约 11.8 KB 给闹钟业务 | 构建日志 `Program Size` |

---

## 9. 我做不到的 / 必须你确认的

### 9.1 我做不到（硬边界）

| 项 | 说明 |
|---|---|
| 真机验证 | 我只能做静态三重验证（编译 0 Error / 链接 UNRESOLVED=0 / `.m51` 尺寸与库）。**烧录后的电平、时序、屏幕坐标是否正确必须你上机反馈** |
| ⚠ 舵机 P2.5 是否真有 50Hz | 我只能从 `Lib/STC8H_PWM.h` 确认 P2.5 = PWM3**N**（互补通道）。"只使能 N 通道能不能单独输出"必须上机量 P2.5 |
| ⚠ 超声波 P2.4/P3.6 | 出处是《02》设计文档（不是验证过的代码）。上机时若测不到距离，先量 TRIG 有没有 20µs 脉冲 |
| 仪表级事实 | PWM 实际频率、I²C 波形、ADC 基准实际值、LED 亮度线性度 |
| 原理图原件核对 | 我只解析了 `项目开发文档-STC8H.xlsx` 与代码；**PDF 原理图**我只确认了文件名，未逐网络表比对 |
| 汉字裁剪 | 主屏走硬件字库，**不需要**裁剪汉字；副屏本来就不能显示汉字，因此《04》里“裁 80 个汉字省 3.5KB”这条**在本板上不成立**（硬件字库不占 Flash）——这条需要你确认是否同意 |

### 9.2 需要你现在回答的问题

见对话中的提问。未回答前我不写任何代码。
