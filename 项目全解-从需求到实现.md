# 一机两用终端 · 项目全解（从需求到实现，逐层讲透）

> **工程**：`D:\Develop\embedded-project\51_MCU_Alarm`（`Project.uvproj`）
> **芯片**：STC8H8K64U（24MHz / 64KB Flash / 8KB xdata / 256B data）
> **操作系统**：RTX51 Tiny　**编译器**：Keil C51（Large 模型，Optimize 8）
> **版本**：`APP_VERSION_STR = "1.0.0"`
> **实测基线（2026-09-23 第 70 轮减重后）**：
> `data = 46.1 / 256`　`xdata = 3713 / 8192`　`code = 55535 / 61440`（余 5905 字节 = 9.6%）
> `0 Error(s), 0 Warning(s)`　`UNRESOLVED = 0`　浮点库符号 = 0
> **规模**：98 个源文件 / 18823 行（`.c` 50 个 13481 行 + `.h` 47 个 4658 行 + `Conf_tny.A51` 684 行）
>
> **本文性质**：**现状描述 + 因果解释**。所有结构、数字、行为都从**代码与编译产物**实测得来，
> 不是设计意图；凡"为什么这么做"，都给出代码里的证据或真机现象。
> 本文写于 2026-09-23，是一次"全工程通读"的产物 —— 因此它讲的是**代码真正在做什么**，
> 而不是文档曾经计划什么。

---

## 目录

| 部分 | 内容 | 适合谁读 |
|---|---|---|
| 一 | 这个项目是什么 | 所有人（先读这个） |
| 二 | 需求是怎么变成设计的（含"计划了但没做"） | 想知道"为什么是这个样子"的人 |
| 三 | 硬件真值：每根线、每个寄存器 | 要接线 / 要改硬件的人 |
| 四 | 软件架构：分层、任务、通信、数据流 | 要读代码 / 要加功能的人 |
| 五 | 启动流程逐步拆解 | 排查"上电卡住"的人 |
| 六 | 导航与界面系统（含三个游戏） | 要改界面的人 |
| 七 | 两块屏的渲染层（含两条通路铁律） | 要改显示的人 |
| 八 | 时间与闹钟（全项目唯一的真算法） | 要改闹钟的人 |
| 九 | 存储（EEPROM 分区） | 要加持久化数据的人 |
| 十 | 输入系统（按键 / 矩阵 / 旋钮 / 事件） | 要改按键的人 |
| 十一 | 声音与反馈（蜂鸣器 / 马达 / 小灯） | 要改音效的人 |
| 十二 | 传感器（NTC / DHT11） | 要改采集的人 |
| 十三 | 外接模块（超声波 / 舵机） | 要接外设的人 |
| 十四 | 上位机通信协议 | 要写电脑端程序的人 |
| 十五 | 工程化：编译 / 验证 / 体积 | 要交付的人 |
| 十六 | 踩坑史（按主题归类） | 遇到问题时先来这里查 |
| 十七 | 设计取舍汇总（为什么选 A 不选 B） | 想推翻某个决定的人 |
| 十八 | 已知限制与未来可做什么 | 要加新功能的人 |
| 附录 A | 文件清单与职责 | — |
| 附录 B | 接口清单（按真实头文件逐条抄录） | — |
| 附录 C | 关键常量表 | — |
| 附录 D | 文档地图（22 份文档怎么用） | — |

---

# 第一部分 这个项目是什么

## 1.1 一句话

一台**双屏桌面终端**：平时当**桌面时钟 + 闹钟**，想玩的时候当**掌上游戏机**；
外接一个超声波模块和一个舵机之后，还能**测距**和**转云台**。
不连手机、插上 USB 就能用。

## 1.2 七个任务（主菜单就是这七项）

| 屏上编号 | 任务 | 需要外接件 | 一句话说它干什么 |
|---|---|---|---|
| 1 | 日期和时间 | 否 | 大字时钟；进去改年月日 / 改时分 |
| 2 | 闹钟 | 否 | 3 组闹钟，每组可选"周几响"，到点响铃或震动 |
| 3 | 番茄钟 | 否 | 专注 / 休息倒数，时长可改（1~99 分钟） |
| 4 | 测距仪 | **超声波 HC-SR04** | 实时显示前方距离（2~400cm），只在副屏 |
| 5 | 云台 | **舵机** | 按键每次转 10°，可一键回中位 |
| 6 | 设置 | 否 | 7 项：音量 / 曲目 / 提醒 / 主屏 / 日出 / 贪睡 / 震动强度 |
| 7 | 游戏 | 否 | 3 个游戏：贪吃蛇 / 打砖块 / 飞机大战 |

> 屏上编号从 **1** 开始（2026-09-21 起改的），代码里的下标从 **0** 开始，差 1。
> 这一条很容易看错，见 §6.3。

## 1.3 硬件构成

```
①核心板（STC8H8K64U，所有程序跑在这里，板载 CH340N 做 USB 转串口）
      ↓ 插
②转接板（把核心板针脚"翻译"成扩展板认的排布）
      ↓ 插
③扩展板（屏幕、按键、数码管、灯、蜂鸣器、传感器都焊在这）
      ↓ 杜邦线外接
超声波（P2.4 / P3.6） · 舵机（P2.5，另需 5V 与 GND）
      ↓ USB 线（同一个口既下载又通信）
电脑
```

**扩展板上焊着的 15 项**（逐项都被用到，没有闲置的）：

两块 OLED（主屏 SPI + 副屏 I2C）、8 位数码管、4 个独立按键、4×4 矩阵键盘、
8 颗小灯 + 总开关、无源蜂鸣器、振动马达、旋钮（电位器）、
NTC 热敏电阻、DHT11 温湿度、时钟芯片 PCF8563（带纽扣电池）、USB 串口。

## 1.4 项目形态

**功能已经全部做完，处于"只维护、不加新功能"的状态。**

| 项 | 数值 |
|---|---|
| 源文件 | **98 个**（`.c` 50 + `.h` 47 + `Conf_tny.A51` 1） |
| 代码行数 | **18823 行** |
| 程序空间 | `code = 55535` / 上限 **61440** ⇒ 余 **5905 字节（9.6%）** |
| 扩展 RAM | `xdata = 3713` / 8192 |
| 片内 RAM | `data = 46.1` / 256 |
| EEPROM | 4KB @ `0xF000`，分成 8 个 512 字节区 |

> ⚠️ **程序上限是 61440，不是 65536** —— EEPROM 占了 `0xF000~0xFFFF`。
> 而且 `Project.uvproj` 里芯片定义写的是 `IROM(0-0xFFF8)`，
> **链接器以为代码区能用满 64KB**，所以程序超过 60KB 时 **Keil 不报错**，
> 但 ISP 下载会因超出程序区拒收。**判"够不够"必须按 61440 自己算。**

---

# 第二部分 需求是怎么变成设计的

## 2.1 起点：一份交出去的"约束文档"

这个项目的第一份文档不是需求说明书，而是一份**给 AI 的约束规范**
（`D:\BWJJ\Desktop\AI项目生成规范.md`，第一版 2026-09-16）。
它的核心原则只有一条：

> **"参考"是 AI 出错的最大来源。** 必须把"参考这些资料"改成
> "**这些资料是唯一真值，逐字对齐；任何与资料不一致的地方，必须显式列出并说明理由。**"

原因很直白：AI 读参考代码后，写自己的实现时会用"通用知识"补全细节，
而这些通用知识在特定硬件上往往是错的。本项目早期踩的坑**全是这一类**。

这份规范后来又长了两版（截至 2026-09-23），从"动手前的门禁"扩到
"跑起来之后"和"完工之后"，是理解本项目一切决策的**元文档**。

## 2.2 需求到功能的映射

原计划分三步走：**M1 闹钟模式 → M2 掌机模式 → M3 外接模块 + 上位机**。
实际是**一次做完**的。以下逐项对照（左=原计划，右=实际）：

| 原计划 | 实际 | 差异原因 |
|---|---|---|
| 8 组闹钟 | **3 组** | 用户："闹钟只要三个，多了任意分不清" |
| 独立"游戏大厅"页 + 每日挑战 + 副屏分数 + 小灯生命值 + 旋钮控制挡板 | **「7 游戏」页内两状态**，3 个游戏，方向键控制，**游玩时副屏关闭** | 用户："游戏大厅不要了，做成页内两个状态" |
| 小灯当"生命值" | 只用于**日出唤醒**和**上电进度指示** | 同上 |
| 蓝牙模块 | **改走 USB 串口**（板上 CH340 直通） | 串口 2 的引脚被主屏和 DHT11 占了 |
| 超声波"挥手停闹钟" | 做成独立的**「4 测距仪」**任务 | 用户要独立页面 |
| 舵机"物理敲铃" | 做成独立的**「5 云台」**任务 | 同上 |
| 副屏显示状态栏 + 取模图标（48×32，6~8 个） | **7 张 128×64 整屏任务图标**（7168 字节） | UI 改造时的实际选择 |
| 电脑端 Python 上位机界面 | **只做了板子这侧的协议**（11 条命令） | 没做 |

**明确没做的**（别按老文档找）：
游戏大厅独立页、每日挑战、游戏最高分/存档（EEPROM 1 号区至今一个字节都没写）、
游戏音效、撞击震动、蓝牙、Python 上位机界面。

## 2.3 三次架构级转向（记录为什么）

这三条都是"用真机迭代换来的"，不是一开始就这么设计的。

### 转向 1：从"一个外设一个任务"改成"一个职责一个任务"

- **原参照**：`02_STC8_扩展板自检程序v3.1` —— 一个外设一个任务，
  切业务时 `os_create_task` / `os_delete_task`（适合"一次只验一个外设"的自检程序）；
- **本工程要的是产品**：7 个页面并存、每个页面都要显示 + 按键 + 逻辑，
  所以**按职责分 5 个常驻任务**（渲染 / 输入 / 逻辑 / 音效 / 采集）。
- **但保留了一条原则**（照 v3.1）：**进入 = 完整初始化，离开 = 彻底收尾，
  不依赖上一次留下的任何状态**。落在 `menu_task_enter()` / `menu_task_leave()` 上。

### 转向 2：从"总线锁排队"改成"所有 I2C 收进一个任务"

**这是全项目最重要的一个决定**，花了 25 轮真机迭代才定下来。

- **原方案**：副屏和时钟芯片共用 I2C，加一把总线锁"谁用谁举手"；
- **怎么失败的**：一次整屏重画约 **350ms**，远大于 RTX51 的时间片 **25ms**，
  持锁期间必然被换出去；锁超时后**强制夺锁**，于是两个任务同时往同一控制器发字节
  → 字节流错位 → 数据字节被 SSD1306 当成控制字节 → **花屏 / 黑屏**，
  而且 **MCU 重启救不回来**（屏一直带电、控制器自锁，只有拔 USB 给它断电才恢复）；
- **最终方案**（照 `demo30`）：**所有 I2C 访问收进 `TASK_RENDER` 一个任务**，
  其余任务只置 5 个请求标志。天然串行 ⇒ **根本不需要锁**。
- 完整排查过程见《副屏显示链路专项总结》（含被真机数据否掉的 6 个假设）。

### 转向 3：从"主屏做全部交互"改成"主屏切任务、副屏看内容"

- v1 设计稿：主屏做全部交互（中文菜单 + 编辑 + 提示），副屏做状态栏 + 图标点缀 → **作废**；
- v2（定稿，照 demo30）：**主屏（SPI）= 任务切换界面；副屏（I2C）= 当前任务的具体内容**；
- 落地时用户又改了两处：任务从 5 项加到 **7 项**（第 0 项"时钟"改名"日期和时间"，
  新增"测距仪""云台"），键位改成 K1=上移 / K2=确认 / K3=下移 / K4=返回。

---

# 第三部分 硬件真值：每一根线、每一个寄存器

> 本节所有内容都逐条对着代码/驱动头文件核过；出处更全的版本见《硬件真值表》。

## 3.1 引脚分配总表

### 板载部分（焊接固定）

| 硬件 | 占用引脚 | 模式 | 说明 |
|---|---|---|---|
| **主屏 SPI OLED**（6 根线） | SCL=**P5.0**、MOSI=**P1.3**、DC=**P1.6**、CS=**P4.7**、MISO=**P1.1**、字库CS=**P1.0** | 前 5 根 + 字库CS **推挽输出**（除 MISO）；MISO 留给输入 | **软件模拟 SPI**（IO 口一位位翻转）。★ 端口模式必须显式配，否则主屏漆黑，见 §16.1 |
| **副屏 I2C OLED** | SCL=**P3.2**、SDA=**P3.3** | 开漏（板上有外部上拉） | 与时钟芯片**共用**，硬件 I2C |
| **时钟芯片 PCF8563** | P3.2/P3.3（I2C）+ **P3.7**（闹钟中断） | P3.7 准双向口 | 中断走 **INT3，下降沿** |
| **数码管（8 位，74HC595）** | DI=**P4.4**、SCK=**P4.2**、RCK=**P4.3** | 推挽 | 段码低有效、位码高有效；扫完必须恢复 |
| **矩阵键盘 4×4** | 列 COL1~4 = **P0.3 / P0.6 / P0.7 / P1.7**；行 ROW1~4 = **P3.4 / P3.5 / P4.0 / P4.1** | 全部准双向口 | 逐行拉低读列，扫完 4 行全拉高 |
| **独立按键 K1~K4** | **P5.1 / P5.2 / P5.3 / P5.4** | 准双向口 | 按下=低。P5.4 兼复位脚，实测可当按键 |
| **8 颗小灯 + 总开关** | 总开关=**P4.5**；灯=**P2.7 P2.6 P1.5 P1.4 P2.3 P2.2 P2.1 P2.0** | 推挽 | **低电平点亮**；总开关**低电平导通（打开）** |
| **蜂鸣器** | **P0.0** | 推挽 | PWM5，**PWMB 组** |
| **振动马达** | **P0.1** | 推挽 | PWM6，**PWMB 组**。★ 上电默认电平危险，见 §3.3 |
| **旋钮（电位器）** | **P0.5** | 模拟输入（高阻） | ADC 通道 13，**满量程约 2740（不是 4095）** |
| **NTC 热敏电阻** | **P0.4** | 模拟输入（高阻） | ADC 通道 12 |
| **DHT11 温湿度** | **P4.6** | 准双向口 | 单总线时序 |

### 外接部分（杜邦线）

| 模块 | 引脚 | 模式 |
|---|---|---|
| 超声波 · TRIG | **P2.4** | 推挽输出，平时拉低 |
| 超声波 · ECHO | **P3.6** | **高阻输入**（不是准双向口） |
| 舵机 · 信号 | **P2.5** | 硬件 PWM（**PWMA 组 / PWM3N 通道**） |
| 上位机通信 | **P3.0 (RxD) / P3.1 (TxD)** | 经板上 CH340N 到 USB |
| **备用** | **P0.2** | **唯一剩下的空脚** |

> **引脚宏全部写在各自驱动的 `.h` 里**（`XXX_GPIO_INIT()` / `sbit` / `#define`），
> 改引脚只改头文件，`.c` 不动。

## 3.2 引脚复用与冲突（三个必须搞清楚的地方）

### ① P2.4 同时是"超声波 TRIG"和"PWM3P"

**结论：不冲突，有代码级证据。**

1. `PWM3_USE_P24P25()` 只改 `PWMA_PS`（引脚映射），**不动 `PWMA_ENO`**（输出使能）；
2. `Lib/STC8H_PWM.c` 的 `PWM_Configuration()` 里 P/N 是**各自独立判断**的：
   `if (PWM_EnoSelect & ENO3P) {...} else { PWMA_ENO &= ~ENO3P; }`
3. 舵机传的是 `ENO3N` ⇒ 走 else ⇒ **`ENO3P` 被清 0** ⇒ **P2.4 保持普通 IO** ✅

另外 `UART1_SW(UART1_SW_P30_P31)` ⇒ **P3.6 也不被串口占用** ✅

### ② P3.2 / P3.3 被副屏和时钟芯片共用

这是全项目唯一真正的"共享硬件"。解决方式不是加锁，而是
**把所有访问收进 `TASK_RENDER` 一个任务**（见 §2.3 转向 2、§4.3）。

### ③ PWMA 与 PWMB 是两组独立寄存器 —— 舵机能不能和蜂鸣器共存

| | PWMA（A 组） | PWMB（B 组） |
|---|---|---|
| 通道 | PWM1~PWM4 | PWM5~PWM8 |
| 预分频 | `PWMA_PSCR` | `PWMB_PSCR` |
| 周期 | `PWMA_ARR`（`PWMA_AutoReload`） | `PWMB_ARR`（`PWMB_AutoReload`） |
| 谁在用 | **舵机（PWM3N，50Hz）** | **蜂鸣器（PWM5）+ 马达（PWM6），1kHz~kHz 级** |

**关键点：同一个组里的所有通道共用一个周期寄存器。**
所以蜂鸣器每换一个音就调一次 `PWMB_AutoReload()` —— 如果舵机也在 PWMB 组，
它的 50Hz 就会被蜂鸣器改掉。**这就是"舵机必须放 PWMA 组"的全部理由。**

## 3.3 上电安全电平（哪些脚"默认危险"）

STC8H 复位后端口是**准双向口且输出锁存器为 1（高）**。
对有些外设，"一直是高"是危险状态：

| 引脚 | 复位后状态 | 会出什么事 | 处置 |
|---|---|---|---|
| **P0.1（马达）** | 高 | **马达直接开始震** | `Motor_SafeLevel()` 必须是 `sys_init()` **第一句** |
| **P2.5（舵机信号）** | 高 | 舵机把"一直是高"当成超长脉宽 ⇒ **甩到极限位置**（真机现象："没进过云台任务舵机偶尔自己转"） | `Servo_SafeLevel()` 在 `sys_init()` 最前面拉低 |
| P0.0（蜂鸣器） | 高 | 无源蜂鸣器要靠 PWM 才响，静态电平不会响，但仍是未知态 | `Buzzer_SafeLevel()` 显式拉低 |
| P4.5（小灯总开关） | 高 | 高=关总开关 ⇒ 灯不受控 | `Led_Init()` 显式配推挽 + 关总开关 |
| P2.4（超声波 TRIG） | 高 | 可能让模块误发波 | `SR04_Init()` 配推挽 + 拉低；离页面调 `SR04_Stop()` |
| 数码管 DI/SCK/RCK | 随机 | 上电瞬间移位寄存器内容随机 | `Nixie_ScanInit()` 后立即清屏 |

> **顺序规矩**：**先写 IO 口按到安全电平，再配端口模式** ——
> 此刻端口还是准双向口，"写 0"能可靠拉低；配成推挽之后再写才更稳。

## 3.4 芯片资源总账

### 定时器（5 个，用掉 4 个）

| 定时器 | 用途 | 配置 | 谁配的 |
|---|---|---|---|
| **Timer0** | RTX51 Tiny 心跳 **5ms** | 12T（复位默认） | `Conf_tny.A51` |
| **Timer1** | UART1 波特率发生器 | 16 位自动重载 + 1T，`ET1 = 0` 不开中断 | `Lib/UART.c` |
| **Timer2** | **数码管 1kHz 扫描** | 16 位自动重载 + 1T | `Driver/NixieScan.c` |
| **Timer3** | **1ms 系统时基**（`g_sysTick`） | 16 位自动重载 + 1T | `Driver/Timers.c` |
| **Timer4** | **空闲** | — | 中途给超声波当过计数器，改回软件延时后释放 |

> **"看起来没用"的陷阱**：Timer1 被波特率发生器占着，但它 `ET1 = 0` 不产生中断，
> 所以搜"中断函数"、搜 `Timer_Inilize` **全都搜不到**。
> **判"某定时器是否空闲"要看寄存器（`TRn`/`THn`/`TLn`/`AUXR`/`BRT_TimerN`），
> 不能看有没有中断代码。**

### 中断（5 个使能，3 类显式禁止）

| 中断 | 状态 | 优先级 | 用途 |
|---|---|---|---|
| Timer0 | 库内部 | — | RTX51 心跳 |
| Timer2 | **ENABLE** | 0 | 数码管扫描 |
| Timer3 | **ENABLE** | 0 | 1ms 时基 |
| **INT3（P3.7）** | **ENABLE** | 0 | 时钟芯片闹钟到点，**只置一位标志** `g_rtcIrqFlag` |
| UART1 | **ENABLE** | 1 | 串口收发缓冲 |
| I2C | **DISABLE** | — | 查询式（`NVIC_I2C_Init(..., DISABLE, ...)`） |
| ADC | **DISABLE** | — | 查询式 |
| PWM（A/B 两组） | **DISABLE** | — | 只用硬件 PWM 输出，不要中断 |

**中断里只做最少的活**：INT3 只置标志，I2C 清标志和业务派发都留到 `TASK_LOGIC`。
原因：I2C 总线要和副屏共用，**中断里抢总线会花屏**。

> 参考工程 v3.1 是在中断里直接读改写 I2C 寄存器的，**本工程没照抄这一点**。

### 程序空间消耗分布（从 `.m51` 的段表统计）

| 模块 | 字节 | 占比 | 说明 |
|---|---|---|---|
| `APP_DISPLAY` | 8635 | 15.6% | 两块屏全部版面（最大） |
| `APP_ICONS` | **7239** | 13.0% | **7 张 128×64 图标 = 7168 字节** + `Icon_Get()` 71 字节 |
| `APP_MENU` | 5205 | 9.4% | 页面栈 + 全部按键处理 + 掌机外壳 |
| `I2C_OLED` | 2932 | 5.3% | 副屏驱动 + ASCII 字库（2072 字节是字库表） |
| `APP_GMPLANE` | 2419 | 4.4% | 飞机大战 |
| `APP_ALARM` | 2188 | 3.9% | 闹钟调度 |
| `SPI_OLED` | 2116 | 3.8% | 主屏驱动（含显存版文字） |
| `APP_UART` | 1826 | 3.3% | 串口协议 |
| `LIB_CODE` | 1705 | 3.1% | C51 库代码 |
| `APP_GMSNAKE` | 1454 | 2.6% | 贪吃蛇 |

其余值得单独记的段：`?PR?PRINTF?PRINTF` **869 字节**（printf/sprintf 共用段）、
`?CO?APP_FONT` **832 字节**（16×32 大字字模）、`?RTX?CODE` 739 字节（RTX51 库）。

### 扩展 RAM（xdata 3713 字节）花在哪

| 模块 | 字节 | 是什么 |
|---|---|---|
| `SPI_OLED` | 1028 | `SPI_OLED_GRAM[1024]` 主屏显存 |
| `APP_DISPLAY` | 1021 | 副屏行缓冲、图标中转、日期输入行等 |
| 中断/绝对段 | 314 | RTX51 + 中断栈 |
| `UART` | 261 | 串口收发缓冲（只留 UART1） |
| `APP_GMSNAKE` | 212 | 贪吃蛇静态节点池（40 节点 × 5 字节） |

**副屏故意不做全屏缓存**（按行写），省下约 0.75KB。

### EEPROM（4KB @ `0xF000`，8 个 512 字节区）

| 区 | 地址 | 用途 |
|---|---|---|
| 0 | `0xF000` | `Settings_t` + `AlarmItem_t[3]`（同一区，一次擦除 + 一次写回） |
| 1 | `0xF200` | **空着**（原计划给游戏存档，最终游戏没有存档功能，一个字节都没写） |
| 2~7 | `0xF400`~ | 温湿度记录，2 字节/条，共 **1536 条** ≈ 10.7 天 |

---

# 第四部分 软件架构

## 4.1 五层目录与分层规矩

```
51_MCU_Alarm/
├── User/           程序入口（只有 main.c）
├── App/            业务层：页面、功能、显示
│   └── Game/       掌机三个小游戏（自成一块）
├── Driver/         器件层：一个器件一个文件
│   ├── SPI_OLED/   主屏（带硬件字库）
│   └── I2C_OLED/   副屏（含 ASCII 字库 oledfont.h）
├── Lib/            STC 官方库（已按本工程裁剪）
└── OS/             Conf_tny.A51（RTX51 Tiny 配置）
```

**分层规矩**：

- `Lib/` 放寄存器操作与官方外设驱动，**不含任何业务**；
- `Driver/` 把某个器件封装成函数，**不含页面概念**；
- `App/` 只 include `App_Public.h` 一个入口，**不直接碰寄存器**。

> **一句话判据**：想知道"某个操作该写在哪一层"——看它有没有"页面/任务"的语义。
> 有就是 App，没有就是 Driver。

## 4.2 RTX51 任务模型（5 个常驻 + 1 个一次性）

| 任务号 | 名字 | 周期 | 职责 | 文件 |
|---|---|---|---|---|
| 0 | `TASK_MAIN` | 一次性 | 配外设、建其它任务、**自毁** | `User/main.c` |
| 1 | `TASK_RENDER` | **5ms** | 刷两块屏；**全工程唯一的 I2C 执行者** | `App/App_Display.c` |
| 2 | `TASK_INPUT` | **10ms** | 按键 / 矩阵键盘 / 旋钮 → 事件队列 | `App/App_Input.c` |
| 3 | `TASK_LOGIC` | **10ms**（内部对准每秒） | 串口协议、闹钟调度、页面栈、游戏外壳、测距 | `App/App_Menu.c` |
| 4 | `TASK_MUSIC` | **5ms** | 换音符、马达计时、每 100ms 调日出亮度 | `App/App_Music.c` |
| 5 | `TASK_SENSOR` | **1s** | 读 DHT11 / NTC，每 10 分钟存一条记录 | `App/App_Sensor.c` |

**RTX51 Tiny 配置**（`OS/Conf_tny.A51`）：心跳 **5ms**、时间片 **25ms**、`FREE_STACK = 20`。

> **为什么任务按"职责"分而不按"外设"分**：每个任务要服务**全部 7 个页面** ——
> `TASK_RENDER` 画所有页、`TASK_INPUT` 收所有键。
> 所以这 5 个任务只要系统在跑就必须一直在，**不能按页面创建/销毁**。

## 4.3 任务间通信只走两条路

### ① 事件队列（`App_Input.c`，16 条，`Event_t` = 2 字节）

```
Driver 层按键扫描（Keys / MatrixKey 回调）
      ↓  立即上报，不阻塞
TASK_INPUT 的 evt_push()  →  s_queue[16]（xdata 环形队列）
      ↓  TASK_LOGIC 每 10ms 取一条（Input_GetEvent）
Menu_OnEvent(evt)
```

- **队列满时丢最旧的一条**（不是丢新的）—— 代码原话："保证最新的操作不丢，比丢新的更友好"。
- 独立按键**短按在抬手时上报**、长按在按满 2 秒时上报，两者不互相污染；
  矩阵键盘只在**按下**时产生事件。

### ② I2C 请求标志（`App_Public.h`，5 个 `volatile bit`）

```
任何任务想动 I2C  →  只置一个标志
      ↓
TASK_RENDER 看到标志才真正执行 I2C 操作
```

| 标志 | 谁置位 | 含义 | 谁执行 |
|---|---|---|---|
| `g_reqClockRefresh` | TASK_LOGIC（每秒） | 读一次时间 | `Clock_Refresh()` |
| `g_reqClockSet` | 校时流程（`Clock_Set`） | 把时间写进芯片 | `Clock_SetNow()` + `Alarm_ApplyHardwareNow()` |
| `g_reqClockRecover` | `Clock_TryRecover()` | 时钟自救 | `Clock_TryRecoverNow()` |
| `g_reqAlarmApply` | 闹钟改动后 | 重写硬件闹钟 | `Alarm_ApplyHardwareNow()` |
| `g_reqRtcIrq` | INT3 中断 | 处理芯片中断（清标志） | `Alarm_OnRtcIrq()` |

> **一个值得学的实现技巧**：把"真正干活的函数"改名（`Clock_Set` → `Clock_SetNow`），
> 原名字保留成"只置请求标志"的薄壳 ——
> **这样所有既有调用点一行都不用改，自动全部变成"发请求"。**

## 4.4 为什么数码管不在任务里

数码管由 **Timer2 的 1kHz 中断**直接刷（`Driver/NixieScan.c`），不走任务。

原因：RTX51 心跳是 5ms，**任务最快也只能 5ms 醒一次**，
而"每 2ms 刷一位"在 RTX51 下做不到（用任务刷会是 40ms/帧 = 25Hz，会看到闪烁）。
中断里每次只刷一位（32 次移位 + 一次锁存，约 20µs，占 1ms 的 2%），
一圈 8 位 = 8ms = **125Hz**，肉眼不闪。任务只负责"往缓冲里填要显示的数字"。

## 4.5 刷新模型：一个入口 + 一对标志

```
各模块的状态（g_clock / g_settings / g_alarms / 各页面的 s_xxx）
      ↓
TASK_RENDER → Display_Poll()
      ↓ 按需调用
主屏：main_redraw()  → main_draw_line_fit()   ← 每次写满 128px 整行
副屏：sub_redraw()   → sub_draw_line() / sub_blit() / gauge_flush()
      ↓
真正上屏：SPI 直写 / SPI_Refresh / I2C 写
```

**唯一刷新入口是 `Display_Refresh(clearScreen)`**：

| 参数 | 行为 | 什么时候用 |
|---|---|---|
| `1` | **允许清屏**（整屏重画） | 切页、内容形态变了 |
| `0` | **只重画、绝不清屏** | 数值在变（时间、测距、音量…） |

> **两条硬约束**（都是真机换来的）：
> 1. **`clearScreen=1` 的请求会走 100ms 执行点限流**（防止"洪流"）。
>    所以"数值刷新"绝不能传 1 —— 那会让界面卡顿、闪黑。
> 2. **不清屏就必须保证"写入覆盖整屏"**，二者必居其一。

**限流只对"要清屏"的那种生效**，页内移动（`clearScreen=0`）完全不受限流约束 ——
所以挪光标不会"卡一下再跳一格"。

## 4.6 数据流全景

```
【输入侧】
物理键 → Driver（消抖/长按/矩阵扫描/ADC）→ App_Input 打包成 EVT_*
                                                  ↓ 环形队列
【逻辑侧】                          TASK_LOGIC ← Input_GetEvent
                                       │  Menu_OnEvent（页面分派）
                                       │  Alarm_Tick1s（每秒）
                                       │  Range_Poll / Game_Poll
                                       ↓  改状态 + 置请求
【存储侧】App_Storage ←→ 片内 EEPROM            │
【输出侧】  TASK_RENDER ← 标志 │ 状态            │
              ↓                                  ↓
        两块屏 + 数码管（Timer2 中断）      TASK_MUSIC（蜂鸣器/马达/小灯）
                                          TASK_SENSOR（读温湿度 → 全局量）
```

---

# 第五部分 启动流程逐步拆解

## 5.1 `User/main.c`

```c
void main_start(void) _task_ TASK_MAIN
{
    sys_init();                    /* 初始化硬件（见 5.2） */
    os_create_task(TASK_RENDER);   /* 1 */
    os_create_task(TASK_INPUT);    /* 2 */
    os_create_task(TASK_LOGIC);    /* 3 */
    os_create_task(TASK_MUSIC);    /* 4 */
    os_create_task(TASK_SENSOR);   /* 5 */
    os_delete_task(TASK_MAIN);     /* 自毁，释放资源 */
}
```

> 函数名**不要用 `start` 这类词** —— 会和 `Lib/I2C.h` 里的 `Start` 撞名（v3.1 的注释点名过）。

## 5.2 `sys_init()` 的 8 步（顺序不能随意调）

| 步 | 做什么 | 为什么必须在这个位置 |
|---|---|---|
| **1** | `Motor_SafeLevel()` / `Buzzer_SafeLevel()` / `Servo_SafeLevel()` | **先按安全电平、再配端口模式**。P0.1 高=马达震、P2.5 高=舵机甩到极限 |
| **1** | `Led_Init()` | **故意只配端口 + 全灭，不开总开关** —— 总开关关着，8 颗灯物理上不可能亮（用户要求"开机动画期间其它设备不能有反应"） |
| **2** | `EAXSFR()` → `Timers_Init()` | 扩展寄存器使能必须在写扩展 SFR 之前；**1ms 时基必须最早** —— 后面一切"看钟算差值"的逻辑都依赖它 |
| **3** | `GPIO_config()` / `UART_config()` / `I2C_config()` / `ADC_config()` | 通信与模拟外设 |
| **3.5** | `SPI_OLED_Init()` → `ColorTurn(0)` → `DisplayTurn(0)` → `boot_animation()` | 开机动画卡在**全系统最干净的窗口**，见 §5.4 |
| **4** | `Led_Power(1)` + `Buzzer_Init()` + `Motor_Init()` | 动画之后才允许点灯；两个 PWM 属 PWMB |
| **5** | `Nixie_ScanInit()` + `Nixie_SetTime(0,0,0)` | 数码管由 Timer2 中断刷，配在这里让它与其它外设同时就绪 |
| **6** | `Storage_Load()` | 只读不写，**不做任何 I2C 访问** |
| **7** | `EA = 1` | 开总中断。**在它之前，Timer2 / Timer3 / INT3 一个都不会来** |

**两个"漏了会出大事"的点**：

- **`Nixie_ScanInit()` 必须在这里调**。漏了它：Timer2 从未配置、`NIXIE_init()` 从未执行、
  8 位数码管一位都不亮。（真机踩过，见 §16.1）
- **`Clock_Init()` 不能放在这里**。它内部会走 I2C 读 PCF8563；一旦卡住，
  `sys_init()` 永不返回 ⇒ `os_create_task()` 一句都没执行 ⇒ 一个任务都建不出来 ⇒
  **三块屏全无反应**。移到 `TASK_LOGIC` 之后，最坏情况变成"时间读不到，界面显示占位值"。

## 5.3 上电进度指示（`BOOT_CRUMB`）

真机上"三块屏都不亮、按键没反应"时，无法判断程序跑到哪一步。
所以把 **8 颗小灯当成进度条**：

| 现象 | 含义 |
|---|---|
| 亮 k 颗 | `sys_init()` 走到第 k 步停住 → 问题在第 k+1 步 |
| **亮 8 颗** | `sys_init()` 全部走通 |
| **只剩第 1 颗** | 五个任务都跑起来了（**正常运行状态**） |
| 全灭 | 卡在第 1 步之前 |

**它为什么可信**：**直接写 IO 口**，不经过任何任务、不排队、不依赖任何外设初始化、
甚至**不依赖 RTX51 跑起来**。只要 CPU 还在执行指令，灯就会变。
（这条满足"**诊断手段的输出不能依赖被诊断对象**"这条硬规矩 —— 正是它让排查一次拿到答案。）

交付正式固件时把 `App_Public.h` 的 `BOOT_CRUMB_ENABLE` 改成 `0` 即可全部去掉。

## 5.4 开机动画与"干净窗口"

`boot_animation()`：由屏幕中心向外扩散 65 帧、再收缩 64 帧，
每帧 `delay_ms(15)` + 一次整屏刷新（软件 SPI 约 8.5ms），共 129 帧 ≈ **3.0 秒**。

**为什么敢在动画期间"别的设备一律不许动"**：它被安排在全系统最干净的窗口里：

- `EA = 0` → 数码管扫描(Timer2)、1ms 时基(Timer3)、时钟中断(INT3) **一个都不会来**；
- LED 总开关还关着 → 8 颗灯**物理上**不可能亮；
- 蜂鸣器(PWM5)/马达(PWM6) 还没 Init，引脚已被 `*_SafeLevel` 按低；
- 数码管还没 Init（Timer2 没配、位选/段选没驱动）；
- 舵机信号脚已被 `Servo_SafeLevel()` 拉低；
- 副屏还没 Init，SSD1306 上电默认显示关（`0xAE`）→ 全黑。

⇒ 动画**结束之前**，任何别的设备都不会有反应。这是用户明确要求的。

**第 70 轮的一处清理**：`Display_Init()` 里原来又做了一遍
`SPI_OLED_Init()` + `ColorTurn` + `DisplayTurn` —— 与 `sys_init()` 第 3.5 步**完全重复**
（那一步里已经初始化过、还跑完了动画，而且 `SPI_OLED_Init()` 内部有 200ms 的稳定等待）。
删掉之后省约 **210ms** 开机时间。

---

# 第六部分 导航与界面系统

## 6.1 三级层级模型

```
第 0 级  大字时钟         ← 开机就在这（大号时间 + 日期 + 温湿度）
   │ K2
第 1 级  任务清单（7 项）  ← K1/K3 挪光标（到顶/到底回绕），K2 进入，K4 回第 0 级
   │ K2
第 2 级  任务内部          ← 页面栈；主屏仍画任务清单窗口、光标停在父任务那一行
   │ K4 逐级返回
```

`Menu_Current() == PAGE_HOME && Menu_IsOpen()` 用来区分第 0 级和第 1 级
（`menu_enter()` 第一句就把 `s_menuOpen` 清 0）。

## 6.2 页面栈

"像一摞盘子" —— 进一页往上摞，返回就拿掉最上面的。深度上限 `MENU_STACK_MAX = 4`。

| API | 行为 |
|---|---|
| `Menu_Push(page)` | 先 `menu_task_enter(page)`，再压栈，然后 `Display_SetPage + Refresh(1)` |
| `Menu_Back()` | 先 `menu_task_leave(当前页)`，再弹栈；落到 `PAGE_HOME` 时**停在任务清单**、且**光标停在刚离开的那个任务上** |
| `Menu_Home()` | **把栈里每一层都逐层 `leave`**，清到只剩 `PAGE_HOME` |

> **`Menu_Home` 为什么要逐层收尾**（真机踩过）：
> 在云台页被闹钟打断 → 跳到响铃页 → 停止后回主页。
> 此刻"当前页"是响铃页，**如果只关当前页，云台页被弹出去了却没人关它的舵机 PWM**，
> 舵机就一直挂在总线上（现象："没进云台页舵机也会动"）。

### 进入 / 离开钩子

**只有独占型外设才需要挂钩子**（云台 PWMA 通道、测距仪 TRIG/ECHO 两个引脚）：

| 页面 | `menu_task_enter()` | `menu_task_leave()` |
|---|---|---|
| `PAGE_SERVO` | `Servo_Init()`（**完整初始化**，不依赖上次状态） | `Servo_Off()`（关输出 + 拉低引脚） |
| `PAGE_RANGE` | `SR04_Init()` + `SR04_ResetFilter()` + 立刻测一次 | `SR04_Stop()`（拉低 TRIG） |
| `PAGE_HOME` | — | 退出"日期和时间"子模式 |
| `PAGE_GAME_HALL` | — | 清游戏状态 |

纯读传感器的页面（时钟、温湿度）**不需要** —— 它们的外设在 `sys_init()` 里配一次就够。

## 6.3 七个任务页逐页讲

> **编码口径**：屏上编号从 **1** 开始（`sprintf(..., k + 1, ...)`），
> 代码下标从 **0** 开始（`s_taskPage[]` / `s_taskName[]` 两张同序码表）。
> `Menu_IndexOf()` 是"页面 → 第几项"的反查（子页面归位到父任务）。

| 屏上编号 | 任务名 | 页面 | 主屏（SPI） | 副屏（I2C） |
|---|---|---|---|---|
| 1 | 日期和时间 | `PAGE_HOME`（子模式，**不压栈**） | 选项态两个选项 / 输入态当前选项 | 选项态：任务图标；输入态：已输入的年 / 月日 |
| 2 | 闹钟 | `PAGE_ALARM_LIST`（+`PAGE_ALARM_EDIT`） | 大字时间 + 字段名 + 值 | 列表态 3 组（选中靠左、其余靠右） |
| 3 | 番茄钟 | `PAGE_POMODORO` | 大字倒计时 + 项名 + 值 | 专注 / 休息 / 剩余 |
| 4 | 测距仪 | `PAGE_RANGE` | **只有两行引脚提示** `TRIG 接 P2.4` / `ECHO 接 P3.6` | `DISTANCE` / 距离值 / 10 格进度条 / 量程 |
| 5 | 云台 | `PAGE_SERVO` | **只有一行** `PWM 接 P2.5` | 半圆仪表盘 + 粗针 + `Angle nnn DEG` |
| 6 | 设置 | `PAGE_SETTINGS` | 列表态 4 行窗口 / 编辑态大字 | 当前项内容 |
| 7 | 游戏 | `PAGE_GAME_HALL` | 列表 / 游戏画面 / 结算 | 列表态亮（图标）；**游玩态关屏** |

### 「1 日期和时间」的两个状态

- **选项态**：主屏两个选项（改年月日 / 改时分），K1/K3 切换，K2 进入输入；
  此时副屏显示**任务图标**。
- **输入态**：只认矩阵键盘数字。**年月日 8 位 `YYYYMMDD`**、**时分 4 位 `HHMM`**，
  **输满自动提交**；K4 = **删最后一位**，删到"已输入位数 = 0"就取消修改、退回选项态
  （所以"输入 0000"也要按 4 次 K4 —— 有意的设计，单独记"已输入位数"计数器）。
- 提交时非法（13 月、25 时）→ 标"非法"，K4 删一位改正即可。
- **改完日期会自动算星期**（Sakamoto 算法），不用手动设周几。

### 「2 闹钟」的两个状态

- **列表态**：K1/K3 挪光标；**K2 短按 = 开/关当前这一组**；**K2 长按（约 2 秒）= 进编辑态**；
  矩阵键盘 `0/1/2` 直接跳到第 1/2/3 组。
- **编辑态**：5 个字段 `时 → 分 → 星期 → 开关 → 曲目`；K2 切下一字段（最后一个再按 = 保存返回）；
  K1/K3 值减/加；矩阵键盘数字**只在"时/分"字段有效**；K4 = 返回并保存。

> **闹钟时间是 `HH:MM`，没有秒** —— PCF8563 的硬件闹钟寄存器只有分/时/日/周，
> 不支持秒（要秒只能靠软件每秒比对）。

### 「7 游戏」的三个状态

| 状态 | 表现 |
|---|---|
| 0 列表 | 主屏列 3 个游戏 + 第 4 行 `K2 开始  K4 返回`；副屏亮（显示任务图标） |
| 1 游玩 | 主屏由**游戏自己**画（显示层在 `main_redraw()` 里直接 return）；**副屏自动关**（`I2C_OLED_DisplayOff()`）；独立按键全部屏蔽 |
| 2 结算 | 显示 `GAME OVER` + `SCORE:n`，停 3 秒后自动回列表 |

| 操作 | 矩阵键盘编号 |
|---|---|
| 上 / 左 / 下 / 右 | `9 / 12 / 13 / 14` |
| 飞机大战发射 | `13`（**与"下"同键**，代码里先判发射） |
| **强制退出** | `3`（**第一行第四个**，即右上角） |

**外壳驱动三个游戏**（`Game_Poll()`，在 `TASK_LOGIC` 里）：

| 宏 | 值 | 含义 |
|---|---|---|
| `GAME_FRAME_MS` | 30 | 打砖块 / 飞机，每帧 30ms |
| `GAME_STEP_MS_SNAKE` | 250 | 贪吃蛇每步 250ms（它每步走 8 像素） |
| `GAME_FIRE_CD_MS` | 3000 | 飞机发射冷却 3 秒 |
| `GAME_OVER_HOLD_MS` | 3000 | 结算画面停留 3 秒 |

**节拍全部用 `g_sysTick` 的毫秒差算，不数"任务被调用的次数"** ——
后者依赖 RTX51 的 tick 假设，节拍失准时会让游戏快得没法玩（真机踩过）。

### 移植三个游戏时做的机械适配

1. `#include "oled.h"` → `"SPI_OLED.h"`（参考工程画在副屏，本工程画在**主屏**）；
2. `Delay_ms` → `delay_ms`；
3. 游戏内全局名加各自前缀（`Snake_` / `Brick_` / `PlaneGame_`）避免撞名；
4. **删掉游戏自己的 `while(1) + os_wait2` 主循环**（被外壳取代）—— 留着会阻塞整个任务；
5. **贪吃蛇的链表改成静态节点池**（`SNAKE_MAX_LEN = 40`）——
   C51 上不用堆（会链入一大坨库代码、且 8KB xdata 碎片化不可控）。
   换成静态池之后必须自己 `snake_pool_reset()`（原来用 malloc 时天然有节点，
   漏了这一步就会 `snakeHead == NULL` ⇒ **"进游戏后蛇完全不动"**）。

## 6.4 双屏分工与刷新策略

| | 主屏（SPI） | 副屏（I2C） |
|---|---|---|
| 器件 | SPI OLED，**带硬件汉字字库 IC** | SSD1306，字库在 MCU 里（`oledfont.h`） |
| 能显示 | 汉字、图标、大字数字 | **只有 ASCII** |
| 汉字成本 | **不占单片机程序空间**（点阵在屏上的字库 IC 里，通过 P1.0/P1.1 读 ROM） | 不能显示（原来那套 7 个汉字字模全工程没人调用，第 70 轮删了） |
| 寻址 | 按**页**（16px 汉字占 2 页，写一行必须用**偶数页**） | `line * 2` 已在 `sub_draw_line()` 内部处理 |
| 行宽 | `main_draw_line_fit()` 补满 **128px** | `sub_draw_line()` 补满 **16 字符 = 128 列** |
| 图形 | 显存通路（`GRAM[1024]`） | 行缓冲 + 批量写 |
| 图标 | 7 张 **128×64 整屏**（`App_Icons.c`，各 1024 字节） | — |

> **"中文裁剪省 Flash"这条对本板不成立** —— 主屏走硬件字库，汉字不占程序空间。
> 程序空间真正的大块是**图标数据 7168 字节（占 13%）**。

---

# 第七部分 两块屏的渲染层

## 7.1 主屏（SPI，软件模拟 + 硬件字库）

**6 根线全为普通 IO**，驱动用 `for` 循环一位位翻转（约 1~3MHz）。

**★ 端口模式必须显式配成推挽**（原厂驱动和 v3.1 都没配）：

```c
P5_MODE_OUT_PP(GPIO_Pin_0);                            /* SCLK  */
P1_MODE_OUT_PP(GPIO_Pin_0 | GPIO_Pin_3 | GPIO_Pin_6);  /* 字库CS / MOSI / DC */
P4_MODE_OUT_PP(GPIO_Pin_7);                            /* 屏片选 */
/* P1.1 = MISO 保持不动（从字库 IC 读数据的输入线） */
```

**不配的后果**：停在复位默认的准双向口，输出高电平靠内部弱上拉（几十 kΩ）。
对按键那种慢信号够用，但 **SCL 周期只有几百纳秒**，弱上拉遇上线路电容
**上升沿爬不到高位门限** ⇒ SSD1306 收不到有效时序 ⇒ **主屏漆黑**。

> 这条很反直觉：**"逐字照抄原厂驱动"不等于正确** —— 要检查每种信号的电气要求。
> 对照：副屏的 I2C 两根线被显式配成开漏（板上有上拉），所以它一直是好的。

**汉字取字地址公式**（屏上字库 IC 的 ROM 地址）：

```
Address = ((MSB - 0xB0) * 94 + (LSB - 0xA1) + 846) * 32      /* 汉字 */
Address = 0x3CF80 + (ASCII - 0x20) * 16                       /* ASCII */
```

⇒ **源码必须是 GBK**：如果存成 UTF-8，一个汉字变 3 字节，上面这个公式算出来是垃圾地址 ⇒ 屏上乱码。

## 7.2 副屏（I2C，SSD1306）

- 硬件 I2C，400kHz（`I2C_Speed = 13`，依据 `Lib/I2C.h` 的宏注释：`总线速度 = Fosc/2/(Speed*2+4)`）；
- 与时钟芯片**共用** P3.2/P3.3；
- **只有 ASCII**（`asc2_1608[][16]` / `asc2_0806[][6]` 两套字模）；
- **不做全屏缓存**，按行写。

**★ 文字行必须固定宽度（补空格到 16 字符）**：
刷新粒度是"只重画变化的那一行"、不做整行清屏。新字符串比上一帧短的话，
旧内容的尾巴会留在屏上 —— 真机现象：`VOL 10/10 SONG 1`(15 字) 降到
`VOL 0/10 SONG 1`(14 字)，显示成 `VOL 0/10 SONG 11`（多出来那个 `1` 是残留）。

**★ 屏刷新成本的正确算法**：

| 路径 | 事务数 | 耗时 |
|---|---|---|
| 驱动原生的 `I2C_OLED_WR_Byte()` | **每字节一次完整事务**（START + 设备地址 + 控制字节 + 数据 + STOP ≈ 340µs） | — |
| **文字行**（`sub_draw_line` → `ShowString`，16 字 × 16 字节） | 256 次 | **≈ 87ms/行** |
| 副屏"画满 4 行文字" | 1024 次 | **≈ 350ms** |
| `I2C_OLED_Clear()`（已改批量写，一页一次事务） | 8 页 × 2 = 16 次 | ≈ 2ms |
| `sub_blit()`（图标，一页一次事务） | 8 页 × 2 = 16 次 | ≈ 5ms |
| `gauge_flush()`（仪表盘区域，批量） | 少量 | 很快 |

⇒ **估开销要按"事务次数 × 单次事务耗时"，不能按总线波特率的理论值算。**
本项目曾把整屏估成 3.5ms，实测 350ms，**差 100 倍** —— 这个低估直接导致了一个错误设计判断。

## 7.3 ★★ 主屏的两条**互不相容**的绘制通路

**这是本工程最容易踩的一个坑**（真机踩过：游戏结算画面全黑）。

| | **A. 直写屏通路** | **B. 显存通路** |
|---|---|---|
| 清屏 | `SPI_OLED_Clear()` | `SPI_OLED_GClear()`（清的是**显存**） |
| 画东西 | `SPI_OLED_Display_GB2312_string()`<br>`_16x16` / `_8x16`<br>`SPI_OLED_WR_Byte(..., DATA)` | `SPI_OLED_DrawPoint()` / `SPI_OLED_ClearPoint()`<br>`SPI_OLED_GBuf_8x16()` / `SPI_OLED_GBuf_string()` |
| 上屏时机 | **立即**（逐字节写进 OLED） | `SPI_OLED_Refresh()` 时**整屏**送出 |
| 谁在用 | **菜单 / 所有页面**（`main_draw_line_fit()` 走这条） | **三个游戏的 `Draw()`、开机动画** |

两条通路的**数据完全隔离**：直写屏不碰 `SPI_OLED_GRAM[1024]`，显存通路也不直接写 OLED。

**★ 混用的后果**：

```c
SPI_OLED_GClear();                                    /* 清的是显存 */
SPI_OLED_Display_GB2312_string(28, 2, "GAME OVER");   /* 直写屏 —— 显存里其实什么都没写 */
SPI_OLED_Refresh();                                   /* 把「空显存」整屏覆盖上去 */
                                                      /* -> 刚写的字被自己擦掉，画面全黑 */
```

**判据（一句话）：同一段代码里，清屏和画东西必须同属一条通路。**

**为什么要有 B 这一条**：图形类内容（蛇身、砖块、飞机、子弹）需要逐点绘制。
如果每个点都直接写屏，一是慢、二是会闪。显存通路把整帧画在内存里，最后一次上屏，
所以游戏画面不闪。

> 配套：`main_redraw()` 在**游戏态（含结算态）会在清屏之前直接 return**
> （`Menu_GameIsPlaying()` = `s_gameState != 0`）—— 游戏画面只归游戏自己管。

## 7.4 刷新策略与限流

| 策略 | 值 | 为什么 |
|---|---|---|
| 主屏整屏重画 | 软件 SPI，约 1.3ms | 便宜，清不清屏主要影响观感 |
| 主屏"要清屏"的执行点限流 | **100ms** | 防"请求洪流" |
| 副屏整屏重画 | 只在**切页**时做 | 4 行文字约 350ms 太贵 |
| 副屏运行期周期性整屏重画 | **没有** | 原来有（每 10 秒自愈），后来删了 —— 它是"某行闪乱码"的来源之一 |
| 主屏环境行（温湿度） | 每 **2 秒**限流重画 | `s_envTimer >= 400`（400 × 5ms） |
| 电位器事件 | 限流 **200ms** | 一条 `EVT_POT` 会触发一次 EEPROM 扇区擦写 + 一次整屏重画 |

**副屏切页的顺序**：**熄面板 → 清屏 → 画内容 → 再点亮**（`s_subOn` 状态位）。
面板黑着的时候把该清的清掉、该画的画完，用户看到的就是"整屏瞬间完整出现"，
看不到"清了还没画完"的中间过程。
**之所以现在敢这么做**：`I2C_OLED_Clear()` 已改成按页批量写（约 2ms；
原来一字节一次事务要 70~250ms —— 那么长的黑屏是不能接受的）。

> **注意**：`sub_redraw()` 里**不能用 `return` 提前退出**，否则末尾的"点亮"会漏掉。

---

# 第八部分 时间与闹钟（全项目唯一的真算法）

## 8.1 PCF8563 驱动（`Driver/PCF8563.c`）

**接口与真实签名**（照抄江文聪老师封装驱动，参数是**值传递**）：

```c
void PCF8563_init(void);
void PCF8563_get_clock(Clock_t *p_clock);   /* 一次 I2C 读 7 字节，内部做 BCD -> 十进制 */
void PCF8563_set_clock(Clock_t clock);
void PCF8563_set_alarm(Alarm_t alarm);      /* 硬件闹钟：分/时/日/周 */
void PCF8563_enable_alarm(u8 enable);
void PCF8563_int_call(void);                /* INT3 回调：清标志 + 调下面两个 */
extern void PCF8563_on_alarm(void);         /* 由 App 实现 */
extern void PCF8563_on_timer(void);
```

| 项 | 真值 |
|---|---|
| 设备写地址 | `0xA2`（`0x51 << 1`） |
| 时间寄存器首址 | `0x02`（秒/分/时/日/周/月/年，共 7 字节） |
| 闹钟寄存器 | `0x09` 分 / `0x0A` 时 / `0x0B` 日 / `0x0C` 周 |
| 控制寄存器 CS2 | `0x01`（bit3 = AF 闹钟标志，bit1 = AIE 闹钟中断使能） |
| 世纪位 | 月寄存器 bit7：0→20xx，1→21xx |
| 中断脚 / 触发沿 | P3.7 / **INT3 下降沿** |
| 时间格式 | BCD（"压缩版十进制"），读写都要转换 |

**修掉的三处参考代码缺陷**（都在《硬件真值表》§7 有记录）：

1. `PCF8563_set_alarm()` 的"周"分支判据写成了 `if(alarm.day < 0)`（应该是 `alarm.week`）；
2. `PCF8563_init()` 里配 I2C 引脚的 `GPIO_config()` 被注释掉了 ⇒ 直接不通；
3. `PCF8563_ALARM_ENABLE` 是 `DISABLE` ⇒ `PCF8563_int_call()` 函数体被 `#if` 整段裁掉，
   **闹钟中断从来没有被处理过**。

**中断里只置一位标志**（`g_rtcIrqFlag`），I2C 清标志留到 `TASK_LOGIC`。
原因：I2C 总线要和副屏共用。

## 8.2 时间服务（`App/App_Clock.c`）

**星期不信任芯片，每次自己算**（Sakamoto 算法，纯整数、无查表大数组）：

```c
u8 Clock_WeekdayOf(u16 year, u8 month, u8 day)
{
    static u8 code t[] = { 0, 3, 2, 5, 0, 3, 5, 1, 4, 6, 2, 4 };
    u16 y, sum; u8 m;
    y = year; m = month;
    if (m < 3) { y = y - 1; }                 /* 1、2 月算作上一年的 13、14 月 */
    sum = y + y/4 - y/100 + y/400 + t[m-1] + day;
    return sum % 7;                           /* 0=周日, 1=周一 ... 6=周六 */
}
```

**为什么自己算**：PCF8563 的星期寄存器只是一个 0..6 的计数器，
上电默认值不对，芯片自己也不知道"今天是周几"。

**分层（方案 B 样板）**：

| 函数 | 做什么 |
|---|---|
| `Clock_Set(Clock_t *t)` | **只登记**（置 `g_reqClockSet`），不碰 I2C |
| `Clock_SetNow(Clock_t *t)` | **真写芯片**（由 `TASK_RENDER` 调） |
| `s8 Clock_Refresh(void)` | 读一次；失败返回负错误码（0=成功） |
| `Clock_TryRecover()` / `Clock_TryRecoverNow()` | 时钟自救（只登记 / 真做） |
| `Clock_IsValid()` | 就是 `return s_valid;` |

**`Clock_Init()` 的三条规矩**：

1. **8 次重试，每次间隔 100ms**（共约 800ms 窗口）—— 因为 USB 插入瞬间 MCU 和芯片同时上电，
   芯片内部（晶振起振、电源稳定）需要几百毫秒才应答 I2C。
   （原来三次重试之间没有任何等待，等于三次都在同一时刻试，必然一起失败。）
2. **读出来无效时才写一次默认值（2026-01-01 00:00:00）**。
   《04》的原话是"时间不要每次开机都重设，芯片靠小电池维持走时" ——
   那条规则的前提是"芯片里已经有一个有效时间"。实机读回来的原始字节是
   `sec=0x21 min=0x20 hr=0x00 day=0x00 mon=0x00 yr=0x00`（秒在走，但日/月/年全是 0），
   不写的话 `Clock_Refresh()` 会因"月份=0"永远判非法 ⇒ 时间冻结 ⇒ 闹钟的时间基准是坏的。
   **两条规则不冲突**：只在"读出来无效"时写。
3. **`Clock_TryRecoverNow()` 每 5 秒试一次、最多 60 次**，成功后自动恢复（`s_valid` 为真就退出）。
   放弃是为了避免无限重试一直擦写芯片。

## 8.3 ★ 闹钟调度算法（`App/App_Alarm.c`，全项目唯一的真算法）

### 核心难点

**硬件只有一套闹钟寄存器，但软件上有 3 组闹钟**（每组还能选"周几响"）。怎么办？

### 解法：两层配合 + 一个去重键

**第一层（硬件）**：每次闹钟改动或响铃结束后，从 3 组里挑出
"**时间上最近、还没响的那一组**"，把它的（分、时）写进芯片。
- 若该组是"每天响" → 禁用芯片的周字段；
- 若是"指定周几" → 填那一天。
⇒ 这样"P3.7 + INT3 + 下降沿"这条硬件唤醒链是**真实在用的，不是摆设**。

**第二层（软件兜底）**：`TASK_LOGIC` 每秒（`Alarm_Tick1s()`）把当前时:分和 3 组比一遍，
命中且今天该响、这一分钟还没响过 → 补触发。
⇒ **即使硬件闹钟因为星期计数、温漂、纽扣电池掉电等原因漏触发，也不会漏响。**

**去重键**：两层共用同一个记录 —— `s_lastRingWom[ALARM_MAX]`（见下）。

### "周内分钟序号"（wom）—— 算法的巧处

```c
/* g_clock.week: 0=周日..6=周六 -> 0=周一..6=周日 */
static u8 monday_index(void) { return (u8)((g_clock.week + 6) % 7); }

/* 当前时间 -> "本周从周一 00:00 起的分钟序号"，范围 0..10079 */
static u16 now_wom(void)
{
    return (u16)((u16)monday_index() * 1440U + (u16)g_clock.hour * 60U + (u16)g_clock.minute);
}
```

- 把"周几 + 时 + 分"压成**一个整数**（0..10079 = 7×1440-1）；
- 于是"哪一组最近"变成**整数比较**，没有跨月/闰年/跨年的麻烦；
- **去重**：记住"这一组上次响的是哪个 wom"，同一个 wom 不再响
  ⇒ **"同一分钟只响一次"天然成立，跨天也自动复位**；
- 初始值填 `0xFFFF`（一个不可能的分钟序号）表示"还没响过"。

### 组数相关的定义

```c
#define ALARM_MAX  3     /* 用户要求："闹钟只要三个，多了任意分不清" */
typedef struct {
    u8 enable;   /* 0=关 1=开 */
    u8 hour;     /* 0-23 */
    u8 minute;   /* 0-59 */
    u8 days;     /* DAY_xxx 掩码，bit0=周一 .. bit6=周日 */
    u8 song;     /* 0=用全局设置，1..3=指定曲目 */
} AlarmItem_t;
```

## 8.4 响铃、贪睡、日出唤醒、自动静音

| 功能 | 参数 | 实现 |
|---|---|---|
| 响铃 | — | 按"提醒方式"设置：`Music_PlayLoop()`（蜂鸣器放曲子，循环）或 `Motor_On()`（震动） |
| **贪睡** | 默认 **9 分钟**（`SNOOZE_MINUTES`，可在设置页改 1~30） | `Alarm_Snooze()`：记下剩余秒数 + 是哪一组；期间主界面照常能用 |
| **自动静音** | **120 秒** | 一直没人理就自动静音，但**界面仍停在响铃页**（不把闹钟标记成"已处理"） |
| **日出唤醒** | 闹钟响前 **600 秒（10 分钟）** 开始，每 **100ms** 调一级 | `Alarm_TickSunrise()`（由 `TASK_MUSIC` 每 100ms 调）算出"距下次响铃还有多久"，映射成亮度 `255 - 255*remain/600`（纯整数），驱动 8 颗灯 |

**响铃时键位单独定义、不过按键置换**：**K2 = 停止**，**K4 = 贪睡**，其余键无反应。
（响铃是紧急状态，键位要跟平时不一样。）

---

# 第九部分 存储

## 9.1 EEPROM 分区（4KB @ `0xF000`，8 个 512 字节区）

```c
#define EE_BASE         0xF000U     /* 64KB 从末尾往前划 4KB */
#define EE_PAGE_SIZE    512U        /* 最小擦除单位 */
#define EE_SETTINGS     0xF000U     /* 0 号区：设置 + 闹钟 */
#define EE_GAME_SAVE    0xF200U     /* 1 号区：游戏存档（至今未写） */
#define EE_LOG_BASE     0xF400U     /* 2 号区起：温湿度记录 */
#define EE_LOG_PAGES    6U          /* 2..7 共 6 个区 */
#define EE_LOG_CAPACITY (EE_PAGE_SIZE / 2U * EE_LOG_PAGES)   /* = 1536 条 */
```

**0 号区布局**（512 字节内）：偏移 0 = `Settings_t`（约 20 字节）；
偏移 20 = `AlarmItem_t[3]`（每组 5 字节，共 15 字节）；其余保留。

## 9.2 设置与闹钟的"原子写"

```c
u8 Storage_SaveAll(void)
{
    /* 1) 先在内存里把整页内容拼好（s_page0[64]，放 xdata） */
    s_page0[0] = (u8)(g_settings.magic & 0xFF);
    s_page0[1] = (u8)(g_settings.magic >> 8);
    s_page0[2..13] = version / volume / alert_mode / song /
                     pomodoro_work / pomodoro_rest / screen_on /
                     snooze_min / sunrise_en / logCount(低高) / vibrate;
    for (i = 14; i < OFF_ALARMS; i++) { s_page0[i] = 0; }   /* 补齐到偏移 20 */
    for (i = 0; i < ALARM_MAX; i++) {                       /* 5 字节一组 */
        s_page0[OFF_ALARMS + i*5 + 0] = g_alarms[i].enable;
        s_page0[OFF_ALARMS + i*5 + 1] = g_alarms[i].hour;
        s_page0[OFF_ALARMS + i*5 + 2] = g_alarms[i].minute;
        s_page0[OFF_ALARMS + i*5 + 3] = g_alarms[i].days;
        s_page0[OFF_ALARMS + i*5 + 4] = g_alarms[i].song;
    }

    /* 2) 一次擦除 + 一次写回（只写 35 字节，不必写满 512） */
    EEPROM_SectorErase(EE_SETTINGS);
    EEPROM_write_n(EE_SETTINGS, s_page0, (u16)(OFF_ALARMS + ALARM_MAX * 5));
    return ST_OK;
}
```

**为什么设置和闹钟放同一区**：一次擦除、一次写回，中间**不会出现"擦了没写"的窗口**
（最小擦除单位是 512 字节，做不到"只擦这 35 字节"）。

> **★ 第 70 轮修掉的一个真实缺陷**：`Storage_Load()` 里把 `reserved` 清零的循环写的是
> `for (i = 0; i < 4; i++)`，而 `Settings_t.reserved` 只有 **3** 字节 ——
> 第 4 次循环写的是 `g_settings` **之后紧邻的那个字节**，
> 属于"静默踩到别的全局量"（踩到谁取决于链接顺序，改代码就变）。
> `Storage_Default()` 里写的是正确的 `i < 3`，两边本来就不一致。**已改成 `< 3`。**

## 9.3 温湿度记录：顺序填充 + 只在进新页时擦

```c
u8 Storage_AppendLog(s8 temp, u8 humi)
{
    if (humi > 100) { humi = 100; }                  /* 钳位，保证 != 0xFF */
    seq  = g_settings.logCount;
    addr = log_addr(seq);                            /* EE_LOG_BASE + (seq % 1536) * 2 */

    offsetInPage = (u16)((addr - EE_LOG_BASE) % EE_PAGE_SIZE);
    pageAddr     = (u16)(addr - offsetInPage);
    if (offsetInPage == 0) { EEPROM_SectorErase(pageAddr); }   /* 只在页首擦 */

    buf[0] = (u8)((s16)temp + 100);                  /* 整体加 100，避开 0xFF */
    buf[1] = humi;
    EEPROM_write_n(addr, buf, 2);

    g_settings.logCount++;
    if ((g_settings.logCount % 32U) == 0U) { Storage_SaveAll(); }   /* 每 32 条回写指针 */
}
```

**三个设计点**：

1. **温度整体加 100 再存**：`s8` 的 `-1` 编码是 `0xFF`，会和"空位标记"撞车。
   `-55..125` 变成 `45..225`，永远不会出现 `0xFF`；读的时候减回去。
2. **空位标记 = `0xFF`**：擦除态就是 `0xFF`，而湿度不可能等于 255（已钳到 100）。
3. **擦除频率**：只在"正好要写某一页的第 0 条"时擦这一页 ⇒
   **每 256 条才擦一次**（约 42 小时一次），寿命完全不是问题；页内续写不擦 ——
   这正好利用了"擦除态就是 `0xFF`，可以直接按字节写"这一点。

**写指针 `logCount` 只在"每 32 条"或"设置变更"时回写 0 号区**：
掉电最多丢 32 条的指针（下次从旧位置继续写会覆盖最近几条，但不会破坏更早的数据）；
若 10 分钟擦一次 0 号区，10 万次寿命只够两年。

## 9.4 版本与默认值

```c
#define SETTINGS_MAGIC    0x414C     /* "AL" */
#define SETTINGS_VERSION  3
```

`Storage_Load()` 的校验：魔数、版本、`volume <= VOLUME_MAX`、`vibrate <= 100`、
`alert_mode <= ALERT_VIBRATE`、`pomodoro_work` 在 1~99、`logCount <= 1536`；
闹钟逐项兜底（`enable>1` / `hour>23` / `minute>59` / `days == DAY_NONE` / 有越界位
⇒ 这一组判无效、关掉并改成工作日）。
**任何一项不过 ⇒ 填默认值并返回 `ST_ERR_MAGIC`。**

**出厂默认值**（`Storage_Default()`）：

| 项 | 默认 |
|---|---|
| 音量 | **1%** |
| 震动强度 | **25%** |
| 提醒方式 | 铃声 |
| 默认曲目 | 0（第 1 首） |
| 番茄钟 | 25 分钟专注 + 5 分钟休息 |
| 主屏 | 开 |
| 日出唤醒 | **开** |
| 贪睡时长 | **9 分钟** |
| **3 组闹钟** | **全部关闭**（每组默认 07:00、工作日） |
| 温湿度记录 | 从 0 条开始 |

> **改结构就 +1**：`SETTINGS_VERSION` 每改一次 `Settings_t` 或 `ALARM_MAX` 就加一，
> 让 `Storage_Load()` 判定为"不是本版本"、重新填默认值。
> **代价：旧设置会重置一次**（这是有意的取舍）。
> 本项目加过两次：音量 0~10 改 0~100 + 新增"震动强度"（+1）、`ALARM_MAX` 8 改 3（+1）。

---

# 第十部分 输入系统

## 10.1 四个独立按键（`Driver/Keys.c`）

**状态机（每个键独立）**：

```
稳定抬起 --(原始电平变低)--> 记录变化时刻
原始电平持续 20ms 都是低 --> 判定"稳定按下"，记按下时刻，回调 on_keydown
按住累计 >= 2000ms 且还没报过长按 --> 回调 on_longpress，置"已报长按"标记
原始电平持续 20ms 都是高 --> 判定"稳定抬起"，若没报过长按则回调 on_keyup
```

- **短按在"抬起"时上报，长按在"按满 2 秒"时上报** ⇒ 两者不互相污染、长按也不会连发；
- 全部消抖/计时都用 `g_sysTick` 算差值，**不用 delay 硬等**；
- 初始化时**用上电时的真实电平作为初始状态**，避免把上电电平误判成一次按下。

**比参考工程强的地方**：v3.1 的 `Keys_scan()` **只做边沿判断、完全没有消抖**，
手一抖就是一次"按下"。

## 10.2 4×4 矩阵键盘（`Driver/MatrixKey.c`）

- 外循环把第 `row` 行拉低（其余行拉高），内循环读 4 个列电平；
- 拉低行之后 `NOP2()` 等信号稳定再读；
- **扫完 4 行全部拉高恢复原状**（那几个端口的引脚还有别的用途）；
- 按键编号 `pos = row * 4 + col`（0..15）。

**本工程的改进：先拼 16 位再统一比较**：

```c
static u16 s_raw = 0xFFFF;      /* 16 位原始电平，1 = 抬起 */
static u16 s_stable = 0xFFFF;
static u16 s_changeMs[MK_KEY_COUNT];

/* 采样：逐行拉低读列，拼成 u16 rawNow */
/* 用 s_raw ^ rawNow 找出变化的位，按位记录变化时刻 */
/* 再逐位判 20ms 稳定，产生 on_keydown / on_keyup */
```

好处：**消抖时刻可以按位记录**，代码也短一半。（v3.1 是"逐位比较"。）

## 10.3 旋钮（电位器，`App/App_Input.c`）

| 参数 | 值 | 依据 |
|---|---|---|
| 引脚 / ADC | P0.5 / `ADC_CH13` | v3.1 |
| **满量程** | **约 2740（不是 4095）** | demo30 的 `PWM6_Duty = PERIOD * adc / 2740` |
| 档位 | **0~100（1% 步进，共 101 档）** | 用户要求"每百分之一步进" |
| 采样 | 连读 **8 次取平均**，**第一次转换丢掉** | TASK_INPUT 读通道 13、TASK_SENSOR 读通道 12，切通道后采样保持电容还没充到新电压 |
| 防抖 | 原始值变化 ≥ **60** 才认一次拧动 | |
| **事件限流** | **200ms** | 见下 |

**为什么必须限流**：每一条 `EVT_POT` 在 `App_Menu` 里会触发
① 一次 `Storage_SaveAll()`（一次 EEPROM 扇区擦写，几十毫秒，还带 `EA=0`）
② 一次 `Display_Refresh(1)`（一次约 350ms 的整屏重画）。
真机数据表明：5 秒内做了 16 次整屏重画，16 × 350ms = 5.6 秒的 I2C 传输挤进 5 秒窗口
⇒ 总线 100% 饱和 ⇒ 抢不到锁 ⇒ 超时强夺 ⇒ 两个任务同时发字节 ⇒ **控制器被写坏（黑屏）**。
**所以从源头把事件频率限住。**

**档位换算（第 70 轮朴素化过）**：

```c
static u8 pot_level_of(u16 raw)          /* 原始 ADC 值 -> 档位 0..100 */
{
    u32 lvl = ((u32)raw * POT_LEVELS) / POT_ADC_FULL;   /* 用 u32：4095*101 超 u16 */
    if (lvl >= POT_LEVELS) { lvl = POT_LEVELS - 1; }
    return (u8)lvl;
}
/* 判"动过一截"用 diff 取绝对值，不用两个对称分支 */
if (raw > s_potMark) { diff = (u16)(raw - s_potMark); }
else                 { diff = (u16)(s_potMark - raw); }
if (diff < POT_RAW_STEP) { return; }
```

## 10.4 事件队列（`App/App_Input.c`）

```c
static Event_t xdata s_queue[EVT_QUEUE_SIZE];   /* 16 条 */
static u8 s_head, s_tail, s_count;

static void evt_push(u8 type, u8 param)
{
    if (s_count >= EVT_QUEUE_SIZE) {
        /* 队列满：丢最旧的一条，保证最新的操作不丢 */
        s_head++; if (s_head >= EVT_QUEUE_SIZE) { s_head = 0; }
        s_count--;
    }
    s_queue[s_tail].type = type;
    s_queue[s_tail].param = param;
    s_tail++; if (s_tail >= EVT_QUEUE_SIZE) { s_tail = 0; }
    s_count++;
}
```

**事件类型**（`App_Public.h`）：

| 宏 | 值 | 含义 |
|---|---|---|
| `EVT_KEY1`..`EVT_KEY4` | 1..4 | 4 个独立键**短按** |
| `EVT_KEY1_LONG`..`EVT_KEY4_LONG` | 5..8 | 4 个独立键**长按** |
| `EVT_MATRIX` | 9 | 矩阵键盘，`param = 0..15`（`row*4+col`） |
| `EVT_POT` | 10 | 旋钮，`param = 0..100` |

> 计划版是"`EVT_MATRIX_0..F` 共 16 个类型"，实际改成"**一个类型 + 参数**"。

## 10.5 ★ 按键有"两层"：物理键 → 逻辑键

**硬件事实**：4 个按键接 P5.1~P5.4，驱动层按引脚顺序报 `KEY1..KEY4`。
**用户要的键位**：K1=上移、K2=确认、K3=下移、K4=返回 —— 和引脚顺序不一致。

**做法**：在 `App_Menu.c` 里做**一次全局置换**（`key_remap()`）：

```c
EVT_KEY1 -> EVT_KEY3        EVT_KEY2 -> EVT_KEY1
EVT_KEY3 -> EVT_KEY4        EVT_KEY4 -> EVT_KEY2
（长按同理）
```

⇒ **代码里看到的 `case EVT_KEY3:` 对应的是板上的 K1，不是 K3。**
这条在《硬件真值表》§9.4 和《02-技术方案》§6.3 都被专门标出来，因为它最容易看错。

**为什么要在这一处翻译**：如果逐个 case 去改（要动 5 个页面、十几个分支）必然漏一处；
一处翻译，所有页面自动跟着走。
**位置很关键**：置换必须放在"第 0/1 级处理**之后**" ——
上面是新写的代码（用新编号），下面是旧代码（用旧编号）。

**例外**：响铃是紧急状态，键位单独定义、**不过置换**。

---

# 第十一部分 声音与反馈

## 11.1 蜂鸣器（`Driver/Buzzer.c`，P0.0 / PWM5 / PWMB 组）

| 项 | 值 |
|---|---|
| 音阶表 | `FREQS[21]`：低八度 523~988、中八度 1047~1976、高八度 2093~3951 Hz |
| 音高 | 改周期 = `period = MAIN_Fosc / hz`，写 `period - 1` |
| 响度 | 改占空比。`BUZZER_DUTY_MAX_PCT = 50`（50% 最响） |
| 音量口径 | **0~100 百分比**（`BUZZER_VOLUME_FULL = 100`） |
| 占空比算式 | `duty = period * 50 * volume / (100 * 100)`（**纯整数**，用 u32 中转防溢出） |
| 换音顺序 | **先关输出 → 改周期 → 改占空比 → 再开输出**（否则会听到"啪"的爆音） |
| 停止 | `PWMB_CC5E_Disable()` + `BUZZER = 0` |
| 提示音频率 | `BUZZER_BEEP_HZ = 1047`（C5） |
| 总开关 | `BUZZER_ENABLE = 1`（当前**已开启**） |
| 频率下限 | 文档说 ≥ 367Hz；**代码里没有下限检查**，实际下限由调用方保证（`FREQS[]` 的值域 + `BUZZER_BEEP_HZ`） |

**占空比为什么从浮点改成整数**：v3.1 写的是 `(u16)(period * 0.02f)`。
**只要算式里出现 `float`，C51 就会把整个浮点库 `C51FPL.LIB` 链进来**（约 1.8KB）。
改成 `period / 50`（等价于 `period * 2%`）即可。

## 11.2 非阻塞音乐引擎（`App/App_Music.c`）

**核心：整个推进过程不等待**（放曲子时不挡其它任务）。

```
TASK_MUSIC 每 5ms：
  · Music_Tick()        —— 看表判断该不该换音符，从不调用任何延时
  · Motor_Tick()        —— 震动计时心跳（按"5ms 一拍"换算）
  · 每 20 次（100ms）   —— Alarm_TickSunrise()
```

**状态**：`s_song / s_volume / s_idx / s_noteMs / s_noteStart / s_playing / s_loop /
s_gapPhase / s_gapMs / s_beeping / s_beepStart / s_beepMs`。

**★ 断音（"音符之间要留一点静音"）** —— 这是修"音调慢的有拖音"的关键：

原来音符之间**没有任何断音**（上一个音一直响到下一个音开始的那一瞬间）。
方波蜂鸣器没有衰减，于是：
- **同音相邻**的音符会被连成一个长音（最典型的是《两只老虎》的 `3 1 | 1 2 3`：
  句尾的 `1` 和下一句开头的 `1` 合成一个两拍长音，乐句分界消失，节奏整个塌掉）；
- 不同音之间也没有"字头"，听着黏成一片。

⇒ 主观感受就是"音调慢 / 拖音"（其实是**没有断音**，不是速度问题）。

**解法很朴素：每个音符只响 7/8，留 1/8 静音当断音。**
两段加起来正好还是 `ms` ⇒ 不影响速度，只是把音符切开、给出字头。休止符本来就是静音，不用再切。

```c
if (tone == REST) { s_gapMs = 0;       s_noteMs = ms; }
else              { s_gapMs = ms / 8;  s_noteMs = ms - s_gapMs; }
```

**提示音（`Music_Beep(u16 ms)`）**：
- 正在放曲子时**不打岔**（提示音只是辅助，不该盖住闹铃）；
- 音量 0 时也给一个**最低可闻档**（3），否则用户会以为"没响"；
- **非阻塞**：记下起始时刻，`Music_Tick()` 到点关掉。

> **★ 第 70 轮修的一处不一致**：清理 `s_beeping` 的动作原来只在 `Music_Play()` 里做，
> `Music_PlayLoop()`（**响铃走的就是它**）没做。后果：提示音还在计时的时候闹钟响，
> `Music_Tick()` 会先进提示音分支，到点 `Buzzer_Stop()` —— **把刚起的第一个音符掐掉**。
> 已把清理动作统一放进 `music_start()`（两个入口都经过它）。

## 11.3 三首曲子（`App/App_Songs.c`）

| # | 曲名 | 备注 |
|---|---|---|
| 1 | 两只老虎 | 音符序列与拍数**照抄教材**（v3.1 的 `Buzzer_2tiger`），只是把"拍数"换算成毫秒 |
| 2 | 小星星 | — |
| 3 | 马兰开花二十一 | **按童谣的节奏型和歌词断句编配的简易版**（这首童谣各地唱法不一，没有唯一版本） |

- 音符格式 `{音高 1..21, 时长(ms)}`，`0` = 休止；
- 八度前缀宏 `L1..L7` / `M1..M7` / `H1..H7` / `REST`；
- **节奏常量**：

```c
#define BASE_Q      300         /* 四分音符 */
#define BASE_E      100         /* 八分音符 */
#define BASE_QD     300         /* 附点四分音符（= 四分，见下） */
#define BASE_H      400         /* 二分音符 */
```

> **★ 这里有一处"注释与代码不符" + 一处乐理问题（第 70 轮发现、只改了注释）**：
> 原来的注释写"现在整体减半：BASE_Q 400→200、BASE_E 200→100、BASE_QD 600→300、BASE_H 800→400"，
> 但**代码里的实际值是 300 / 100 / 300 / 400**（`BASE_Q` 是 300 不是 200）。
> 已按实际值改写注释。
> 另外 `BASE_QD`（附点四分）现在**等于** `BASE_Q`（四分）= 300ms，
> 而附点四分音符按定义应是四分的 **1.5 倍（450ms）** ——
> 现在的实际听感是"该拖长的地方没拖长"。
> **这条没改值**（改它会改声音），要修就改 `BASE_QD`，但**必须先听一遍再定**。

## 11.4 振动马达（`Driver/Motor.c`，P0.1 / PWM6 / PWMB 组）

| 项 | 值 |
|---|---|
| 频率 | `MOTOR_PREQ = 1000` ⇒ `MOTOR_PERIOD = MAIN_Fosc / 1000 = 24000` |
| 占空比 | `duty = MOTOR_PERIOD * level / 100`（**震动强度 0~100**） |
| 与蜂鸣器 | **同属 PWMB 组、共用周期** ⇒ `Motor_On()` 里**先 `Buzzer_Stop()`** |
| 上电安全 | **P0.1 复位后是准双向口 + 输出 1 = 高电平震** ⇒ `Motor_SafeLevel()` 必须是 `sys_init()` 第一句 |
| 定时震动 | `Motor_Vibrate(ms, level)`：换算成"5ms 拍数"，由 `Motor_Tick()` 递减到 0 自动停 |
| 反馈最低档 | `level == 0` 时给 10（否则用户按了**完全没有感觉**）——与 `Music_Beep` 的"音量 0 也给最低可闻档"同理 |
| 显式调用 | `Motor_On()` / `Motor_Off()` 会**清掉"定时自动停"的状态**（否则闹铃正在震动时，之前某次 `Motor_Vibrate` 的计时到点会把闹铃一起关掉） |
| 总开关 | `MOTOR_ENABLE = 1`（当前**已开启**） |

> **★ 一个曾经走过的错误方向（已否掉，别再重复）**：
> 曾怀疑"马达百毫安负载拉低 3.3V 导致 OLED 花屏" ——
> **不成立**：整段日志里没有 `[MOTOR] Motor_On()` 记录（马达从未被启动）。
> **以后不要把花屏归到马达上。**

## 11.5 八颗小灯（`Driver/LED.c`）

| 项 | 值 |
|---|---|
| 引脚 | LED1~8 = P2.7 / P2.6 / P1.5 / P1.4 / P2.3 / P2.2 / P2.1 / P2.0 |
| 总开关 | `LED_SW` = **P4.5**，**低电平导通（打开）** |
| 极性 | **低电平点亮** |
| 模式 | 推挽 |

**两个用途**：① **日出唤醒**渐亮；② **上电进度指示 `BOOT_CRUMB`**。

**"渐亮"怎么实现**：8 颗灯全是普通 GPIO，**没有独立 PWM 通道**
（LED 引脚的 PWM 备选通道属于 PWMA（舵机占 PWM3）和 PWMB（蜂鸣器占 PWM5、马达占 PWM6），
都腾不出来）。所以用"**一颗接着一颗点亮**"来近似：

```c
void Led_Breath(u8 level)                     /* level 0..255 -> 点亮 0..8 颗 */
{
    u8 count = (u8)(level >> 5);              /* 0..7，用移位代替除法 */
    u8 i;
    if (level >= 224) { count = LED_COUNT; }  /* 补边界，让"全亮"能取到 8 */
    for (i = 0; i < LED_COUNT; i++) { led_write(i, (i < count) ? 1 : 0); }
    s_ledCount = count;
}
```

**为什么用 switch 逐个写而不是整口掩码**：LED1~8 是分布在 P1/P2 上的 `sbit`，
C51 里 `sbit` 不能按下标访问；而且"位掩码 + 端口整体改写"容易误改同一端口上别的外设引脚。
（参考工程的 `LED.c` 就是在这上面出过 bug：注释里的 `P2 |= 0xCF` 掩码与引脚定义不吻合。）

## 11.6 两个总开关的语义

| 宏 | 当前值 | 说明 |
|---|---|---|
| `BUZZER_ENABLE` | **1** | 0 = 整个蜂鸣器通道静音（`Buzzer_Play()` **整个函数体被 `#if` 包着**，只调 `Buzzer_Stop()`，不做任何 PWM 操作） |
| `MOTOR_ENABLE` | **1** | 0 = 不驱动马达 |

> **注意"`#if` 包住整个函数体、而不是提前 `return`"** ——
> 否则编译器会把后面的代码判成 unreachable 并报 `warning C294`。

**提示音的现状**（这条容易记错）：原来有 **21 处** `Music_Beep` 调用
（页面切换、开关闹钟、按键输入…），2026-09-21 用户要求精简，**现在只剩设置页的 4 项**
（改音量 / 改曲目试听 / 改提醒方式 / 改震动强度）。
闹钟响铃、番茄钟到点的那些是**功能本体**，删不掉。

---

# 第十二部分 传感器

## 12.1 NTC 热敏电阻（`Driver/NTC.c`，P0.4 / `ADC_CH12`）

**算法：查表法（全整数）**。热敏电阻的电压和温度不是直线关系，讲义给了对照表
（`temp_table[181]`，下标 0 对应 -55℃，逐行带注释）：

```
原版（v3.1，浮点）                     本工程（整数）
ntc_V = adc * 2.5 / 4096        ->    mv    = adc * 2500 / 4096          [毫伏]
ntc_R = ntc_V * 10 / (3.3-ntc_V) ->   rX100 = mv * 1000 / (3300 - mv)    [百分之一千欧]
search_temp(ntc_R * 100) - 55   ->    search_temp(rX100) - 55
```

**溢出核算**：`adc` 最大 4095 ⇒ `adc * 2500 = 10,237,500`（需 u32 中转）；
`mv` 最大 2500 ⇒ `mv * 1000 = 2,500,000`（需 u32）；分母 `3300 - mv` 最小 800，不会除零。

**查表**：`search_temp()` 是"线性找最小绝对差"（与 v3.1 的算法一致）——
表只有 181 项，线性扫描够快，而且**不依赖表的单调性**。

**滤波**：最近 4 次采样的滑动平均（先顺序填满 4 格，之后环形覆盖；
平均值只用"已填充"的格数算，避免刚上电被历史里的 0 拉低）。

> **★ 一个真机修正**：这里原来 `return (s16)(sum / s_filled)`，返回的是"整数摄氏度"，
> 但函数名和调用方约定的是"摄氏度 ×10"，于是 29℃ 被当成 2.9 显示成 `T2.9C`。
> 查表本身只有整数精度，所以现在就是**把整数度乘 10**（`* 10`），不损失信息。

## 12.2 DHT11 温湿度（`Driver/DHT11.c`，P4.6 准双向口）

**时序（照抄 v3.1 的判据形状）**：

1. 主机拉低总线 ≥ 18ms（用 `os_wait2(K_TMO, 4)`，**这一段不关中断**，让 CPU 给别人用）；
2. 主机释放总线，等从机响应；
3. **从"等响应"开始关中断**（"捂耳朵"，窗口约 **4.6ms**），读完 40 位再恢复；
4. 连续读 5 字节（40 位）+ 校验（前 4 字节之和的低 8 位 == 第 5 字节）；
5. 解析：`dat[0]`=湿度整数、`dat[2]`=温度整数、`dat[3]` 低 7 位=温度小数（最高位 1 表示零下）。

**★ 关键修法：0/1 的判据改成"比高低电平长度"**

原来照抄参考代码、按微秒写死窗口（`[70,88]` / `[74,92]`），真机报 `err = -4`
（= `DHT11_ERR_RESP_LOW`）。
根因：参考代码的 `delay_1us()` 是 `NOP8()`（8 个机器周期 = 0.33µs @24MHz），
但一次循环还要加函数调用、`u16` 自增，实际约 **1.2~1.3µs** ——
**"一次计数"和"一微秒"之间有一个未知换算系数**，按微秒写死的窗口必然偏
（按 1.25µs 估，83µs 的响应低电平只会数到约 66，低于下限 70）。

改成：每位先量低电平计数 `lowCnt`（固定约 54µs），再量高电平计数 `highCnt`
（bit0 约 24µs、bit1 约 71µs），**`highCnt > lowCnt` 判为 1**。

```
24/54 = 0.44  与  71/54 = 1.31  分居 1.0 两侧，余量足够
且完全不需要知道"一次计数等于多少微秒"
```

响应阶段只做**宽松的合理性检查**（`[10, 250]`，挡住"没接传感器"）。

**★ 另一处修法：等电平时必须带超时**
参考代码的 `wait_level_change` 宏是 `while(DHT == level){ … }` —— **循环里没有超时判断**，
传感器没插好时会**死循环**，整个程序再也回不来。
本工程改成 `wait_level(level, min, max, &cnt)`：**超过上限就不再等**，返回错误码。

**关中断的代价（可接受）**：
- 系统时钟少走 4 格 / 30 秒 = **0.013%**；
- 数码管暂停刷 4ms（一位本来停留 1ms），肉眼看不出闪。

## 12.3 采集调度（`App/App_Sensor.c`，TASK_SENSOR 每秒一拍）

```c
void task_sensor(void) _task_ TASK_SENSOR
{
    /* 上电先等 1 秒：屏幕初始化要做 I2C/SPI 通信，
       头一秒就跑 DHT11 会关中断 4.6ms，容易把屏幕初始化打断 */
    os_wait2(K_TMO, 200);
    Sensor_Init();
    while (1) { Sensor_Tick1s(); os_wait2(K_TMO, 200); }
}

u8 Sensor_Tick1s(void)
{
    u8 wrote = 0;
    Sensor_ReadOnce();                       /* NTC + DHT11 */
    s_logTimer++;
    if (s_logTimer >= SENSOR_LOG_INTERVAL) { /* 600 秒 = 10 分钟 */
        s_logTimer = 0;
        Storage_AppendLog((s8)(g_tempX10 / 10), g_humiValid ? g_humi : 0);
        wrote = 1;
    }
    return wrote;
}
```

**失败处理**：读失败沿用上一次的有效值；连续失败 **5 次**（`HUMI_FAIL_LIMIT`）
把 `g_humiValid` 置 0，界面据此显示 `--`（**显示的事交给 `App_Display`，本模块不碰屏幕**）。

---

# 第十三部分 外接模块

## 13.1 超声波测距（`Driver/HC_SR04.c`，TRIG=P2.4 推挽 / ECHO=P3.6 高阻输入）

### 最终方案：照抄上课代码的"软件延时数循环"

```c
void Delay10us(void)    /* @24.000MHz */
{
    unsigned char data i;
    i = 78;
    while (--i);        /* Keil 编成紧凑的 DJNZ，3 时钟/圈，78 圈 = 234 时钟 = 9.75µs ≈ 10µs */
}

static s8 sr04_read_raw(u16 *out)      /* 0=成功  -1=等起跳超时  -2=高电平超时 */
{
    SR04_TRIG = 1; Delay10us(); Delay10us(); SR04_TRIG = 0;   /* 触发脉冲 20µs */

    cnt = 0;
    while (SR04_ECHO == 0 && cnt < SR04_EDGE_CNT_MAX) { cnt++; Delay10us(); }   /* 等起跳，5ms 超时 */
    if (cnt >= 500)  { *out = cnt; return -1; }

    cnt = 0;
    while (SR04_ECHO == 1 && cnt < SR04_ECHO_CNT_MAX) { cnt++; Delay10us(); }   /* 数高电平，30ms 超时 */
    if (cnt >= 3000) { *out = cnt; return -2; }

    *out = cnt; return 0;
}
```

> **不要改 `i` 的值、也不要把它改成 `static`**：
> `i = 78` 是 STC-ISP 按 24MHz 算出来的；不加 `static` 是为了保留 `LCALL`/`RET` 的固定开销，
> 时序与上课代码一致（加了 `static` 编译器可能内联，时序会变）。

### ★ 系数为什么是 5.60 计数/厘米（不是上课代码的 5.88）

上课代码写 `距离 = ((cnt × 0.01) × 34) / 2 = cnt × 0.17`，隐含 **5.88 计数/厘米**，
即它认为 1 个计数 = 10µs。按 STC8H 官方手册把编译出的汇编**逐条数时钟**，实际是 **10.50µs**：

```
一次 Delay10us 调用 = LCALL(2) + 78 × DJNZ(direct, 3 时钟) + RET(2) = 238 时钟 = 9.92µs
"数回波"循环每迭代额外 = JB/JNB P36(1) + CLR C(1) + MOV A,R7(1) + SUBB(1)
                       + MOV A,R6(1) + SUBB(1) + JNC(1) + INC R7(1) + CJNE(3) + SJMP(3) = 14 时钟
合计 = 252 时钟 = 10.50µs @24MHz
```

⇒ 1cm 需要 `58.82 ÷ 10.50 = ` **5.60** 个计数 ⇒ `厘米 = 计数 × 5 ÷ 28`
（`SR04_CM_NUM = 5` / `SR04_CM_DEN = 28`）。

**交叉验证**：触发脉冲 = `SETB` + 两次 `Delay10us` = 1 + 2×238 = **477 时钟 = 19.9µs**，
而上课代码注释写的就是"给个 2 个 10us"= 20µs —— 吻合。

沿用 0.17 会**偏小约 5%**（20cm 量出来 19cm）。5% 对"避障"够用，对"量距离"就该修。

### 计时方案的三次修正（完整记录）

| 版本 | 做法 | 真机现象 | 根因 |
|---|---|---|---|
| v1 | 软件延时数循环，但延时函数用 **NOP 拼**出来，把 1 个 tick 当 1µs | "能测但不太准"（约偏小一半） | 每 tick 实际约 **1.96µs**（NOP 延时本身不准 + 循环里还有读引脚、u16 比较、函数调用开销） |
| v2 | 改用 Timer4，`T4_Load(0)` 清零后读**绝对计数** | "读数偏大 3 倍，越测越大"（15cm 显示四五十；移到 1cm 还是 40） | **`T4H/T4L` 是自动重装值，不是计数器**。运行中写它**不会清计数器** ⇒ 残余值逐次累加 |
| v3 | Timer4 + 只取两个时刻差值 `t1 - t0` | 理论正确 | — |
| **v4（定稿）** | **照抄上课代码**：`Delay10us` 用 `i = 78; while(--i);`；超时阈值 500 / 3000；状态码 0/-1/-2；**系数按手册精算修正为 5.60** | 真机跑通 | v1 的病根是"延时函数不准"，上课代码的延时函数是准的；而且**不占定时器、不需要"清零计数器"这件事** |

### 其他参数与行为

| 项 | 值 |
|---|---|
| 量程 | **2 ~ 400cm**（`SR04_CM_MIN` / `SR04_CM_MAX`） |
| 滤波 | **5 点滑动中值滤波**（跨调用滑动窗口，不是一次连测 5 次） |
| 为什么对"计数"而不是对"厘米"滤波 | 换算是线性比例，先滤波再换算**少一次整数截断**，更准；而且计数是原始量，滤波不会掩盖换算问题 |
| 排序 | 最朴素的**插入排序**（只有 5 个元素，不值得引库函数） |
| 测距节流 | **150ms 一次**（`SR04_Measure()` 是**阻塞**的） |
| 阻塞时间 | 常见 2~100cm：0.2~7ms；400cm：约 24ms；模块没插：最坏约 40ms（靠两个超时兜住，**绝不会死等**） |
| 中断 / 定时器 | **一个都不用** |
| 离页面 | `SR04_Stop()`：只把 TRIG 拉低（本版本不用定时器，没有"停表"动作） |

> **用 u16 而不是 u32 的一个正面示范**：`cm100 = (u16)(raw * 5)` ——
> `raw` 最大 3000，`× 5 = 15000` 远小于 65535，不会溢出。
> 用 u32 会链入 `?C?LIMUL` / `?C?ULDIV` 两个 32 位乘除库函数（几十字节 Flash + 慢得多）。

## 13.2 舵机云台（`Driver/Servo.c`，P2.5 = PWM3N / PWMA 组 / 50Hz）

| 项 | 值 |
|---|---|
| 引脚 / 通道 | **P2.5** / PWM3**N**（`PWM3_USE_P24P25()` + `ENO3N`） |
| 组 | **PWMA**（与 PWMB 的 ARR/PSCR 是两组独立寄存器） |
| 频率 / 周期 | 50 Hz；`SERVO_PRESCALER = 10`、`SERVO_PERIOD = MAIN_Fosc/10/50 = 48000` |
| 角度 → 脉宽 | `pulse_us = 500 + angle * 2000 / 180`（范围 **500~2500µs**） |
| 占空比寄存器值 | `duty = SERVO_PERIOD * pulse_us / 20000`（一帧 20ms = 20000µs） |
| 按键步长 | 每次 **10°**（`SERVO_KEY_STEP`）；K2 = 回中位 **90°** |
| 默认角度 | 90°（中位） |
| 进入 / 离开 | **上电不初始化**；只有进"云台"页才 `Servo_Init()`，离开时 `Servo_Off()` |
| ★ 上机结论 | **P2.5 能单独输出 50Hz**（云台功能真机跑通） |

**★ 三个真机修正**：

1. **`Servo_Off()` 之后必须把引脚按低**：关掉 PWM 输出后 P2.5 变回普通 IO，
   而它的输出锁存器复位值是 1，又被配成了推挽 ⇒ 这根线**一直卡在高电平**。
   对舵机来说"信号线一直是高"相当于一个超长脉宽 ⇒ **甩到极限位置**。
   真机现象就是"人不在云台页里，舵机偶尔自己转一下"。用 `P2 &= (u8)(~0x20);` 清 bit5。
2. **`Servo_Init()` 每次都完整执行，不要用"已初始化"标志短路**：
   原来用 `s_inited` 做了短路 ⇒ `Servo_Off()` 把输出关掉之后，
   第二次进云台**输出再也开不回来**。真机现象："第一次进云台能调角度、P2.5 有波形；
   退出再进就调不动、没波形"。修法：完整重配（`PWM_Configuration(PWM3, ..., ENO3N)` 会重新使能输出）。
3. **不要"先配成占空比 0、再单独设真实角度"**：
   两次之间有一段"**占空比 = 0**"的窗口 —— 对舵机就是"信号线上没有脉冲"，
   有些舵机会回中或抖一下。现在把目标占空比**直接算好、一次配置到位**（见下面的 `servo_fill_cfg`）。

**★ 结构体每个字段都要填满**（`servo_fill_cfg()`）：

```c
static void servo_fill_cfg(PWMx_InitDefine *c, u16 period, u16 duty, u8 eno)
{
    c->PWM_Mode          = CCMRn_PWM_MODE1;
    c->PWM_Period        = period;
    c->PWM_Duty          = duty;
    c->PWM_DeadTime      = 0;
    c->PWM_EnoSelect     = eno;
    c->PWM_CEN_Enable    = ENABLE;
    c->PWM_MainOutEnable = ENABLE;
}
```

理由：局部结构体没赋值的字段 = **内存垃圾**，会被库函数原样写进寄存器，
而且它的值随代码布局变化（"以前好用、改完就坏"）。

## 13.3 ★ 为什么舵机不能用 P0.2（完整论证）

用户问过两次（2026-09-19、2026-09-23），**两次答案都是"不行"**。

### 理由 1：PWM7 属 PWMB 组，与蜂鸣器/马达共用周期寄存器

`Lib/STC8H_PWM.h` 里 PWM5/6/7/8 的引脚切换宏都是 `PWMB_PS` 系列 ——
**四个通道共用同一个 `PWMB_ARR`**。

### 理由 2：蜂鸣器每次换音都会重写这个周期寄存器

`Buzzer_Play()` 里 `period = MAIN_Fosc / hz` ⇒ `PWMB_AutoReload(period - 1)`。

### 理由 3：蜂鸣器/马达和舵机**必然要"同时使用"**

把全部调用点核了一遍：

| 发声 / 震动场景 | 位置 | 能不能删 |
|---|---|---|
| **闹钟响铃**（蜂鸣器放曲子） | `Alarm` 走 `Music_PlayLoop` | **不能** —— 这是闹钟的本体 |
| **闹钟响铃**（震动模式） | `Motor_On` | **不能** |
| **番茄钟专注结束 / 休息结束** | `Music_Play` | **不能** —— 这是番茄钟的本体 |
| 设置页改音量 | `Music_Beep(100)` | 可删（用户要保留） |
| 设置页改曲目（试听） | `Music_Play` | 可删（用户要保留） |
| 设置页改提醒方式 | `Music_Play` / `Motor_Vibrate` | 可删（用户要保留） |
| 设置页改震动强度 | `Motor_Vibrate` | 可删（用户要保留） |

⇒ **前三行是功能本体、删不掉**。所以"把提示音限制在设置页"这个办法**解决不了冲突**：
只要闹钟会响，`PWMB_ARR` 就一定会被改。

### 理由 4（最关键）：闹钟响铃会破坏舵机，**而且响铃结束后不会自愈**

```
云台页调好角度（PWMA 侧 50Hz 正常）
  ↓ 闹钟到点
Music_PlayLoop → 每个音符 Buzzer_Play(hz)
  → PWMB_AutoReload(音高周期)          ← 如果舵机在 PWMB，它的"周期"就被改了
  → 舵机收到频率错、脉宽也错的信号      ⇒ 乱转 / 抽搐 / 甩到极限
  ↓ 响铃结束
Buzzer_Stop() 只 PWMB_CC5E_Disable()（关通道输出）
  → 不会把 PWMB_ARR 恢复成 50Hz
  ⇒ PWMB_ARR 停在最后一个音符的周期上
  ⇒ 舵机持续收到错误信号，不会自己恢复
  ⇒ 只有退出云台页再进来（重新 Servo_Init()）才恢复正常
```

而且这个破坏是**单向**的：蜂鸣器每次都自己 `PWMB_AutoReload`，所以不受影响；
**只有舵机受害**（舵机不会去恢复 ARR）。

### 结论

**用 P2.5（PWM3N / PWMA）**。只有当"板子上舵机接口物理固定在 P0.2、无法改线"时才考虑
"舵机改软件 PWM 占 Timer4"这条路；"关掉两个总开关"相当于**用两个功能换一个引脚**，不划算。

---

# 第十四部分 上位机通信协议

## 14.1 走哪条线

**UART1**（P3.0 RxD / P3.1 TxD），115200-8-N-1，经板上 CH340N 接 USB。
**同一个口既下载又通信。**

## 14.2 帧格式

```
+------+------+------+------+-----------+------+
| 0xAA | 0x55 | LEN  | CMD  | DATA[..]  | SUM  |
+------+------+------+------+-----------+------+

LEN = CMD + DATA 的字节数（不含帧头与校验）
SUM = (LEN + CMD + DATA 各字节之和) 的低 8 位
```

- 单帧最长 45 字节 < RTX51 的 25ms 时间片 ⇒ 阻塞发送是安全的；
- 接收缓冲 `RX_FRAME_MAX = 48`。

## 14.3 十一条命令

| CMD | 名称 | 请求 DATA | 回复 DATA |
|---|---|---|---|
| 0x01 | 读时间 | — | 年H 年L 月 日 周 时 分 秒（8） |
| 0x02 | 校时 | 年H 年L 月 日 时 分 秒（7） | 状态（1） |
| 0x03 | 读设置 | — | 音量 提醒方式 曲目 专注 休息 屏开关 贪睡 日出（8） |
| 0x04 | 写设置 | 同上 8 字节 | 状态（1） |
| 0x05 | 读全部闹钟 | — | 3 × {开关 时 分 星期掩码 曲目}（**15**） |
| 0x06 | 设一组闹钟 | 序号 开关 时 分 星期掩码 曲目（6） | 状态（1） |
| 0x07 | 删一组闹钟 | 序号（1） | 状态（1） |
| 0x08 | 读温湿度 | — | 温度H 温度L 湿度 有效标志（4） |
| 0x09 | 读记录条数 | — | 条数H 条数L（2） |
| 0x0A | 读第 n 条记录 | 序号H 序号L（2） | 温度 湿度（2） |
| 0x0B | 读版本 | — | 主 次 修订（3） |

**状态**：`0x00` = 成功，`0x01` = 失败。
**星期掩码**：bit0=周一 … bit6=周日；`0x1F` = 工作日，`0x7F` = 每天，`0x00` = 不响。

**例子（读版本）**：

| 方向 | 字节 | 算法 |
|---|---|---|
| 发送 | `AA 55 01 0B 0C` | `LEN = 1`（只有 CMD）；`SUM = (0x01 + 0x0B) & 0xFF = 0x0C` |
| 收到 | `AA 55 04 0B 01 00 00 10` | `LEN = 4`；DATA = 主1 次0 修订0；`SUM = (0x04+0x0B+0x01+0x00+0x00) & 0xFF = 0x10` |

## 14.4 并发与边界处理

**帧解析状态机**：`ST_HEAD1 → ST_HEAD2 → ST_LEN → ST_BODY → ST_SUM`。

- `LEN == 0` 或 `> RX_FRAME_MAX` ⇒ 拒绝，回 `ST_HEAD1`；
- `SUM` 校验不过 ⇒ 整帧丢掉；
- **接收缓冲的处理方式（值得学）**：先**快照 `RX_Cnt`**，处理快照范围内的字节，
  最后把已处理的字节**搬走**（`RX1_Buffer[i] = RX1_Buffer[n + i]`）而不是粗暴清零 ——
  这样**不会丢掉处理期间并发到达的字节**；
- 一次最多处理 32 个字节，避免一次占用任务太久。

> **★ 第 70 轮删掉的一处重复调用**：`UC_SET_TIME` 里原来在 `Clock_Set()` 之后
> 又调了 `Alarm_ApplyHardware()`。它是多余的：`Clock_Set()` 已经把 `g_reqClockSet` 置 1，
> 而 `TASK_RENDER` 的 `Display_Poll` 在处理 `g_reqClockSet` 时本来就紧跟一句
> `Alarm_ApplyHardwareNow()` —— 再多调一次只会让硬件闹钟被白写两遍（多一份 I2C 流量）。
> （`App_Menu.c` 的 `dt_commit()` 里有同样的重复，一起删了。）

---

# 第十五部分 工程化：编译、验证、体积

## 15.1 编译环境与命令

```bash
# 全量重编译（可脚本化，适合"每批改动后对体积"）
"D:/Develop/Keil_v5/UV4/UV4.exe" -r Project.uvproj -j0 -o build_log.txt
# 必须看到：0 Error(s), 0 Warning(s)
#           Program Size: data=.. xdata=.. code=..
```

**工程设置**：设备 STC8H8K64U，Large 内存模型，Optimize 8，`uAC6 = 0`（用 C51 而非 AC6）。

**STC-ISP 下载时的两个开关（都极易忘）**：

1. **EEPROM 大小 = 4KB** —— 设错了会出现"编译能过、下载后跑不起来"；
2. **取消勾选**"下次下载用户程序时擦除用户 EEPROM 区" ——
   不取消的话，**每次下载都会清空已存的闹钟、设置和温湿度记录**。

## 15.2 三重验证

| 层 | 怎么看 | 必须满足 |
|---|---|---|
| **编译** | `build_log.txt` | `0 Error(s), 0 Warning(s)` |
| **链接** | `.m51` 里搜 `UNRESOLVED` | **= 0** |
| **尺寸与库** | `.m51` 里 `Program Size` + 搜 `FPMUL/FPDIV/CASTF/C51FPL` | 尺寸在红线内；**浮点引用 = 0** |

**★ 尺寸要按"真实上限"判断**（见 §1.4）。

## 15.3 逐模块体积账本

见 §3.4 的两张表（CODE 前 10 / XDATA 前 5）。

**统计方法**：`.m51` 的段表每行形如

```
            CODE    3A88H     03ACH     UNIT         ?CO?APP_DISPLAY
            XDATA   0404H     03FDH     UNIT         ?XD?APP_DISPLAY
```

末列 `?XX?YYYY?MODULE` 里的 `MODULE` 就是归属模块，
把同一模块的 `LENGTH` 列相加即可。**光看总数不够，要知道是"谁"吃掉的。**

## 15.4 两轮减重

| 轮次 | 做什么 | 结果 |
|---|---|---|
| **开发收尾（系统减法）** | 关掉不用的 UART2/3/4；删 `NVIC.c` 里 26 个未调用函数；清掉全部调试 `printf` 及其附属变量；裁 `PWM_Configuration` 的 8 个单通道分支；裁 `Timer_Inilize` / `GPIO_Inilize` 未用分支；清空死代码文件 | 约 **−12%（7939 字节）** |
| **第 70 轮（全工程通读减重）** | 删 **40 个**"定义+声明都在、全工程零调用"的函数；删死变量、死逻辑、无人用的字模表；修 1 处真实缺陷 + 2 处重复调用；注释全部对齐代码 | **−3404 字节** |

**最紧的时候程序空间只剩 318 字节（0.5%）。** 第 70 轮之后 `code = 55535 / 61440`（余 9.6%）。

**第 70 轮删掉的 40 个死函数，最典型的四类**：

| 类 | 例子 | 为什么没人用 |
|---|---|---|
| 一整个原厂接口套件（副屏） | `I2C_OLED_ShowNum` / `ShowChinese` / `DrawBMP` / `pow` / `Display_On/Off`，**以及一整套软件 I2C**（`I2C_Start` / `Send_Byte` / `I2C_WaitAck` / `I2C_Stop` / `IIC_delay`） | 驱动早就改用硬件 I2C 了，这些只剩 `#if 0` 里的旧实现引用它们 |
| 一整个原厂接口套件（主屏） | `SPI_OLED_Display_128x64` / `_5x7` / `_string_5x7` / `ShowNum` / `RefreshPart` / `GFill` | 本工程只用"直写字符串"和"显存画点"两条路 |
| 备用接口 | `Nixie_SetDigit/SetBuffer/Clear/ScanStop`、`Led_AllOff/AllOn/GetCount`、`Buzzer_IsEnabled`、`Motor_IsOn`、`Timers_GetSystemMs`、`Keys_IsPressed`、`UART_FrameCount/ErrorCount`、`PCF8563_set_timer/enable_timer/clear_timer/clear_alarm`、`I2C_IsLocked`、`Input_PendingCount` | 写了但没派上用场 |
| **一个 49 行的算法函数** | `Clock_AddSeconds()`（五层嵌套进位） | 番茄钟走自己的倒数、贪睡走 `Alarm_Snooze`，**它一次都没被调用过** |

> **配套发现**：删掉那些函数之后，`s_failStreak` / `s_hwAlarmHit` / `s_frames` / `s_errors`
> 这几个"只写不读"的计数器就彻底没有读者了 —— 它们本来就是给已删的 `printf` 用的。
> **死代码常常是成串出现的。**

## 15.5 源码编码与行尾（硬规矩）

| 类别 | 数量 | 编码 | 行尾 |
|---|---|---|---|
| `.c` / `.h` | 68 | **GBK**（**纯 GBK 可表示**，禁用 GB18030 四字节区字符） | 同文件内不许混 |
| `.md` 文档 | 22 | **UTF-8** | LF 或 CRLF，同文件内一致 |

**为什么源码必须 GBK**：主屏的中文串是**按字节算字库 ROM 地址**的
（`((MSB-0xB0)*94 + (LSB-0xA1) + 846)*32`）。存成 UTF-8 的话一个汉字 3 字节，
算出来的地址是垃圾 ⇒ 屏上乱码。

**为什么不能用 `⇒ ⚠ ↔ ⇄ −` 这类字符**：它们属于 **GB18030 四字节区**。
`encode('gb18030')` 能写进去，但 `encode('gbk')` 会失败，Keil 编辑器会显示成乱码；
最危险的是万一漏进 `sprintf` 的字符串里，上面那个地址公式会算出**垃圾地址**。

**替换表**：`⇒`→`->`、`⇄`/`↔`→`<->`、`−`→`-`、`⚠`→`※`。
**写盘前必须断言**（不能"先用 gb18030 写、再单独校验" —— 先写盘就晚了）。

**★ 一个容易搞错的点**：一个工程里可以有两种编码。
自动探测编码的工具要**先试 UTF-8 严格解码**；反过来"先试 gb18030"会把
UTF-8 文档误判成 GBK（GB18030 解码极其宽容），**写回时整体转码**。

## 15.6 批量改代码的纪律（脚本化）

改的量大时必然写脚本。以下是本项目反复踩过、现已固化的规矩：

| # | 规矩 | 踩过的坑 |
|---|---|---|
| 1 | **一次只做一类改动**，每批单独编译并记体积 | "改多了"会让新问题掩盖老问题，白多花好几轮 |
| 2 | 替换用"**精确串替换 + 断言旧串恰好出现 1 次**" | 用"已改就跳过"的幂等判据会误判 —— 旧文本里本来就可能含判据串，于是跳过替换，编译报"未定义标识符" |
| 3 | **注释块替换的区间不要跨到函数体内** | 本次把"函数签名 + 左花括号"一起吃掉、新文本又忘了补 `{`，编译报一串 `error C132 / C141` |
| 4 | 删函数的正则要容忍多种定义写法，并**打印删到了几个** | 同一个工程里函数写法不止一种（花括号是否同行、行尾是否粘注释、返回类型带不带 `const`）；**漏删不报错，只表现为"体积没降"** |
| 5 | **不要用 `re.sub` 的非贪婪 `/*.*?*/` 去注释** | 会被字符串字面量打乱配对。要**逐行跟踪 `/* */` 与 `//` 状态** |
| 6 | 定位函数体**必须跳过前置声明** | `text.index('void F(void)')` 会命中前置声明，导致花括号配对取到别的函数 |
| 7 | 写源码前**先探测编码与行尾** | 往 CRLF 文件插 LF 片段 ⇒ 行尾混合；按错编码写回 ⇒ 中文全乱 |
| 8 | 批量改完**跑一遍综合校验** | 校验四件事：行尾一致性 / 编码纯净性 / 已删符号残留 / 关键改动点回读 |

## 15.7 "移除一个源文件"的正确做法

**要"从工程移除某个 `.c`"：不要改 `.uvproj`，要把该文件内容清空成注释。**

因为 **Keil 的 UV4 命令行编译会把工程的 `<File>` 列表回写 `.uvproj`**，
手工删掉的节点会被它恢复。本项目实测过：删了、编译也过了，做完一圈别的事再看 —— **它又回来了**。

清空文件内容（保留节点）后：编译不产生任何代码，而且 Keil 动不了它。
`App_Game.c` 和 `Lib/Soft_I2C.c` 都是用这个办法处理的。

## 15.8 交付物的"版本同步"纪律

一个跑了几十轮迭代的项目，最大的文档风险不是"写错"，而是**"数字过时"**。
本项目一共 22 份 markdown，同一件事（程序空间余量）在不同文档里同时存在
**5 个不同的值**（`65218` / `58064` / `58869` / `58424` / `55535`）——
因为每次改完代码只刷了其中一两份。

**推荐做法**：

1. **挑一份文档当"数字的唯一出处"**（本项目是《交付说明》第一节），
   其它文档一律写"见《交付说明》第一节"，不重复贴数字；
2. 需要贴的地方，**在数字旁边写上"实测日期 + 测法"**；
3. 每次交付前用脚本扫一遍所有文档里的关键数字（旧 `code` 值、旧组数、旧档位、已删函数名）；
4. 文档和源码**编码可能不同**（`.c/.h` GBK、`.md` UTF-8），批量改文档时别用错写入编码。

---

# 第十六部分 踩坑史（按主题归类）

> 这一节按"你会遇到什么问题"组织，不按时间。每条都给了**现象 → 根因 → 修法**。

## 16.1 硬件 / 引脚类

| # | 现象 | 根因 | 修法 |
|---|---|---|---|
| 1 | **主屏漆黑** | 6 根软件 SPI 线**没配端口模式**，停在准双向口；弱上拉在 1~3MHz SCL 下上升沿爬不到门限 | `SPI_OLED_Init()` 里配推挽（MISO 除外） |
| 2 | **上电马达震** | P0.1 复位后是准双向口且输出 1 = 高 | `Motor_SafeLevel()` 做 `sys_init()` 第一句 |
| 3 | **没进云台页舵机偶尔自己转** | `Servo_Off()` 关掉 PWM 后 P2.5 变回普通 IO，锁存器是 1 且配了推挽 ⇒ 一直卡在高电平 | 关输出后 `P2 &= ~0x20;` 拉低 |
| 4 | **第二次进云台就没反应、没波形** | `Servo_Init()` 用 `s_inited` 短路，而 `Servo_Off()` 已把输出关掉，再也开不回来 | 进页面**完整初始化**，不用状态标志短路 |
| 5 | **调角度时舵机抖一下再转过去** | 原来"先配成占空比 0、再单独设角度"，中间有一段"没有脉冲"的窗口 | 目标占空比一次配置到位 |
| 6 | **数码管一位都不亮** | `Nixie_ScanInit()` 定义了但**全工程无人调用** | `sys_init()` 第 5 步调它 |
| 7 | **USB 冷插入时数码管与副屏时间都不走，按重启就好** | I2C 经典故障：从机在电源爬坡时被"半个时钟"卡住，一直拉着 SDA | `I2C_BusRecover()`：9 个时钟脉冲 + 补一个 STOP |
| 8 | 怀疑"马达负载拉低 3.3V 导致花屏" | **假设不成立**（日志里马达从未启动） | 别再重复这个方向 |

## 16.2 库函数 / 结构体类

| # | 现象 | 根因 | 修法 |
|---|---|---|---|
| 9 | **1ms 节拍变成 217ms**（按键无反应、时间冻结、闹钟不响） | 配 Timer3 时**漏赋 `TIM_PS`**，库函数 `TM3PS = TIMx->TIM_PS` 把内存垃圾写进预分频寄存器 | 显式 `TIM_PS = 0`；并做全量结构体审计 |
| 10 | 同上第二、三处 | `COMx_InitDefine` 漏 `Morecommunicate`；`I2C_InitTypeDef` 漏 `I2C_SL_ADR`/`I2C_SL_MA` | 逐字段填满 |
| 11 | **I2C 可能永远卡住** | 原厂 `Wait(){ while(!(I2CMSST&0x40)); }` **无超时** | 加计数上限 10000 |
| 12 | 传 `delay_ms(400)` 变成 144 | 形参是 `u8`（1~255） | 本项目延时统一走 `g_sysTick` 或 `NOP` 宏，从根上避开 |
| 13 | 副屏全乱码 | 误以为 `display_8x16` 的第三个参数是字符串 | 它是**点阵指针**（`u8 *dp`） |

## 16.3 并发 / 共享资源类

| # | 现象 | 根因 | 修法 |
|---|---|---|---|
| 14 | **副屏花屏 / 黑屏，MCU 重启都救不回来** | 两个任务靠一把"超时强夺"的锁轮流访问同一 I2C 控制器 ⇒ 字节流错位 ⇒ 数据字节被当成控制字节 | **所有 I2C 收进 `TASK_RENDER` 一个任务**（25 轮迭代后的定论） |
| 15 | 花屏后长期不恢复 | 自愈只重写显存，修不了 SSD1306 的**控制器寄存器** | 控制器初始化只在开机做一次；运行期不重初始化 |
| 16 | 全黑（比花屏更糟） | 自愈时重跑 `I2C_OLED_Init()`，它第一条命令是 `0xAE`（关显示），跑不完就停在"已关闭" | 同上 |
| 17 | **板子自动重启** | **热的 printf 把 RTX51 任务栈踩穿**（`FREE_STACK` 仅 20 字节），走 `?RTX_STACKERROR` 复位 | 热路径不许放 printf；诊断改用"直接写 IO 的进度指示" |

## 16.4 显示类

| # | 现象 | 根因 | 修法 |
|---|---|---|---|
| 18 | **游戏结算画面全黑**（"GAME OVER" 和分数都看不到） | **两条绘制通路混用**：`GClear()` 清的是显存，`Display_GB2312_string()` 是直写屏，紧接着 `Refresh()` 把空显存整屏覆盖 | 结算画面也走显存通路（`GClear` + `GBuf_string` + `Refresh`） |
| 19 | 副屏某行"瞬态闪乱码" | 单次写入被任务切换打断的残余概率 | 架构改造（收进一个任务）后从"永久损坏"降级为"瞬态自愈"；**再靠"行写入补满 16 字符固定宽度"进一步减少** |
| 20 | 副屏行尾残影（`VOL 0/10 SONG 11`） | 新字符串比上一帧短，旧内容尾巴留在屏上 | 行写入补空格到 16 字符满宽 |
| 21 | 副屏整屏"刷新不完整、下边有残留" | "清了还没画完"的中间过程被看到 | 顺序改成 **熄面板 → 清屏 → 画内容 → 再点亮** |
| 22 | 副屏整屏要 350ms，太慢 | 驱动**每写 1 字节发一次完整事务** | 整页批量写（`I2C_WriteNbyte` 一次写 128 字节）⇒ 16 次事务 ≈ 5ms |
| 23 | 挪光标"卡一下再跳一格" | 整屏重画的执行点带 100ms 限流，连累了主屏（只花 1ms） | **限流只对"要清屏"的执行点生效** |
| 24 | 主屏中文变乱码方块 | 源码被 UTF-8 编辑器改过 | 转回 GBK 重编译 |

## 16.5 时序 / 计算类

| # | 现象 | 根因 | 修法 |
|---|---|---|---|
| 25 | 超声波读数只有真实值的一半 | "1us 延时"实际约 **1.96µs**（NOP 拼的延时不准 + 循环开销） | 照抄上课代码的 `while(--i)`（编成 DJNZ，3 时钟/圈） |
| 26 | 超声波读数**越测越大**（15cm 显示四五十） | 用 `T4_Load(0)` 想清计数器 —— 但 **`T4H/T4L` 是自动重装值不是计数器**，残余值逐次累加 | 改用"先判断再减"或"两次读值相减 `t1 - t0`"；最终整体改回软件延时 |
| 27 | 超声波读数固定偏小约 5% | 上课代码隐含的系数是 5.88 计数/cm（它按"1 计数 = 10µs"估），实际是 10.50µs/计数 | 按手册精算改成 **5.60 计数/cm**（`cm = cnt × 5 / 28`） |
| 28 | NTC 温度显示成 `T2.9C`（29℃ 被当成 2.9） | 原来返"整数摄氏度"，但函数名与调用方约定的是"℃×10" | 返回前 `* 10`（查表本身只有整数精度，不损失信息） |
| 29 | DHT11 一直报 `err = -4` | 按微秒写死的窗口照抄了参考代码，而"一次计数"≠"1 微秒"（实际约 1.2~1.3µs） | 判据改成"**比高低电平长度**"（`highCnt > lowCnt`），与时间单位无关 |
| 30 | DHT11 模块没插时**程序卡死** | 参考代码的 `while(DHT == level)` 循环**没有超时** | `wait_level()` 加上限，超时就返回错误码 |
| 31 | 贪吃蛇"进游戏后完全不动" | 链表换成静态节点池后，`snake_pool_reset()` 没被调用 ⇒ `snake_head == NULL`，读到地址 0 的垃圾 | 在 `Snake_Init()` 里先 reset |
| 32 | 贪吃蛇"无法移动"（其实是一进去就结算） | 每步走 8 像素，30ms 一步 = 264 像素/秒，从屏幕中间到右墙只需 0.24 秒 | 贪吃蛇单独用 **250ms/步**，出生点挪到左侧 |
| 33 | 飞机"打着打着不会开火了" | `y -= 4` 后判 `y < 0` —— `y` 是 `u8`，`0-4` 回绕成 252，判据恒不成立 ⇒ 子弹永不失效、弹位占满 | **先判断"再走一步会不会越界"**，越界就直接释放弹位 |
| 34 | 一发子弹能连续打穿多个敌机（白送分） | 命中后清了 `active` 但**没有 `break`**，内层循环继续跑完 | 命中就跳出内层循环 |
| 35 | 打砖块卡在一条竖线上来回弹 | 挡板正中命中时 `(x - paddle)/4 == 0` ⇒ 球变纯上下运动，左右再也不推进 | 保证"至少偏向一侧 1 像素" |

## 16.6 流程 / 工具链类

| # | 现象 | 根因 | 修法 |
|---|---|---|---|
| 36 | **"删了某个 `.c` 能编译过，过一会儿它又回工程里了"** | UV4 命令行编译会把工程的 `<File>` 列表**回写 `.uvproj`** | 不要改 `.uvproj`，把文件内容**清空成注释** |
| 37 | **判断"某函数有没有被用到"时 grep 骗人** | ① 库函数同名；② 注释里提到；③ `grep` 把 GBK 文件当二进制 | **编译一次看 `UNRESOLVED`** 才是硬证据（本项目因此误删过 `Delay.c`） |
| 38 | 删完一批代码体积一点没降 | 正则漏删（函数定义写法不止一种） | 脚本打印"删到了几个"并核对 |
| 39 | 批量替换后编译报 `error C132 / C141` | 替换区间跨到了函数体内，把 `函数签名 + {` 一起吃掉了 | 注释块替换的区间**结束锚点必须落在注释里** |
| 40 | 改完一批文档后中文全乱 | 用 GBK 编码写回 UTF-8 文档（探测编码时"先试 gb18030"） | **先试 UTF-8 严格解码** |
| 41 | 同一个数字在不同文档里对不上（5 个不同的 `code`） | 每次只刷了一两份文档 | 挑一份当"数字的唯一出处"，其它引用它 |

---

# 第十七部分 设计取舍汇总（为什么选 A 不选 B）

| # | 问题 | 选了 | 没选 | 理由 |
|---|---|---|---|---|
| 1 | 任务怎么分 | **按职责分 5 个常驻任务** | 按外设分 + 动态创建/销毁 | 7 个页面并存，每个任务要服务所有页面，做不到"用完就销毁" |
| 2 | 共享 I2C 怎么管 | **所有访问收进一个任务** | 总线锁 + 排队 | 锁被超时强夺就退化成并发 ⇒ 花屏/黑屏（25 轮换来的） |
| 3 | 中断里做什么 | **只置标志** | 直接在中断里读改写 I2C | I2C 与副屏共用，中断里抢总线会花屏 |
| 4 | 数码管怎么扫 | **Timer2 1kHz 中断** | T0 任务每 2ms 刷一位 | RTX51 心跳 5ms，任务粒度做不到 2ms |
| 5 | 舵机放哪一组 | **PWMA / PWM3N / P2.5** | PWMB / PWM7 / P0.2 | 同组共用 ARR，蜂鸣器会改它且不恢复 |
| 6 | 舵机什么时候初始化 | **进页面才 Init，离页面 Off** | 上电就 Init | 上电就 Init 会让舵机一直有信号、嗡嗡响/抖 |
| 7 | 音乐怎么放 | **状态机 + 看钟推进（非阻塞）** | `os_wait2` 阻塞播放 | 阻塞期间按键会失灵 |
| 8 | 主屏画法 | **两条通路分工**（直写屏 / 显存） | 只用一条 | 图形内容必须逐点画，直写屏慢且闪 |
| 9 | 副屏刷新粒度 | **只重画变化的那一行 + 固定宽度** | 周期性整屏重画 | 整屏 350ms 太贵；周期重画还是"闪乱码"的来源 |
| 10 | 副屏 Controller init | **只在开机做一次** | 运行期自愈时重跑 | 重跑会停在"已关闭显示"= 全黑 |
| 11 | 闹钟怎么实现 | **硬件（最近一组）+ 软件每秒兜底** | 只用软件每秒比对 | 硬件唤醒链（INT3）能真省电且更准；软件兜底防漏响 |
| 12 | 闹钟去重 | **周内分钟序号（整数）** | 逐个字段比较 | 压成一个整数后比较简单、天然跨天复位、无跨月闰年问题 |
| 13 | 录音记录写法 | **顺序填充 + 只在页首擦** | 每条都擦整页 | 每 256 条才擦一次，寿命够用 |
| 14 | 温度存储 | **+100 偏移（避开 0xFF）** | 直接存 `s8` | `s8` 的 `-1` 是 `0xFF`，会和"空位标记"撞车 |
| 15 | 浮点 | **全工程零浮点** | 该用就用 | 一处 `float` 就把整个 `C51FPL.LIB` 链进来（约 1.8KB） |
| 16 | 小数显示 | **定点整数（×10 / ×100 / 计数×5÷28）** | `sprintf("%.2f")` | 同上 |
| 17 | 按键语义 | **一处全局置换（`key_remap`）** | 逐个 case 改 | 改 5 个页面十几个分支必然漏；一处翻译全跟着走 |
| 18 | 按键上报时机 | **短按在抬手、长按在按满 2 秒** | 都在按下时报 | 两者会互相污染、长按会连发 |
| 19 | 矩阵键盘比较 | **先拼 16 位再统一比较** | 逐位比较（v3.1） | 消抖时刻可按位记录，代码也短一半 |
| 20 | 旋钮量程 | **固定 2740** | 运行期自学习 | 自学习要等用户拧到底一次，刚开机不起作用 |
| 21 | 电位器事件 | **限流 200ms** | 不限 | 不限会把 I2C 挤到 100% 饱和 ⇒ 控制器被写坏 |
| 22 | 汉字怎么显示 | **只在主屏（硬件字库）** | 副屏取模汉字 | 主屏走屏上字库 IC、**不占程序空间**；副屏只有 ASCII |
| 23 | "中文裁剪省 Flash" | **不做**（结论：本板不适用） | 裁 80 个汉字 | 汉字本来就不占程序空间，没得裁 |
| 24 | 图标怎么存 | **7 张 128×64 全点阵（7168 字节）** | 段式压缩（可省约 4500） | 会给"朴素"的工程引入编解码复杂度；余量够 |
| 25 | 游戏节拍 | **`g_sysTick` 毫秒差** | 数任务被调用的次数 | 后者依赖 RTX51 tick 假设，节拍失准时游戏快得没法玩 |
| 26 | 贪吃蛇链表 | **静态节点池** | `malloc` | C51 上不用堆（链库代码 + xdata 碎片化不可控） |
| 27 | 超声波计时 | **软件延时数循环**（照抄上课代码） | Timer4 硬件计数 | 上课代码的延时函数本身就准；且不占定时器、不需要"清零计数器" |
| 28 | 超声波滤波 | **对"计数"取中值（跨调用滑动）** | 一次连测 5 次再取中值 | 后者 5 次 = 400ms 阻塞，RTX51 任务里不能这么干 |
| 29 | DHT11 判据 | **比高低电平长度** | 按微秒写死窗口 | "一次计数"与"1 微秒"之间有个未知系数 |
| 30 | 小灯"渐亮" | **逐颗点亮（0→8）** | 真 PWM 调亮度 | 8 颗灯的 PWM 备选通道都被舵机/蜂鸣器/马达占了 |
| 31 | 上电进度诊断 | **8 颗小灯 + 直接写 IO** | 串口打印 | 不依赖任何外设初始化、不依赖 RTX51、不占栈 |
| 32 | 串口用途 | **UART1 直通 USB** | 蓝牙（串口 2） | 串口 2 的引脚被主屏和 DHT11 占了 |
| 33 | 空闲返回 | **只在"任务清单"那一层生效** | 所有页面都生效 | 正在操作的事不能被弹走（用户明确要求） |
| 34 | 亮屏/熄屏 | **熄面板（0xAE）而不是反复清屏** | 反复清屏追残留 | 关了之后控制器里剩什么都不可能亮，而且省电 |
| 35 | 代码减重 | **删没用到的（模块级链接）** | 改代码优化 | C51 里"没被调用的函数照样占 Flash"，删比改收益大 |

---

# 第十八部分 已知限制与未来可做什么

## 18.1 硬限制（改不了或很难改）

| 限制 | 说明 |
|---|---|
| **程序空间 61440 字节**（不是 65536） | EEPROM 占了 `0xF000~0xFFFF`；且 `IROM(0-0xFFF8)` 让 Keil 不报超 |
| **任务栈 `FREE_STACK = 20` 字节** | 热路径不能加大局部变量、不能放 `printf` |
| **副屏只能 ASCII** | 没有汉字字模（有也删了） |
| 副屏"画满 4 行文字"约 **350ms** | 文字行是逐字节写的，所以运行期不做周期性整屏重画 |
| **Timer4 是唯一可再分配的定时器** | 其余 4 个都有主 |
| **P0.2 是唯一空闲引脚** | 但它的 PWM7 属 PWMB，会和蜂鸣器抢周期（见 §13.3） |
| 3 组闹钟的调度逻辑 | 全项目唯一的"真算法"，改动要格外小心（两层去重机制别破坏） |

## 18.2 明确没做的功能

| 功能 | 状态 |
|---|---|
| 电脑端 Python 上位机界面 | ❌ 只做了板子这侧的协议（11 条命令） |
| 游戏最高分 / 存档 | ❌ EEPROM 1 号区（512 字节）一直空着 |
| 游戏音效（按事件配） | ❌ 游戏里完全没有声音 |
| 每日挑战、游戏大厅独立页、小灯生命值、副屏记分板 | ❌ 都没做 |
| 超声波/舵机接进闹钟流程（挥手停闹钟 / 物理敲铃） | ❌ 做成了独立的「4 测距仪」「5 云台」两个任务页 |
| 蓝牙模块 | ❌ 改走 USB 串口 |

## 18.3 还可以做的（按"收益 / 代价"排序）

| # | 做什么 | 收益 | 代价 / 风险 |
|---|---|---|---|
| 1 | **图标数据段式压缩** | 约 **4500 字节** Flash | 要给"朴素"的工程引入编解码复杂度；是**收益最大**的一项 |
| 2 | 超声波的**实际标定**（当前是手册精算值） | 把 5% 的理论偏差收敛到实测 | 要拿尺子量、改 `SR04_CM_NUM/DEN`；屏上已不显示原始计数，标定步骤写在 `HC_SR04.h` |
| 3 | 把 `BASE_QD`（附点四分）改成 450ms | 曲子该拖长的地方拖长 | **必须先听一遍**（改的是声音） |
| 4 | Python 上位机界面 | 用电脑设闹钟 / 看温湿度曲线 | 协议已就绪，纯电脑端工作 |
| 5 | 游戏最高分存 EEPROM 1 号区 | 补上"记最高分" | 512 字节区一直空着；改动小 |
| 6 | 砍掉一个游戏 | 2000~3000 字节 Flash | 功能损失 |
| 7 | `sprintf` 全部换成手工拼串 | 约 **869 字节** Flash | `App_Display.c` 里有几十处，**风险高** |
| 8 | 减少图标数量（7 张 → 4 张） | 每张 1024 字节 | 界面观感损失 |
| 9 | 舵机改软件 PWM 占 Timer4，从而能用 P0.2 | 换来 P0.2 这个空闲脚 | 脉宽精度受中断延迟影响；P2.5 的硬件 PWM 已经跑通，**不建议** |

---

# 附录 A 文件清单与职责

## A.1 App 层（含 Game）

| 文件 | 行数 | 职责 | 关键接口 |
|---|---|---|---|
| `App_Public.h/.c` | 299 / 58 | **全 App 层唯一入口**：任务 ID、公共类型、跨模块全局量、I2C 请求标志 | `g_sysTick` / `g_clock` / `g_settings` / `g_alarms` / `g_req*` |
| `App_System.h/.c` | — / 401 | 系统初始化（`sys_init()` 8 步）+ 上电进度指示 | `sys_init()` / `Boot_Crumb()` |
| `App_Menu.h/.c` | 178 / 1989 | **页面栈 + 全部页面的按键处理 + 掌机模式外壳** | `Menu_Push/Back/Home` / `Menu_OnEvent` / `Menu_Tick1s` / `Range_Poll` / `Game_Poll` |
| `App_Display.h/.c` | 60 / 1913 | 两块屏的**全部版面** + TASK_RENDER（唯一的 I2C 执行者） | `Display_Refresh` / `Display_SetPage` / `Display_Poll` |
| `App_Input.h/.c` | 40 / 294 | 事件队列 + TASK_INPUT + 旋钮 | `Input_Scan10ms` / `Input_GetEvent` / `Input_GetPotLevel` |
| `App_Clock.h/.c` | 60 / 407 | 时间服务（PCF8563 之上的薄封装 + 星期算法） | `Clock_Refresh` / `Clock_Set` / `Clock_SetNow` / `Clock_TryRecover` |
| `App_Alarm.h/.c` | 92 / 621 | **闹钟调度（全项目唯一的真算法）** | `Alarm_Tick1s` / `Alarm_ApplyHardware` / `Alarm_Start/Stop/Snooze` |
| `App_Storage.h/.c` | 65 / 300 | 片内 EEPROM 分区读写 | `Storage_Load` / `Storage_SaveAll` / `Storage_AppendLog` |
| `App_Music.h/.c` | 60 / 267 | **非阻塞音乐引擎** + 马达 + 日出（TASK_MUSIC） | `Music_Play` / `Music_PlayLoop` / `Music_Tick` / `Music_Beep` |
| `App_Songs.h/.c` | 88 / 106 | 3 首曲子的音符表（纯数据） | `Songs_Get` / `Songs_Length` |
| `App_Sensor.h/.c` | 29 / 117 | 采集任务（TASK_SENSOR） | `Sensor_Tick1s` / `Sensor_HumiValid` |
| `App_Uart.h/.c` | 59 / 349 | 上位机协议（11 条命令） | `Uart_Poll` |
| `App_Font.h/.c` | 18 / 93 | 16×32 大字数字字模（832 字节数据） | `DIGIT32[13][64]` |
| `App_Icons.h/.c` | 32 / 655 | 7 张 128×64 任务图标（7168 字节数据） | `Icon_Get` |
| `Game/App_GmSnake.h/.c` | 62 / 388 | 贪吃蛇（静态节点池） | `Snake_Init/Update/Draw/HandleInput` |
| `Game/App_GmBrick.h/.c` | — / 263 | 打砖块 | `Brick_Init/Update/Draw/MovePaddle` |
| `Game/App_GmPlane.h/.c` | — / 415 | 飞机大战 | `PlaneGame_Init/Update/Draw/HandleKey/Shoot` |

## A.2 Driver 层（一个器件一个文件）

| 文件 | 器件 | 接口风格 |
|---|---|---|
| `Keys.c/h` | 4 个独立按键 | `Keys_Scan` + **回调** `Keys_on_keydown/keyup/longpress`（由 App 实现） |
| `MatrixKey.c/h` | 4×4 矩阵键盘 | `MK_Scan` + 回调 `MK_on_keydown/keyup` |
| `SPI_OLED/spi_oled.c/h` | 主屏（软件 SPI + 硬件字库） | `SPI_OLED_Init` / `_Clear` / `Display_GB2312_string` / `GClear` / `DrawPoint` / `Refresh` / `GBuf_string` |
| `I2C_OLED/I2C_OLED.c/h` + `oledfont.h` | 副屏（含 ASCII 字库） | `I2C_OLED_Init` / `_Clear` / `ShowString` / `DisplayOn/Off` |
| `NIXIE.c/h` + `NixieScan.c/h` | 数码管段码表 + Timer2 扫描 | `NIXIE_show` / `Nixie_ScanInit` / `Nixie_SetTime` / `Nixie_Scan1ms` |
| `PCF8563.c/h` | 时钟芯片 | `PCF8563_get_clock` / `set_clock` / `set_alarm` / `enable_alarm` / `int_call` |
| `HC_SR04.c/h` | 超声波 | `SR04_Init` / `SR04_Measure` / `SR04_Stop` / `SR04_ResetFilter` |
| `Servo.c/h` | 舵机（PWMA 3） | `Servo_SafeLevel` / `Servo_Init` / `Servo_Off` / `Servo_SetAngle` / `Servo_GetAngle` |
| `Buzzer.c/h` | 蜂鸣器（PWMB 5） | `Buzzer_SafeLevel` / `Buzzer_Init` / `Buzzer_Play` / `Buzzer_Stop` / `Buzzer_Freq` |
| `Motor.c/h` | 马达（PWMB 6） | `Motor_SafeLevel` / `Motor_Init` / `Motor_On/Off` / `Motor_Vibrate` / `Motor_Tick` |
| `LED.c/h` | 8 颗小灯 + 总开关 | `Led_Init` / `Led_Power` / `Led_SetSingle` / `Led_SetMask` / `Led_Breath` |
| `NTC.c/h` | 热敏电阻（查表） | `NTC_Init` / `NTC_GetTempX10` |
| `DHT11.c/h` | 温湿度 | `DHT11_Init` / `DHT11_Read(s16*, u8*)` |
| `Timers.c/h` | 1ms 系统时基（Timer3） | `Timers_Init` |
| `I2C_Lock.c/h` | 总线互斥（**当前"保留但永不争用"**） | `I2C_Lock` / `I2C_TryLock` / `I2C_Unlock` |

> **Driver 层的一个统一约定**："默认电平危险"的外设都有 `XXX_SafeLevel()`，
> 必须在 `sys_init()` 最开头调（马达、蜂鸣器、舵机、小灯）。

## A.3 Lib 层（已裁剪）

**保留**：`GPIO` / `NVIC`（只剩 7 个函数）/ `Timer` / `STC8H_PWM`（只剩 PWMA/PWMB 整组 + PWM3/PWM6）/
`UART`（只开 UART1，UART2/3/4 的定义被注释掉）/ `I2C` / `ADC` / `EEPROM` / `Exti` / `Delay` / `Config` / `Switch`。

**已废弃并清空**（文件保留、不含函数）：`Soft_I2C.c` / `ADC_Isr.c` / `I2C_Isr.c`。

## A.4 OS 层

`Conf_tny.A51`（心跳 5ms / 时间片 25ms / `FREE_STACK = 20`）+ `RTX51TNY.LIB`（放进仓库，全组版本统一）。

---

# 附录 B 接口清单（按真实头文件逐条抄录）

> 以下原型是用脚本从 39 个头文件里抽出来的，**不是凭记忆写的**。
> 计划文档里曾经有若干函数名/签名写错的地方，这里以代码为准。

## B.1 任务 ID 与请求标志（`App_Public.h`）

```c
#define TASK_MAIN    0    /* 初始化：建外设、建任务、销毁自己 */
#define TASK_RENDER  1    /* 渲染：刷两块屏，5ms 一拍 */
#define TASK_INPUT   2    /* 输入：按键/矩阵键/旋钮 -> 事件队列，10ms */
#define TASK_LOGIC   3    /* 逻辑：闹钟调度、页面栈、中断标志清理 */
#define TASK_MUSIC   4    /* 音效：换音符、马达、小灯 */
#define TASK_SENSOR  5    /* 采集：DHT11、NTC、写温湿度记录 */

extern volatile bit g_reqClockRefresh, g_reqClockSet, g_reqClockRecover,
                    g_reqAlarmApply, g_reqRtcIrq;

u32 SysTick_Get(void);
#define SysTick_Elapsed(t0)  (g_sysTick - (u32)(t0))   /* u32 相减，天然处理回绕 */
```

## B.2 时间与存储

```c
/* Driver/PCF8563.h */
void PCF8563_init(void);
void PCF8563_get_clock(Clock_t *p_clock);
void PCF8563_set_clock(Clock_t clock);          /* 值传递 */
void PCF8563_set_alarm(Alarm_t alarm);          /* 值传递 */
void PCF8563_enable_alarm(u8 enable);
void PCF8563_int_call(void);

/* App_Clock.h */
void Clock_Init(void);
s8   Clock_Refresh(void);
void Clock_Set(Clock_t *t);        void Clock_SetNow(Clock_t *t);
void Clock_TryRecover(void);       void Clock_TryRecoverNow(void);
u8   Clock_IsValid(void);
u8   Clock_WeekdayOf(u16 year, u8 month, u8 day);
u8   Clock_TodayMask(void);        u8 Clock_MaskToWeek(u8 mask);
u8   Clock_DaysInMonth(u16 year, u8 month);
u16  Clock_MinutesOfDay(u8 hour, u8 minute);

/* App_Alarm.h —— 全项目唯一的"真算法" */
void Alarm_Init(void);
void Alarm_ApplyHardware(void);    void Alarm_ApplyHardwareNow(void);
void Alarm_OnRtcIrq(void);
u8   Alarm_Tick1s(void);
void Alarm_Start(u8 idx);          void Alarm_Stop(void);      void Alarm_Snooze(void);
u8   Alarm_IsRinging(void);        u8   Alarm_RingingIndex(void);
s32  Alarm_SecondsToNext(void);    u16  Alarm_SnoozeRemainSec(void);
void Alarm_TickSunrise(void);
u8   Alarm_Set(u8 idx, const AlarmItem_t *a);
u8   Alarm_Add(const AlarmItem_t *a);   u8 Alarm_Delete(u8 idx);   u8 Alarm_Toggle(u8 idx);
void PCF8563_on_alarm(void);       void PCF8563_on_timer(void);   /* 驱动回调 */

/* App_Storage.h */
u8   Storage_Load(void);           u8   Storage_SaveAll(void);
void Storage_Default(void);
u8   Storage_AppendLog(s8 temp, u8 humi);
u16  Storage_LogCount(void);
u8   Storage_ReadLog(u16 idx, s8 *temp, u8 *humi);
```

## B.3 两块屏幕

```c
/* Driver/SPI_OLED/spi_oled.h —— 主屏 */
void SPI_OLED_Init(void);          void SPI_OLED_Clear(void);
void SPI_OLED_ColorTurn(u8 i);     void SPI_OLED_DisplayTurn(u8 i);
void SPI_OLED_DisPlay_On(void);    void SPI_OLED_DisPlay_Off(void);
void SPI_OLED_WR_Byte(u8 dat, u8 cmd);
void SPI_OLED_address(u8 x, u8 y);
void SPI_OLED_Display_16x16(u8 x, u8 y, u8 *dp);
void SPI_OLED_Display_8x16(u8 x, u8 y, u8 *dp);          /* 第三参是点阵指针 */
void SPI_OLED_Display_GB2312_string(u8 x, u8 y, u8 *text);  /* 能出汉字 */
void Send_Command_to_ROM(u8 dat);  u8 Get_data_from_ROM(void);
void SPI_OLED_get_data_from_ROM(u8 addrHigh,u8 addrMid,u8 addrLow,u8 *pbuff,u8 DataLen);
void SPI_OLED_Refresh(void);       void SPI_OLED_GClear(void);
void SPI_OLED_DrawPoint(u8 x, u8 y);  void SPI_OLED_ClearPoint(u8 x, u8 y);
void SPI_OLED_GBuf_8x16(u8 x, u8 page, u8 *dp);
void SPI_OLED_GBuf_string(u8 x, u8 page, u8 *text);

/* Driver/I2C_OLED/I2C_OLED.h —— 副屏 */
void I2C_OLED_Init(void);          void I2C_OLED_Clear(void);
void I2C_OLED_ColorTurn(u8 i);     void I2C_OLED_DisplayTurn(u8 i);
void I2C_OLED_WR_Byte(u8 dat, u8 cmd);
void I2C_OLED_Set_Pos(u8 x, u8 y);
void I2C_OLED_DisplayOn(void);     void I2C_OLED_DisplayOff(void);
void I2C_OLED_ShowChar(u8 x,u8 y,u8 chr,u8 sizey);
void I2C_OLED_ShowString(u8 x,u8 y,u8 *chr,u8 sizey);

/* App_Display.h —— 唯一的刷新入口 */
void Display_Init(void);
void Display_Refresh(u8 clearScreen);      /* 1=换页(先清屏) 0=只改值(不清屏) */
void Display_SetPage(UIPageId_t page);
void Display_Poll(void);                   /* TASK_RENDER 每拍调一次 */
void Display_MainPower(u8 on);
```

## B.4 输入

```c
#define EVT_KEY1 1 ... EVT_KEY4 4            /* 短按 */
#define EVT_KEY1_LONG 5 ... EVT_KEY4_LONG 8  /* 长按 */
#define EVT_MATRIX 9                         /* param = 0..15（row*4+col） */
#define EVT_POT    10                        /* param = 0..100 */
typedef struct { u8 type; u8 param; } Event_t;

/* App_Input.h */
#define EVT_QUEUE_SIZE 16
void Input_Init(void);   void Input_Scan10ms(void);
u8   Input_GetEvent(Event_t *evt);
void Input_Flush(void);
u8   Input_GetPotLevel(void);
```

## B.5 音效 / 小灯 / 马达 / 传感器

```c
/* App_Music.h */
#define SONG_NONE 0 / SONG_TIGER 1 / SONG_STAR 2 / SONG_MALAN 3 / SONG_COUNT 3
void Music_Play(u8 songId, u8 volume);   void Music_PlayLoop(u8 songId, u8 volume);
void Music_Stop(void);  u8 Music_IsPlaying(void);
void Music_Tick(void);  void Music_Beep(u16 ms);

/* Driver/Buzzer.h */
void Buzzer_SafeLevel(void);  void Buzzer_Init(void);
void Buzzer_Play(u16 hz_value, u8 volume);   /* volume 0..100 */
void Buzzer_Stop(void);       u16 Buzzer_Freq(u8 tone);

/* Driver/LED.h */
void Led_Init(void);  void Led_Power(u8 on);
void Led_SetSingle(u8 idx, u8 on);   void Led_SetMask(u8 mask);
void Led_Breath(u8 level);           /* 0..255 -> 点亮 0..8 颗 */

/* Driver/Motor.h */
void Motor_SafeLevel(void);  void Motor_Init(void);
void Motor_On(u8 level);     void Motor_Off(void);   /* level 0..100 */
void Motor_Vibrate(u16 ms, u8 level);   void Motor_Tick(void);

/* Driver/NTC.h / Driver/DHT11.h / App_Sensor.h */
void NTC_Init(void);   s16 NTC_GetTempX10(void);
void DHT11_Init(void); u8  DHT11_Read(s16 *tempX10, u8 *humi);
void Sensor_Init(void); u8 Sensor_ReadOnce(void);
u8 Sensor_HumiValid(void);  u8 Sensor_Tick1s(void);
```

## B.6 外接模块与驱动回调

```c
/* Driver/HC_SR04.h */
void SR04_Init(void);  void SR04_Stop(void);
u16  SR04_Measure(void);   void SR04_ResetFilter(void);

/* Driver/Servo.h */
void Servo_SafeLevel(void);  void Servo_Init(void);  void Servo_Off(void);
void Servo_SetAngle(u8 angle);   u8 Servo_GetAngle(void);

/* Driver/Keys.h —— App 层必须实现这三个回调 */
extern void Keys_on_keydown(u8 key);
extern void Keys_on_keyup(u8 key);
extern void Keys_on_longpress(u8 key);
void Keys_Init(void);  void Keys_Scan(void);

/* Driver/MatrixKey.h */
extern void MK_on_keydown(u8 row, u8 col);
extern void MK_on_keyup(u8 row, u8 col);
void MK_Init(void);  void MK_Scan(void);

/* Driver/Timers.h / Driver/NixieScan.h / Driver/I2C_Lock.h */
void Timers_Init(void);
void Nixie_ScanInit(void);  void Nixie_SetTime(u8 h, u8 m, u8 s);  void Nixie_Scan1ms(void);
void I2C_Lock(void);   u8 I2C_TryLock(void);   void I2C_Unlock(void);
```

---

# 附录 C 关键常量表

| 宏 | 值 | 位置 | 含义 |
|---|---|---|---|
| `MAIN_Fosc` | `24000000L` | `Lib/Config.h` | 主频 |
| `APP_VERSION_STR` | `"1.0.0"` | `App_Public.h` | 版本号 |
| `ALARM_MAX` | **3** | `App_Public.h` | 闹钟组数 |
| `VOLUME_MAX` | 100 | `App_Public.h` | 音量上限（%） |
| `SNOOZE_MINUTES` | 9 | `App_Public.h` | 贪睡默认分钟数 |
| `IDLE_BACK_HOME_SEC` | 30 | `App_Public.h` | 空闲返回秒数 |
| `SUNRISE_LEAD_SEC` | 600 | `App_Public.h` | 日出提前量（秒） |
| `SUNRISE_STEP_MS` | 100 | `App_Public.h` | 日出调级间隔 |
| `SETTINGS_MAGIC` | `0x414C` | `App_Public.h` | 设置魔数 |
| `SETTINGS_VERSION` | **3** | `App_Public.h` | 设置结构版本 |
| `DAY_WORKDAY` / `DAY_ALL` | `0x1F` / `0x7F` | `App_Public.h` | 星期掩码 |
| `BOOT_CRUMB_ENABLE` | **1** | `App_Public.h` | 上电进度指示（交付时改 0） |
| `DEBUG_EVT_ECHO` | 0 | `App_Public.h` | 按键事件回声（调试用） |
| `MENU_TASK_COUNT` / `SET_ITEM_COUNT` / `GAME_COUNT` | 7 / 7 / 3 | `App_Menu.h` | 任务数 / 设置项数 / 游戏数 |
| `MENU_STACK_MAX` | 4 | `App_Menu.c` | 页面栈深度上限 |
| `GAME_FRAME_MS` | 30 | `App_Menu.c` | 打砖块/飞机帧间隔 |
| `GAME_STEP_MS_SNAKE` | 250 | `App_Menu.c` | 贪吃蛇步进间隔 |
| `GAME_FIRE_CD_MS` | 3000 | `App_Menu.c` | 飞机发射冷却 |
| `GAME_OVER_HOLD_MS` | 3000 | `App_Menu.c` | 结算停留 |
| `GAME_KEY_UP/LEFT/DOWN/RIGHT/EXIT/FIRE` | 9/12/13/14/3/13 | `App_Menu.c` | 游戏键位 |
| `SERVO_KEY_STEP` | 10 | `App_Menu.c` | 舵机每次角度 |
| `EVT_QUEUE_SIZE` | 16 | `App_Input.h` | 事件队列容量 |
| `POT_ADC_FULL` / `POT_LEVELS` / `POT_RAW_STEP` | 2740 / 101 / 60 | `App_Input.c` | 旋钮量程/档位/防抖 |
| `KEY_DEBOUNCE_MS` / `KEY_LONGPRESS_MS` | 20 / 2000 | `Keys.h` | 消抖 / 长按 |
| `MK_DEBOUNCE_MS` | 20 | `MatrixKey.h` | 矩阵键消抖 |
| `LED_COUNT` | 8 | `LED.h` | 小灯数 |
| `BUZZER_TONE_MAX` / `BUZZER_DUTY_MAX_PCT` / `BUZZER_VOLUME_FULL` | 21 / 50 / 100 | `Buzzer.h` | 音阶数 / 最响占空比 / 音量满档 |
| `BUZZER_ENABLE` | **1** | `Buzzer.h` | 蜂鸣器总开关 |
| `BUZZER_BEEP_HZ` | 1047 | `Buzzer.h` | 提示音频率（C5） |
| `MOTOR_PREQ` / `MOTOR_ENABLE` | 1000 / **1** | `Motor.h` | 马达频率 / 总开关 |
| `SERVO_PRESCALER` / `SERVO_FREQ` | 10 / 50 | `Servo.c` | 舵机预分频 / 频率 |
| `SERVO_PULSE_MIN/MAX` / `SERVO_FRAME_US` | 500 / 2500 / 20000 | `Servo.c` | 脉宽范围 / 帧长 |
| `SR04_FILTER_N` | 5 | `HC_SR04.h` | 中值滤波窗口 |
| `SR04_CM_NUM` / `SR04_CM_DEN` | 5 / 28 | `HC_SR04.h` | 换算 `cm = cnt × 5 / 28` |
| `SR04_CM_MIN/MAX` | 2 / 400 | `HC_SR04.h` | 量程（cm） |
| `SR04_EDGE_CNT_MAX` / `SR04_ECHO_CNT_MAX` | 500 / 3000 | `HC_SR04.h` | 两个超时阈值 |
| `NIXIE_DIGITS` | 8 | `NixieScan.h` | 数码管位数 |
| `NIXIE_POS_REVERSED` | 0 | `NixieScan.h` | 位序方向（实机无需翻转） |
| `BASE_Q/E/QD/H` | 300/100/300/400 | `App_Songs.h` | 曲目时值（见 §11.3 的说明） |
| `HUMI_FAIL_LIMIT` | 5 | `App_Sensor.h` | 连续失败几次判无效 |
| `SENSOR_LOG_INTERVAL` | 600 | `App_Sensor.h` | 记录间隔（秒）= 10 分钟 |
| `RX_FRAME_MAX` | 48 | `App_Uart.c` | 串口帧长上限 |
| `EE_BASE` / `EE_LOG_CAPACITY` | `0xF000` / 1536 | `App_Storage.h` | EEPROM 基址 / 记录容量 |
| `SNAKE_MAX_LEN` / `PLANE_BULLET_MAX` | 40 / 10 | 游戏头文件 | 蛇长上限 / 子弹槽 |

---

# 附录 D 文档地图

本项目一共 **23 份 markdown**（含本文）：
- **1 份**在你的桌面（`AI项目生成规范.md`，元文档）
- **7 份**在 `D://Develop//embedded-project//` 根目录（00~05 + 人员分工）
- **14 份**在工程目录 `51_MCU_Alarm\` 里

## D.1 元文档（给 AI 看的约束）

| 文档 | 内容 |
|---|---|
| `D:/BWJJ/Desktop/AI项目生成规范.md` | **最该先读的一份**。从"动手前的门禁"到"完工之后的减重"，12 节，是本项目一切方法论的总纲 |

## D.2 项目级文档（`embedded-project\` 根，按阅读顺序编号）

| 文档 | 面向谁 | 回答什么 |
|---|---|---|
| `00-新同学导读.md` | 完全的新人 | 不讲技术细节，只讲"我们在做什么、这些东西都是啥"；含常用词白话速查 |
| `01-项目介绍.md` | 想知道"做出什么"的人 | 七个任务能干什么、怎么演示、原计划里没做的是什么 |
| `02-技术方案.md` | 要理解技术取舍的人 | 硬件怎么接、资源怎么分、程序怎么组织、难在哪 |
| `03-协作手册.md` | 要参与的人 | 工具、代码规范、提交流程、验收标准、遇分歧听谁的 |
| `04-M1分工规格.md` | 想知道"谁做了什么"的人 | 8 个角色对应的模块 + **真实接口清单** + 每个模块的关键实现点 |
| `05-M1集成验证.md` | 想看"推演对不对"的人 | 四层静态检查的结论、被真机打脸的地方、第 70 轮的第五层验证 |
| `人员分工.md` | 同上（速查） | 一张表：8 个角色 → 实际对应的模块 |

## D.3 工程内部文档（`51_MCU_Alarm\`）

| 文档 | 什么时候看 |
|---|---|
| **`硬件真值表.md`** | ★ 要接线 / 要改硬件 / 要动某个外设之前**必看**。逐项引脚极性、库函数签名、上电安全电平，每项都引原文 |
| **`整体框架.md`** | ★ 要理解"工程怎么搭起来"时看。分层、任务、数据流、资源分配、改动指引 |
| **`使用说明.md`** | ★ 拿到板子的人看：怎么接、怎么烧、每个键干什么、出问题怎么查 |
| **`交付说明.md`** | ★ 验收的人看：做过什么、验过什么、**数字的唯一出处**（编译尺寸在这里） |
| **`项目全解-从需求到实现.md`** | ★ **本文**。从需求讲到实现，18 部分 + 4 个附录 |
| `M1真机联调总结.md` | 想学"怎么排查硬件问题"时看（第 1~13 轮，9 个真问题 + 2 次弯路） |
| `副屏显示链路专项总结.md` | ★ 想理解"共享资源为什么不能加锁"时看（第 5~29 轮，6 个被否掉的假设） |
| `真机联调记录-1.md` / `-2.md` | 最早两轮的原始记录（含"上电进度指示"的来历） |
| `UI重构设计稿.md` / `-v2.md` | UI 改造的两版设计（v1 已作废，v2 是定稿方向） |
| `UI改造任务清单.md` | UI 改造的 9 个点逐项完成情况（附录 A 的 Sakamoto 算法讲得很细） |
| `demo30参考代码分析.md` | ★ 本工程架构的"准绳"是怎么分析的（四条设计原则、硬件 I2C 的真相、唤醒方式权衡） |
| `参考项目对比与可借鉴清单.md` | 与参考工程 v3.1 的横向对比（已采用 12 条 / 做得更好 6 条 / 学但不采用 7 条） |
| `工程减重通读记录-第70轮.md` | ★ 第 70 轮"减法"的完整清单与四批体积轨迹 |

## D.4 读文档的建议顺序

```
完全新人：  00-新同学导读  →  使用说明  →  项目全解（本文）第一部分
要上手改代码：项目全解（本文）全部  →  硬件真值表（改哪个外设看哪节）  →  整体框架 §13 改动指引
要排查真机问题：使用说明 §十二（常见问题）  →  项目全解 §16（踩坑史）
要理解"为什么"：AI项目生成规范  →  副屏显示链路专项总结  →  demo30参考代码分析  →  项目全解 §17（取舍汇总）
要交付/验收：  交付说明  →  05-M1集成验证
```

## D.5 关于"数字"的一条提醒

**同一个数字在不同文档里可能不一样** —— 因为每次改完代码只刷了其中一两份。
本项目已经把《交付说明》第一节定为**数字的唯一出处**，其它文档一律写"见《交付说明》第一节"。
如果发现某个数字对不上，**以 `Listings/Project.m51` 的 `Program Size` 为准**（那是编译产物，不会说谎）。

---

## 结尾：这个项目最值得记下来的五句话

1. **"参考"是最大的错误来源** —— 说"照抄"就要照抄同一个项目的同一套实现；
   而且照抄只保证"与源文件一致"，**不保证正确**（源文件本身可能有 bug）。
2. **共享资源最稳的办法是"只让一个执行者"**，不是"加锁排队"。
   锁只能保证互斥，保证不了"字节流不断"。
3. **"不清屏"就必须"写满整屏"，二者必居其一**；
   而"清屏"和"画东西"必须同属同一条绘制通路。
4. **估开销要按"事务次数"算，不能按理论波特率算**（本项目在这上面差过 100 倍）。
5. **项目"做完"不等于"没有可删的"** —— C51 是模块级链接，
   通读一遍再删一轮，这一次就挤出了 3404 字节（9.6% 的余量）。
