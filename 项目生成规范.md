# 让 AI 一次性生成正确嵌入式项目的规范

> **第一版来源**：STC8H / RTX51 项目真机联调踩坑复盘（2026-09-16）。
> **第二版扩充**：同一个项目从开工做到"完工 + 减重"（第 1~70 轮）之后的完整复盘（2026-09-23）。
> 第一版只覆盖"**动手前**"和"**第一次编译**"；第二版补上"**跑起来之后**"和"**完工之后**"。
> 用途：把本文档**作为任务约束的一部分**交给任何 AI（含下一次对话的我），
> 让它从"参考资料"升级为"逐字对齐 + 全链路验证"，避免反复烧录调试。

---

## 核心原则（一条）

**"参考"是 AI 出错的最大来源。** 必须把"参考这些资料"改成——
**"这些资料是唯一真值，逐字对齐；任何与资料不一致的地方，必须显式列出并说明理由，否则按资料为准。"**

原因：AI 读参考代码后，写自己的实现时会用"通用知识"补全细节，
而这些通用知识在特定硬件上往往是错的（本次踩的坑全是这一类）。

### 核心原则的推论（第二版新增，第 1~70 轮最贵的两条）

**推论 1：说"照抄"就要照抄同一个项目的同一套实现。**
不能从 A 项目抄驱动、从 B 项目抄总线、再自己接线。
本项目在这上面白烧过约 3 轮固件：拿另一个目录的"软件 I2C"方案，
去改造一个本来用硬件 I2C、而且真机跑得好好的驱动，把驱动原有的接线拆散了。

**推论 2：代码 > 注释 > 文档。**
调试期有两次按注释下结论，结果注释是错的。凡是"注释说……"的判断，
都要去读一遍实现再确认。本项目收尾时做了一轮通读，
单是"注释与代码不符"就找出 **29 处**（详见第 70 轮的减重记录）。

---

## 一、动手前先交"硬件真值表"（不是目录，是逐条事实）

要求 AI 在写任何代码**之前**，先输出下面这张表，每一项都**必须引用官方例程原文**，
禁止凭记忆或经验填写。表填不对，不许写代码。

| 项 | 要求 | 示例（本次） |
|---|---|---|
| 每个外设的引脚号 | 从例程宏定义复制 | `LED1=P27 ... LED8=P20`、马达 `P0.1` |
| 每个 IO 的有效电平 | 从例程注释复制 | `LED 低电平亮`、`P01=0 不工作（高电平震）` |
| 总开关/使能脚极性 | 从例程复制 | `LED_SW=0 导通` |
| 各外设初始化顺序 | 从例程注释复制 | `PCF8563_init 必须在屏幕初始化前（它做 EAXSFR+I2C 配置）` |
| 显示坐标单位 | 从驱动源码推导 | `SPI_OLED_address` 里 `0xB0+y` → y 是"页"不是像素 |
| 看似多余的语句 | 照抄 | 例程末尾的 `EA=1`（任务调度依赖它） |
| 库函数的参数范围 | 读函数签名 | `delay_ms(u8 ms)` → 传 400 会溢出成 144 |
| 库函数是否有超时 | 读源码 | `Wait(){ while(!(I2CMSST&0x40)); }` 无超时 → 必须加 |

### 1.1 真值表的补充项（第二版新增）

| 项 | 为什么必须查 | 本次的真值 |
|---|---|---|
| **引脚复用冲突** | 一根脚可能同时是"超声波 TRIG"和"PWM 互补输出" | P2.4 = PWM3P 也是超声波 TRIG；**实测不冲突**，因为只使能了 PWM3**N**，库函数会把 `ENO3P` 清 0 |
| **同一组 PWM/定时器的"公共寄存器"** | 同组通道共用一个周期寄存器 | STC8H 的 PWM 分 PWMA/PWMB 两组，**同组共用 ARR**。蜂鸣器(PWM5)与马达(PWM6)同属 PWMB；舵机在 PWMA ⇒ 50Hz 与 1kHz 可共存 |
| **"看起来空着"的定时器** | 它可能被"不产生中断"的用途占着 | Timer1 被 UART 波特率发生器占用，`ET1=0` 所以**搜"中断函数"完全搜不到**。判"是否空闲"要看 `TRn`/`AUX`/`BRT_TimerN` 这些寄存器级证据 |
| **"看起来没用"的引脚/中断** | 有人写没人读的变量、没使能的中断空壳 | 全工程清出 1 个"被 5 个中断写、从来没人读"的全局变量 + 6 个"永远不会执行"的中断空壳 |
| **结构体的字段个数** | 局部结构体未赋值字段 = 内存垃圾，会被库函数原样写进寄存器 | 全量审计揪出 3 处：`TIM_PS`（导致 1ms 节拍变 217ms）、`COMx_InitDefine.Morecommunicate`、`I2C_InitTypeDef.I2C_SL_ADR/I2C_SL_MA` |
| **程序空间的真实上限** | 不是"芯片标称 Flash" | 本芯片 64KB，但 **EEPROM 占了 `0xF000~0xFFFF`** ⇒ 程序实际只能用 **61440 字节** |
| **源码编码** | 中文串的字节就是查表地址 | 必须 GBK；且**不能用 GB18030 四字节区字符**（`⇒ ⚠ ↔ −`） |

---

## 二、三重验证，缺一不可（编译通过 ≠ 能用）

| 层 | 怎么做 | 能抓出的问题 |
|---|---|---|
| **编译** | `UV4.exe -r project.uvproj -j0 -o log.txt`，看 `0 Error(s)` | 语法、类型、缺头文件 |
| **链接** | 同一日志里查 `UNRESOLVED EXTERNAL SYMBOL`，必须为 0 | 工程漏注册源文件（本次漏了 EEPROM.c/Exti.c/Exti_Isr.c） |
| **尺寸与库** | 读 `Listings/project.m51`，查 `FPMUL/FPDIV/PRINTF` 是否被引用、看 code 尺寸 | C51 是**模块级链接**——同文件里没调用的函数也会整段链入（本次 `ShowNum(float)` 把整个浮点库拉进来、`sprintf` 把 printf 库拉进来） |

**工程文件不要手改**：`.uvproj` 的 `<FilePath>` 反斜杠、编码（GBK/UTF-8）极易出错。
改完必须重新走上面三重验证。

### 2.1 第三层的补充：怎么读尺寸（第二版新增）

```bash
# 编译（命令行，可脚本化）
"D:/Develop/Keil_v5/UV4/UV4.exe" -r Project.uvproj -j0 -o build_log.txt
# 必须看到： Program Size: data=x xdata=x code=x   和   0 Error(s), 0 Warning(s)
```

```bash
# 从 .m51 里逐模块统计 code/xdata（光看总数不够，要知道是"谁"吃掉的）
grep -E '^\s+(CODE|XDATA)' Listings/Project.m51        # 每个段的地址/长度/归属模块
grep -c 'FPMUL\|FPDIV\|CASTF' Listings/Project.m51     # 必须是 0
grep -c 'UNRESOLVED' Listings/Project.m51              # 必须是 0
```

**两个会骗人的地方**：

1. **`IROM(0-0xFFF8)` 让链接器以为代码区能用满 64KB**，
   所以程序超过 60KB 时 **Keil 不报错**，但 ISP 下载会因超出程序区拒收。
   ⇒ 判"够不够"必须**按真实上限（61440）自己算**，不能以"Keil 没报错"为准。
2. **`sprintf` 会带着整个 printf 库进来**（本项目实测这一段 **869 字节**），
   只要还有**一处** `sprintf` 就省不掉。它和"模块级链接"是同一个道理的两面：
   **一处引用 = 整段库**。

---

## 三、驱动代码：逐字对齐，不要"看懂后自己写"

1. **寄存器/配置序列**：照抄官方例程的 `PWM_config`、`OLED_Init` 等，只改必要参数。
2. **初始化顺序和"多余语句"**：例程里 `EA=1`、`ColorTurn(0)` 这类语句，即使看起来多余也照抄——它们往往是作者踩坑后加的。
3. **查函数签名再传参**：`delay_ms(u8)`、`Display_8x16(x,y,点阵指针)`（不是字符串）这类签名，用之前必须确认。

### 3.1 "逐字照抄"的边界（第二版新增）

**逐字照抄只保证"和源文件一致"，不保证"正确"。**
本次有两个 bug 就是照抄来的——**而那份源文件本身就有 bug**：

- `Timers.c` 漏赋 `TIM_PS` ⇒ 库函数把内存垃圾写进预分频寄存器 ⇒ 1ms 节拍变成 217ms；
- `PCF8563_set_alarm()` 的"周"分支判据写成了 `if(alarm.day < 0)` ⇒ 写进硬件的周字段不对；
- `PCF8563_init()` 里配 I2C 引脚的那句被注释掉了 ⇒ I2C 直接不通。

⇒ **照抄之后，要对着"硬件真值表"再核一遍**：引脚对吗？字段填满了吗？
这个函数的返回值/副作用和调用方预期一致吗？

### 3.2 一个具体的坑：结构体字段必须填满

C51 里函数内的局部结构体是**未初始化**的（不是零）。
库函数往往会把整个结构体原样写进寄存器 —— 没赋值的字段就是**内存垃圾**，
而且它落在 C51 的可覆盖段，**值随代码布局变化**，表现为"以前好用、改完就坏"。

> 本项目最后做了**全量审计**（把每个传给库函数的结构体逐字段对照库实现），
> 又揪出两处漏赋值。这类问题的排查成本极高，**在写的时候花 30 秒填满是唯一划算的做法**。

---

## 四、真机前先自检"诊断工具本身"

本次教训：我给的分步 LED 指示里，`Led_SetSingle` 只改掩码、真正输出在**任务里的** `Led_Refresh()`——
任务没跑灯就不亮，导致诊断工具本身失效，白调一轮。

规则：**任何"看灯/看屏来判断走到哪一步"的诊断手段，其输出必须不依赖被诊断的对象。**
（本次改为 `Led_SetSingle` 立即输出 + 屏幕初始化后立即显示测试文字 + 任务心跳 LED。）

### 4.1 可复用的五种诊断手法（第二版新增，都是本项目真正管用的）

| # | 手法 | 怎么做 | 本次用它定死了什么问题 |
|---|---|---|---|
| 1 | **参照系诊断** | 找一个**不依赖待查对象**的计数器当基准，两个一起打印 | `g_sysTick` 每 5 秒只 +23（应 +5000）⇒ 直接锁定 Timer3 漏赋 `TIM_PS`。参照物是"每 1 秒执行一次"这件事本身（它靠 RTX51 心跳数出来，不依赖 `g_sysTick`） |
| 2 | **把内部状态打出来，一次否掉一整类可能** | 在关键函数入口计数并定期打印 | `Display_Poll` 精确 +1000 / 5 秒 ⇒ 任务一直健康，"任务卡死"这个方向当场作废 |
| 3 | **用"算术可能性"判断内存被踩** | 看打印值在算术上是否可能 | BCD 掩码后月份最大 25、小时最大 45；日志出现 `y=259 mo=93 94:08:07` ⇒ 一定是内存被踩，别再查外设 |
| 4 | **A/B 对照实验** | 把**已验证的参考工程**烧进同一块板、用同样手法操作 | 同板跑参考工程连按 90 秒完全正常 ⇒ 证明"电气/模块"没问题，问题在自己的代码。这是整段排查里最关键的一步 |
| 5 | **先写判读表，再等数据** | 交付每个改动时给出"看到 X ⇒ 结论 A；看到 Y ⇒ 结论 B（则此方向作废）" | 没有判读表的改动，只能换来"还是不行"，白烧一轮 |

> **反例**：本项目有一次把"能解释所有现象的（错误）原因"当成了首要嫌疑，
> 结果方向全错。**能解释所有现象 ≠ 就是原因** —— 应该先设计一个"能区分几种可能"的判据。

### 4.2 诊断手段本身的纪律（第二版新增）

1. **热路径里不许放 `printf`**。任务栈 `FREE_STACK` 只有 20 字节，
   `printf` 是 C51 里栈开销最大的函数 —— 本次就因为热路径的 printf **把栈踩穿、触发复位**
   （`?RTX_STACKERROR`），表现成"读出来的时间字段是算术上不可能的值"。
2. **诊断代码下电前要清**。本次收官时把全部调试 printf 及其附属变量清掉了；
   留下的只有"直接写 IO 的进度指示"（`BOOT_CRUMB`，一个宏关掉）。
3. **诊断手段占的空间也要算**。一个"字库 IC 读回自检"函数当时很有用，
   但它会一直占 Flash —— 项目完工后把它删了。

---

## 五、交付前自检清单

### 5.1 第一版 10 项（仍然全部有效）

1. [ ] 马达（或任何"默认电平危险"的引脚）是否显式初始化为安全电平
2. [ ] LED 等指示灯的极性、引脚顺序、总开关极性与官方例程逐字一致
3. [ ] 各外设初始化顺序与官方例程一致（尤其"A 必须在 B 前"的注释）
4. [ ] `EA=1` 等开中断语句是否照抄
5. [ ] 显示函数坐标单位（页 vs 像素）已从驱动源码确认
6. [ ] 用到的库函数已读源码，确认无"无超时死等"
7. [ ] 传参未超出函数参数类型范围（u8 传 400 之类）
8. [ ] 无 float/printf 等会链入大库的用法（读 .m51 确认）
9. [ ] 工程文件注册了所有用到的 .c（链接 UNRESOLVED=0）
10. [ ] 诊断手段的输出不依赖被诊断对象

### 5.2 第二版新增 14 项（跑起来之后、以及完工之后才暴露的）

**并发 / 共享资源**

11. [ ] **共享的外设（总线/控制器）是不是"只让一个执行者碰"**？加锁排队只是把并发变成串行，
    一旦锁被超时强夺就退化成并发 —— 共享资源最稳的办法是**收进同一个任务**。
12. [ ] **中断里是否只做了"置标志"**？中断里碰共享总线会和任务抢。

**显示 / 刷新**

13. [ ] **同一个界面里，"清屏"和"画东西"是不是同一条通路**？
    驱动有两条路（直写屏 / 显存），混用会让刚画的内容被下一次整屏刷新擦掉。
14. [ ] **不打算清屏的话，是不是"写满了整屏"**？二者必居其一，否则留残影。
15. [ ] **有没有周期性整屏重画**？估算屏刷新开销要按"**每个字节一次事务**"算，
    不能按总线波特率的理论值算 —— 本项目这里低估了 **100 倍**（估 3.5ms，实测 350ms）。
16. [ ] **行式刷新的宽度是否固定**？变短的行会露出上一帧的尾巴。

**时序 / 计算**

17. [ ] 有没有**"先减法再判越界"**的无符号下溢？（`y -= 4; if (y < 0)` 对 u8 恒不成立）
18. [ ] 有没有**用无符号变量做倒序循环**？（`for (i=n; i>=0; i--)` 死循环）
19. [ ] 表示"-1/+1"这种方向值时，用的是不是 `s8`？（C51 的 `char` **默认无符号**）
20. [ ] 16 位定时器读值用的是"**读高→读低→再读高，两次高字节一致**"吗？
    不能用"读低字节两次比较"（低字节每 0.5µs 就变，会死循环）。
21. [ ] 有没有误以为"写 `THn/TLn` 能清计数器"？（对自动重装定时器，那只是"重装值"）

**工程 / 编码**

22. [ ] 源码是不是**纯 GBK 可表示**？写盘前**必须先断言**，不能"先用 gb18030 写、再单独校验"
    （gb18030 能编码四字节区字符，先写盘就晚了）。
23. [ ] 交付前 `BOOT_CRUMB` 之类的诊断开关关掉了吗？
24. [ ] **文档里的数字和代码一致吗**？本项容易被忽略：项目改完代码不刷文档，
    下次读文档的人就会被旧数字误导（本项目文档里同一件事出现过 5 个不同的 `code` 值）。

---

## 六、给 AI 的标准任务提示词（可直接复制）

```
任务：基于官方例程，为 <硬件> 生成 <功能> 代码，要求一次编译链接通过、真机可用。

硬性约束：
1. 动手前先输出"硬件真值表"（引脚/极性/初始化顺序/坐标单位/库函数签名/引脚复用冲突/
   同一组外设的公共寄存器/程序空间真实上限/源码编码），
   每一项必须引用官方例程或驱动源码原文，禁止凭记忆填写。
2. 所有寄存器配置、初始化顺序、看似多余的语句（如 EA=1）逐字照抄官方例程，
   只改必要参数；任何偏离必须显式列出理由。
   ★ 照抄之后要对着真值表再核一遍 —— 照抄只保证"与源文件一致"，不保证"正确"。
3. 完成后必须跑三重验证：编译(UV4 -r)、链接(查 UNRESOLVED)、
   尺寸与库(读 .m51 查 FPMUL/PRINTF/code 尺寸)，并把三段结果附上。
   ★ 尺寸要按"真实上限"自己判断（不要把 EEPROM 占掉的那部分算进来）。
4. 工程文件、编码(GBK/UTF-8)、路径写法需说明并自检。
5. 提供的最小可运行诊断手段，其输出不得依赖被诊断对象本身。
6. 共享的外设（I2C/SPI 总线、显示控制器）只能有一个执行者，不要用锁"轮流用"；
   其余任务只置请求标志。
7. 中断服务里只置标志，不做任何总线访问。
8. 不打算清屏就必须写满整屏；清屏与绘制必须同属一条通路；
   运行期不做周期性整屏重画。
9. 热路径里不放 printf；估算屏刷新开销要按"每字节一次事务"算。
10. 改动分批交付，每批单独编译并报出 data/xdata/code 三个数字的变化。
```

---

## 七、【第二版新增】项目"跑起来之后"才会暴露的四类问题

### 7.1 共享资源：不要加锁排队，要"只让一个执行者"

**问题**：副屏（I2C）和时钟芯片共用同一条总线。最初方案是"加一把总线锁，谁用谁举手"。

**这条路怎么失败的**（本项目花了 25 轮真机迭代才判定）：

```
两个任务共用一个 I2C 控制器
   ↓ 靠锁交替，但"一次整屏重画 350ms" >> "RTX51 时间片 25ms"
   ↓ 持锁期间必然被换出去
锁超时 → 强制夺锁
   ↓
两个任务同时往同一控制器发字节
   ↓
字节流错位：数据字节被从机当成控制字节
   ├─ 落进 0x40~0x7F → "设置显示起始行" → 整屏垂直滚动
   ├─ 落进 0x20     → "设置寻址模式"   → 写入跨页蔓延
   └─ 列地址错位                       → 局部花屏
   ↓
控制器被写坏 → 花屏/黑屏；MCU 重启救不回来（屏没断电、控制器自锁）
```

**最终方案**：**把所有该总线的访问收进同一个任务**，其余任务只置请求标志。
天然串行 ⇒ 根本不需要锁。（本项目 5 个请求标志：
读时间 / 写时间 / 时钟自救 / 重写硬件闹钟 / 处理芯片中断。）

> **实现技巧**：把"真正干活的函数"改名（`Clock_Set` → `Clock_SetNow`），
> 原名字保留为"只置请求标志"的薄壳 —— 这样**所有既有调用点一行都不用改**，
> 自动全部变成"发请求"。

**这条的普适性**：任何"多个执行流共享一个有状态控制器"的场合都适用
（SPI 屏、EEPROM、外部 ADC…），不止 I2C。

### 7.2 两条互不相容的绘制通路

驱动里"直写屏"和"写显存再整屏刷"是两条**完全隔离**的路：

| | A. 直写屏 | B. 显存通路 |
|---|---|---|
| 清屏 | `X_Clear()` | `X_GClear()`（清的是**显存**） |
| 画东西 | `X_Display_xxx()` 系列 | `X_DrawPoint()` / `X_GBuf_xxx()` |
| 上屏时机 | **立即** | `X_Refresh()` 时**整屏**送出 |
| 适合 | 换页、写几行字 | "整屏重绘 + 落点画图"（游戏、动画） |

**混用的后果**（本项目真机踩过，结算画面全黑）：

```c
X_GClear();                       /* 清的是显存 */
X_Display_string(28, 2, "GAME OVER");  /* 直写屏 —— 显存里其实什么都没写 */
X_Refresh();                      /* 把「空显存」整屏覆盖上去 */
                                  /* -> 刚写的字被自己擦掉，画面全黑 */
```

**判据一句话：同一段代码里，清屏和画东西必须同属一条通路。**

### 7.3 估算 I2C/SPI 开销：按"事务次数"算，不是按波特率

本项目有一次把"副屏整屏刷新"估成 **3.5ms**，实测 **350ms**，**差 100 倍**。

原因：驱动是"**每写 1 个字节就发一次完整 I2C 事务**"
（START + 设备地址 + 控制字节 + 数据 + STOP ≈ 340µs）。
1024 字节 ⇒ 1024 次事务 ≈ 350ms。

⇒ 估算公式是：
```
总耗时 = 字节数 × 单次事务耗时        （逐字节写）
总耗时 = 页数   × 单次事务耗时        （批量写，一次事务连发一页）
```
**不是** `字节数 × 8 / 波特率`。

**这个低估直接导致了一个错误的设计判断**（"两块屏分两帧刷，单次不超过 3.5ms ⇒ 可行"），
进而产生"运行期周期性整屏重画"，最后变成花屏的来源之一。

### 7.4 定时器/时钟：三个硬知识点

1. **自动重装定时器的 `THn/TLn` 是"重装值"，不是"计数器"**。
   运行中写它**不会清计数器**。要测时长必须用"两次读值相减 `t1 - t0`"。
   （本项目第一版按"清零再读绝对计数"写，结果读数一次比一次大 ——
   真机"15cm 显示四五十，移到 1cm 还是 40"。）
2. **读 16 位计数器必须"读高 → 读低 → 再读高，两次高字节一致才算有效"**。
   不能用"读低字节两次比较"（低字节每 0.5µs 就变，会死循环）。
3. **"墙上的钟"和"任务心跳"是两套时间，别混**。
   任务心跳（RTX51 的 5ms）只管"任务多久轮换一次"；
   精确计时要用一个独立的 1ms 中断计数器，所有时间差都用"当前减开始"（u32 相减天然处理回绕）。

---

## 八、【第二版新增】"项目做完"之后还有一层：通读减重

**C51 是模块级链接 —— 没被调用的函数照样占程序空间。**
所以省 Flash 的第一手段不是"改代码"，而是"**清掉用不到的东西**"。

### 8.1 可复用的减重流程

1. 用脚本扫出所有"**定义 + 声明都在、全工程搜不到调用**"的函数（判据见下），列成清单；
2. **分批删**（本项目分 4 批：A 删死函数 / B 死变量与缺陷 / C 朴素化 / D 注释同步），
   **每批单独重编译并记录 `data/xdata/code` 三个数字**；
3. 顺手删掉：只写不读的变量、"等价于什么都没做"的死逻辑、
   **只被已删函数引用的常量表/字模表**；
4. 最后做一遍注释同步（注释改动也要重新编译验证 —— 你动过文件就应该复验）。

> **中间某批"体积一点没降"往往是漏删的信号**，别放过它。

### 8.2 为什么"删死代码"这件事风险极低

**漏判会被链接器当场抓住** —— 只要那个符号真被调用，链接立刻给
`UNRESOLVED EXTERNAL SYMBOL`。所以不需要人肉反复确认，删完编译一次就有答案。

### 8.3 判"某函数是不是死的"的三条判据

1. 本文件里有**函数定义**（区分定义与声明：定义行行尾没有分号）；
2. 全工程（**排除第三方库目录和 `*.bak`**）搜不到调用；
3. 头文件里的声明一并删掉（留着"有声明没实现"的接口，将来链接会很难查）。

**两个陷阱**：

- **`grep` 对 GBK 文件搜中文命中率是 0**，而且部分文件会被它当二进制（只输出
  `Binary file ... matches`，不列匹配行）。**只搜 ASCII 符号名**（函数名都是 ASCII），或用脚本按 GBK 解码后再搜。
- **同一个工程里函数定义的写法不止一种**（花括号是否同行、行尾是否粘注释、
  返回类型带不带 `const`）。正则只覆盖最常见的那种会**静默漏删** ——
  **漏删不报错，只表现为"体积没降"**。所以脚本必须打印"删到了几个"，数量不对就修正则。

### 8.4 本项目第 70 轮的实测结果

| | 减重前 | 减重后 | 变化 |
|---|---|---|---|
| `code` | 58939 | **55535** | **−3404 字节** |
| `xdata` | 3820 | **3713** | −107 |
| 误差 / 告警 | 0/0 | **0 Error / 0 Warning** | — |
| `UNRESOLVED` / 浮点库 | 0 / 0 | **0 / 0** | — |

删掉的东西里最典型的三类：
**① 一整个原厂外设接口套件**（副屏的"显示数字/汉字/图片" + 一整套软件 I2C；
主屏的整屏/局部刷新/5x7 字体 —— 全工程一个都没调用）；
**② 一整个"五层嵌套进位"的日期加秒函数**（49 行，从来没人调用）；
**③ 7 个汉字字模表**（唯一使用者是上面已删的取字函数）。

**顺便还揪出一个真实缺陷**：某个结构体数组是 3 字节，代码里却写了 4 个 ——
**越界写 1 字节，踩到紧邻的另一个全局变量**。这类问题不读代码是发现不了的。

---

## 九、【第二版新增】批量改代码（脚本化）的纪律

改的量大时必然写脚本。以下是本项目反复踩过、现已固化的规矩：

| # | 规矩 | 踩过的坑 |
|---|---|---|
| 1 | **一次只做一类改动**，每批单独编译并记体积 | "改多了"会让新问题掩盖老问题，白多花好几轮 |
| 2 | 替换用"**精确串替换 + 断言旧串恰好出现 1 次**" | 用"已改就跳过"的幂等判据会误判 —— 旧文本里本来就可能含判据串，于是跳过替换，编译报"未定义标识符" |
| 3 | **注释块替换的区间不要跨到函数体内** | 本次把"函数签名 + 左花括号"一起吃掉、新文本又忘了补 `{`，编译报一串 `error C132 / C141` |
| 4 | 删函数的正则要容忍多种定义写法，并**打印删到了几个** | 见 8.3 |
| 5 | **不要用 `re.sub` 的非贪婪 `/*.*?*/` 去注释** | 会被字符串字面量打乱配对。要**逐行跟踪 `/* */` 与 `//` 状态** |
| 6 | 定位函数体**必须跳过前置声明** | `text.index('void F(void)')` 会命中前置声明，导致花括号配对取到别的函数 |
| 7 | 写源码前**先探测编码与行尾** | 往 CRLF 文件插 LF 片段 ⇒ 行尾混合；按错编码写回 ⇒ 中文全乱 |
| 8 | 批量改完**跑一遍综合校验** | 校验四件事：行尾一致性 / 编码纯净性 / 已删符号残留 / 关键改动点回读 |

### 9.1 一个具体的编码陷阱：一个工程里可以有两种编码

本项目的实际分布：

| 类别 | 数量 | 编码 | 行尾 |
|---|---|---|---|
| `.c` / `.h` 源码 | 68 | **GBK**（纯 GBK，禁用 GB18030 四字节区字符） | 同文件内不许混 |
| `.md` 文档 | 22 | **UTF-8** | LF 或 CRLF 都行，同文件内一致 |

⇒ **自动探测编码的工具必须"先试 UTF-8 严格解码"**。
反过来"先试 gb18030"是错的 —— **GB18030 解码极其宽容**，UTF-8 文件按它几乎总能
"解码成功"（只是变成乱码），于是 UTF-8 文档会被误判成 GBK，**写回时整体转码**。

```python
try:    raw.decode('utf-8');  enc = 'utf-8'    # 文档
except: enc = 'gb18030'                        # 源码
```

---

## 十、【第二版新增】接手 / 交付一个已有工程时的检查表

| # | 检查什么 | 怎么查 | 本项目发现的实例 |
|---|---|---|---|
| 1 | 有没有"定义了但没人调用"的初始化函数 | 搜调用点 | `Nixie_ScanInit()` 全工程零调用 ⇒ 数码管一位都不亮 |
| 2 | 有没有"有声明没实现"的函数 | 看链接报错 | `LED_Init()` 有声明无实现（`.c` 整文件被注释掉） |
| 3 | 工程文件注册全了吗 | 对数 + 看 `UNRESOLVED` | 漏注册 `EEPROM.c`/`Exti.c`/`Exti_Isr.c` 等 11 个文件 |
| 4 | 有没有文件被注册两次 | 看链接 `MODULE NAME NOT UNIQUE` | `LED.c` 注册了两次 |
| 5 | 有没有空文件 / 死文件被注册 | `ls -l` 看 0 字节 | 2 个 0 字节文件 + 23 个"未收录且无人引用"的死文件 |
| 6 | 中断服务函数是不是"永远不会执行"的空壳 | 对照"该中断使能了吗" | 6 个空壳 ISR（对应中断都没使能） |
| 7 | 有没有"被写但从来没人读"的全局变量 | 搜读点 | 1 个被 5 个中断写、零读取的变量 |
| 8 | 注释说的和代码做的一致吗 | 逐条抽查（**只抽查不够，要通读**） | 本项目通读出 29 处不符 |
| 9 | 文档里的数字和当前代码一致吗 | 扫关键词（旧 code 值 / 旧组数 / 旧档位） | 同一件事在文档里出现过 5 个不同的 `code` 值 |
| 10 | 库目录是否被裁剪过 | 看 `.m51` 里库段大小 | 关掉不用的串口 / 删库里 26 个未调用函数，省约 12% |

> ★ **一条容易搞反的判据**：
> "某个功能没被使用" ≠ "某个函数没被调用"。
> 本项目有一个函数（`Clock_AddSeconds`）功能上确实是"日期加秒"，
> 但番茄钟走自己的倒数、贪睡走自己的逻辑 —— **它一次都没被调用过**。
> 判"死不死"只看调用点，不看"这个功能有没有用"。

---

## 十一、【第二版新增】"移除一个源文件"的正确做法

**要"从工程移除某个 `.c`"：不要改 `.uvproj`，要把该文件内容清空成注释。**

因为 **Keil 的 UV4 命令行编译会把工程的 `<File>` 列表回写 `.uvproj`**，
手工删掉的节点会被它恢复。本项目实测过：删了、编译也过了，做完一圈别的事再看 —— **它又回来了**。

清空文件内容（保留节点）后：编译不产生任何代码，而且 Keil 动不了它。

---

## 十二、【第二版新增】交付物的"版本同步"纪律

一个跑了几十轮迭代的项目，最大的文档风险不是"写错"，而是**"数字过时"**。
本项目一共 22 份 markdown，同一件事（程序空间余量）在不同文档里同时存在
5 个不同的值（`65218` / `58064` / `58869` / `58424` / `55535`）——
因为每次改完代码只刷了其中一两份。

**推荐做法**：

1. **挑一份文档当"数字的唯一出处"**（本项目是《交付说明》第一节），
   其它文档一律写"见《交付说明》第一节"，不重复贴数字；
2. 需要贴的地方，**在数字旁边写上"实测日期 + 测法"**；
3. 每次交付前用脚本扫一遍所有文档里的关键数字（旧 `code` 值、旧组数、旧档位、
   已删函数名），**扫得出来就改得干净**；
4. 文档和源码**编码可能不同**（本项目 `.c/.h` 是 GBK、`.md` 是 UTF-8），
   批量改文档时别用错写入编码。
